---
phase: quick-260709-15i
plan: 01
subsystem: testing
tags: [wow-stubs, fengari, dead-code, tech-debt]

requires:
  - phase: 07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx
    provides: "Removed Tip.spellTexture/CacheSpellTexture (the only consumer of the C_Spell.GetSpellTexture and global GetSpellTexture stubs)"
provides:
  - "spec/support/wow_stubs.lua trimmed of the orphaned C_Spell aura-texture table and global GetSpellTexture stub"
affects: [testing, wow-stubs]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - spec/support/wow_stubs.lua

key-decisions:
  - "Removed only the C_Spell table and bare GetSpellTexture global; left C_SpellBook untouched since it is still live (Twin Fangs IsSpellKnown)"

patterns-established: []

requirements-completed: [IN-01]

duration: 3min
completed: 2026-07-09
status: complete
---

# Quick Task 260709-15i: Remove orphaned C_Spell/GetSpellTexture stubs Summary

Deleted the dead `C_Spell.GetSpellTexture` table stub and global `GetSpellTexture` stub from `spec/support/wow_stubs.lua`, closing 07-REVIEW.md finding IN-01 — the fengari test harness no longer carries stubs for an API surface the addon doesn't call.

## Performance

- **Duration:** ~3min
- **Tasks:** 1 completed
- **Files modified:** 1

## Accomplishments
- Removed `_G.C_Spell = { GetSpellTexture = ... }` table and the bare `_G.GetSpellTexture` global stub from `spec/support/wow_stubs.lua`
- Confirmed `_G.C_SpellBook` (still used by `TipOfTheSpear.lua` for Twin Fangs `IsSpellKnown`) is untouched
- No double blank lines left behind after the deletion
- Full fengari suite still green: 111 passed, 0 failed

## Task Commits

1. **Task 1: Delete orphaned C_Spell/GetSpellTexture stubs from wow_stubs.lua** - `49e1f01` (chore)

_No plan-metadata commit — orchestrator handles the docs commit per sequential-executor constraints._

## Files Created/Modified
- `spec/support/wow_stubs.lua` - Removed the orphaned `C_Spell.GetSpellTexture` table stub and global `GetSpellTexture` stub (6 lines deleted); `C_SpellBook` and all other stubs unchanged

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- FOUND: spec/support/wow_stubs.lua (exists, edited)
- FOUND: 49e1f01 (commit exists in git log)
- Verified: `grep -rnE '\bC_Spell\b|GetSpellTexture' spec/ Duncedmaxxing/` returns zero matches
- Verified: `grep -Pzo '\n\n\n' spec/support/wow_stubs.lua` returns nothing (no double blank lines)
- Verified: `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 111 passed, 0 failed
