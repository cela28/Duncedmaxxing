---
phase: quick-260709-15i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/support/wow_stubs.lua
autonomous: true
requirements: [IN-01]
must_haves:
  truths:
    - "No test or production code references the C_Spell aura-texture stub or the global GetSpellTexture stub"
    - "The full fengari test suite still passes with 0 failures after removal"
  artifacts:
    - path: "spec/support/wow_stubs.lua"
      provides: "WoW API stubs for the fengari harness, minus the orphaned C_Spell/GetSpellTexture texture stubs"
      contains: "C_SpellBook"
  key_links: []
---

<objective>
Remove the orphaned `C_Spell.GetSpellTexture` table stub and the global `GetSpellTexture` stub from `spec/support/wow_stubs.lua`. These were only ever exercised by `CacheSpellTexture`, which Phase 7 removed. Nothing in `spec/` or `Duncedmaxxing/` references them anymore. Closes 07-REVIEW.md finding IN-01.

Purpose: Keep the test harness stub surface aligned with actual API usage — dead stubs mask which WoW APIs the addon truly depends on.
Output: A trimmed `spec/support/wow_stubs.lua` with no double blank lines and a green fengari suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-REVIEW.md
@spec/support/wow_stubs.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned C_Spell/GetSpellTexture stubs from wow_stubs.lua</name>
  <files>spec/support/wow_stubs.lua</files>
  <read_first>spec/support/wow_stubs.lua</read_first>
  <action>
    Delete the `_G.C_Spell` table block (the three lines: `_G.C_Spell = {`, `GetSpellTexture = function(id) return 132275 end,`, and its closing `}`) and the `_G.GetSpellTexture = function(id) return 132275 end` line. These currently sit between the `_G.IsPlayerSpell` stub and the `_G.UnitClass` stub. Also collapse the resulting blank lines so the `_G.IsPlayerSpell` stub is followed by exactly one blank line and then the `_G.UnitClass` stub — no double blank lines left behind.

    DO NOT touch the `_G.C_SpellBook` table stub — it is a distinct, still-live stub referenced by `Duncedmaxxing/Modules/TipOfTheSpear.lua` (IsSpellKnown for Twin Fangs). Only the `C_Spell` aura-texture table and the bare global `GetSpellTexture` are being removed.
  </action>
  <verify>
    <automated>! grep -nE '(^|[^B])\bC_Spell\b|GetSpellTexture' spec/support/wow_stubs.lua && ! grep -Pzo '\n\n\n' spec/support/wow_stubs.lua && npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>
    `spec/support/wow_stubs.lua` no longer contains the `C_Spell` texture table or the global `GetSpellTexture` stub, `C_SpellBook` is still present, there are no double blank lines, and the full fengari suite reports 0 failed.
  </done>
</task>

</tasks>

<verification>
Run from repo root:

1. Word-boundary grep confirms no lingering references (must exclude the legitimate `C_SpellBook`):
   `grep -rnE '\bC_Spell\b|GetSpellTexture' spec/ Duncedmaxxing/` returns zero matches.
   (Plain `C_Spell` without a boundary would false-match `C_SpellBook`, which must remain.)
2. No double blank lines introduced: `grep -Pzo '\n\n\n' spec/support/wow_stubs.lua` returns nothing.
3. Full suite green: `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed.
</verification>

<success_criteria>
- `_G.C_Spell` table and `_G.GetSpellTexture` global removed from `spec/support/wow_stubs.lua`.
- `_G.C_SpellBook` untouched.
- No double blank lines in the edited region.
- fengari suite: 0 failed.
</success_criteria>

<output>
Create `.planning/quick/260709-15i-remove-orphaned-c-spell-getspelltexture-/260709-15i-SUMMARY.md` when done.
</output>
