---
phase: quick-260623-x5d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Core.lua
  - Duncedmaxxing/Options.lua
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
  - spec/core_spec.lua
  - spec/tip_spec.lua
autonomous: true
requirements: [QUICK-X5D]

must_haves:
  truths:
    - "Tracker is shown regardless of combat state (no combat gating of visibility)"
    - "hideWhenEmpty visibility toggle still works unchanged"
    - "No showOnlyInCombat key, UI checkbox, or slash command remains anywhere in the addon source"
    - "inCombat tracking remains intact (used by prediction grace-suppression and aura-verify timing)"
    - "Full spec suite passes green via npx fengari runner"
  artifacts:
    - path: "Duncedmaxxing/Core.lua"
      provides: "DEFAULTS without showOnlyInCombat, slash handler without combat branch"
    - path: "Duncedmaxxing/Options.lua"
      provides: "Options window without the Combat only checkbox"
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Tip:Update without the showOnlyInCombat visibility gate"
  key_links:
    - from: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      to: "Duncedmaxxing/Core.lua DEFAULTS.tip"
      via: "Tip:Update reads cfg.hideWhenEmpty (kept) but no longer reads cfg.showOnlyInCombat"
      pattern: "hideWhenEmpty"
---

<objective>
Remove the `showOnlyInCombat` display feature entirely so the Tip-of-the-Spear tracker is always shown regardless of combat state. The buff stacks expire on their own after ~10s (BUFF_DURATION=10), so combat-gated visibility is unnecessary.

Purpose: Simplify visibility logic and stop the tracker from hiding out of combat.
Output: `showOnlyInCombat` removed from defaults, slash command, options UI, the visibility gate, and tests. `hideWhenEmpty` left fully intact. `inCombat` tracking left fully intact (it is still used by prediction grace-suppression and aura-verify timing — only its use for visibility gating is removed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@Duncedmaxxing/Core.lua
@Duncedmaxxing/Options.lua
@Duncedmaxxing/Modules/TipOfTheSpear.lua
@spec/core_spec.lua
@spec/tip_spec.lua

Verified anchor points (current line numbers):
- Core.lua:22 — `showOnlyInCombat = true,` in DEFAULTS.tip
- Core.lua:158 — PrintHelp line includes `combat on|off`
- Core.lua:297-305 — slash command branch `command == "combat" or command == "combatonly"` setting `db.tip.showOnlyInCombat = enabled`
- Options.lua:254-256 — `CreateCheckbox(window, "Combat only", ...)` getter/setter
- TipOfTheSpear.lua:596-598 — visibility gate `if shouldShow and cfg.showOnlyInCombat and not self.inCombat ...`
- core_spec.lua:64, 156 — `showOnlyInCombat = true,` inside two test DB-fixture builders
- tip_spec.lua:594 — `db.tip.showOnlyInCombat = false` in a before_each (becomes inert; remove for cleanliness)

CRITICAL — do NOT touch `inCombat` tracking. `inCombat` is still referenced at TipOfTheSpear.lua:356 (SyncFromAura grace), :448 and :459 (ScheduleAuraVerify timing), :691/:695/:744 (event handlers), and across tip_spec.lua tests (285-464). Removing it would break prediction logic and tests. Only remove its use inside the showOnlyInCombat visibility branch.

CRITICAL — do NOT touch `hideWhenEmpty`. It is a separate, default-off toggle with its own DEFAULTS key (Core.lua:23), its own checkbox (Options.lua:257-259), and its own visibility branch (TipOfTheSpear.lua:599-601). Leave all of it intact.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove showOnlyInCombat from Core.lua (default, slash command, help text)</name>
  <files>Duncedmaxxing/Core.lua</files>
  <action>
Make three edits to Duncedmaxxing/Core.lua:

1. In the DEFAULTS.tip table (~line 22), delete the entire line that sets `showOnlyInCombat = true,`. Leave the adjacent `enabled` and `hideWhenEmpty` lines intact.

2. In the slash command handler, delete the entire `elseif` branch that handles the combat toggle (~lines 297-305): the branch beginning `elseif command == "combat" or command == "combatonly" then` through its closing `end`, including the `ParseOnOff(rest)` call, the `db.tip.showOnlyInCombat = enabled` assignment, the RefreshTip/Print calls, and the usage-error else clause. Remove the whole branch so the next branch (`elseif command == "resetstyle" ...`) chains directly off the preceding `empty`/`emptyalpha` branch.

3. In PrintHelp (~line 158), edit the help string `"/dmax mode bar|number, size 247 10, border 1, combat on|off"` to drop the `, combat on|off` segment so it reads `"/dmax mode bar|number, size 247 10, border 1"`.

Do NOT modify NormalizeDB: it never references showOnlyInCombat by name (it only validates displayMode and clears deprecated barWidth/barHeight/spacing keys). A persisted showOnlyInCombat key is simply left as an inert unused field on db.tip after migration, which is harmless. Confirm by reading NormalizeDB — no changes needed there.

Verify ParseOnOff is still used elsewhere or becomes unused: after removing the combat branch, check whether `ParseOnOff` (imported at the top via `local ParseOnOff = DMX.Util.ParseOnOff`) has any remaining callers in Core.lua. Grep `ParseOnOff` in Core.lua. If the combat branch was its only caller, leave the import line as-is (it is a harmless unused local and removing it is out of scope) — do NOT remove the import unless trivially confirmed unused AND you choose to; either way the suite must stay green.
  </action>
  <verify>
    <automated>grep -c 'showOnlyInCombat' Duncedmaxxing/Core.lua | grep -qx 0 && echo CORE_CLEAN</automated>
  </verify>
  <done>Duncedmaxxing/Core.lua contains zero occurrences of showOnlyInCombat. DEFAULTS.tip no longer has the key; the combat slash branch is gone; the help text no longer mentions combat on|off. hideWhenEmpty and inCombat usage untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the Combat only checkbox from Options.lua and the visibility gate from TipOfTheSpear.lua</name>
  <files>Duncedmaxxing/Options.lua, Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <action>
Edit Duncedmaxxing/Options.lua (~lines 254-256): delete the entire `CreateCheckbox(window, "Combat only", 126, -80, ...)` call including both its getter `function() return GetCfg().showOnlyInCombat end` and setter `function(value) GetCfg().showOnlyInCombat = value end` lines. Leave the preceding `Enabled` checkbox (~251-253) and the following `Hide empty` checkbox (~257-259) fully intact. Do not reflow the other two checkboxes' x-positions — leaving a gap at x=126 is acceptable and out of scope to adjust.

Edit Duncedmaxxing/Modules/TipOfTheSpear.lua in Tip:Update (~lines 596-598): delete the entire visibility-gate block:
the `if shouldShow and cfg.showOnlyInCombat and not self.inCombat and not self.testMode and not unlocked then` line, its body line `shouldShow = false`, and its closing `end`. Leave the immediately following hideWhenEmpty block (`if shouldShow and cfg.hideWhenEmpty and stacks == 0 ...`) completely intact. Do NOT remove or alter any other use of `self.inCombat` in the file — it remains required by SyncFromAura grace-suppression and ScheduleAuraVerify timing.
  </action>
  <verify>
    <automated>! grep -rq 'showOnlyInCombat' Duncedmaxxing/Options.lua Duncedmaxxing/Modules/TipOfTheSpear.lua && grep -q 'hideWhenEmpty' Duncedmaxxing/Modules/TipOfTheSpear.lua && grep -q 'self.inCombat' Duncedmaxxing/Modules/TipOfTheSpear.lua && echo SRC_CLEAN_KEPT_OK</automated>
  </verify>
  <done>Options.lua and TipOfTheSpear.lua contain zero occurrences of showOnlyInCombat. The Combat only checkbox is gone; the showOnlyInCombat visibility gate in Tip:Update is gone. hideWhenEmpty branch and all other self.inCombat references remain present.</done>
</task>

<task type="auto">
  <name>Task 3: Strip showOnlyInCombat from specs and confirm the suite passes green</name>
  <files>spec/core_spec.lua, spec/tip_spec.lua</files>
  <action>
Edit spec/core_spec.lua: remove the two `showOnlyInCombat = true,` lines inside the test DB-fixture builders at ~line 64 (the `migrationDB` builder) and ~line 156 (the `migratedDB` builder). These are just fixture fields and removing them leaves the surrounding fixture tables valid.

Then add one small assertion proving a persisted showOnlyInCombat key does not break NormalizeDB. In the `describe("NormalizeDB — deprecated fields ignored post-migration (QUAL-03)", ...)` block (or an equivalently scoped already-migrated block), add a new `it(...)` test that: builds a migrated DB via the local `migratedDB` helper with `{ showOnlyInCombat = true }` as an override, calls `DMX._test.NormalizeDB(db)`, and asserts the actual post-removal behavior. NormalizeDB does NOT strip arbitrary keys in the already-migrated branch (it only validates displayMode), so the inert key is left untouched — assert `assert.is_true(db.tip.showOnlyInCombat)` to document that the leftover key is harmlessly preserved and NormalizeDB still completes without error. Confirm this matches the actual NormalizeDB code before asserting; if for any reason the value differs, assert whatever is actually true. Do NOT place the literal token showOnlyInCombat inside any negative-grep target file — it only lives in spec files here, which the negative grep in this plan scopes to Duncedmaxxing/, so spec usage is fine.

Edit spec/tip_spec.lua: remove the now-inert line `db.tip.showOnlyInCombat = false` (~line 594) inside the `describe("Tip:Update number mode color coding", ...)` before_each. Since the visibility gate no longer reads showOnlyInCombat, this line is dead; removing it keeps the fixture clean. Do NOT touch any `Tip.inCombat = ...` assignments elsewhere in tip_spec.lua — those exercise prediction/grace logic and must stay.

Run the full suite and confirm green. The runner requires the npx form because fengari is not installed locally.
  </action>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs 2>&1 | tail -5 | grep -q '0 failed'</automated>
  </verify>
  <done>Spec suite passes with 0 failed. core_spec.lua fixture builders no longer carry showOnlyInCombat; a new assertion documents NormalizeDB's handling of a leftover showOnlyInCombat key. tip_spec.lua no longer sets db.tip.showOnlyInCombat. All Tip.inCombat-based tests still pass.</done>
</task>

</tasks>

<verification>
Full removal + regression verification:
- `grep -rn showOnlyInCombat Duncedmaxxing/` returns no matches (negative grep, scoped to addon source).
- `npx -y -p fengari@0.1.5 node spec/run.cjs` exits 0 with `N passed, 0 failed`.
- Manual code-read confirmation: hideWhenEmpty default/checkbox/branch all still present; self.inCombat still referenced in SyncFromAura, ScheduleAuraVerify, and event handlers.
</verification>

<success_criteria>
- showOnlyInCombat removed entirely from Duncedmaxxing/Core.lua, Options.lua, Modules/TipOfTheSpear.lua.
- Tracker visibility no longer gated by combat state; tracker shows in and out of combat (subject only to enabled/isSurvival/hideWhenEmpty/lock/test rules).
- hideWhenEmpty untouched. inCombat tracking untouched (only its visibility-gating use removed).
- `grep -rn showOnlyInCombat Duncedmaxxing/` returns nothing.
- Spec suite green via the npx fengari runner.
</success_criteria>

<output>
Create `.planning/quick/260623-x5d-remove-the-showonlyincombat-display-feat/260623-x5d-SUMMARY.md` when done
</output>
