---
phase: 260624-0hx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Core.lua
  - README.md
autonomous: true
requirements: [SLASH-SETTINGS-ONLY, README-DEBLOAT]

must_haves:
  truths:
    - "Typing /dmax or /duncedmaxxing with any input opens the settings window"
    - "No slash subcommand branches (lock, mode, size, color, etc.) remain in Core.lua"
    - "PrintHelp is fully removed and never referenced"
    - "Options.lua still works — every setting remains adjustable in the settings window"
    - "README describes exactly two display modes (segmented bar OR plain number) and an always-show tracker"
    - "spec suite passes with 0 failures via the npx fengari runner"
  artifacts:
    - path: "Duncedmaxxing/Core.lua"
      provides: "Settings-only slash dispatcher"
      contains: "DMX:OpenOptions"
    - path: "README.md"
      provides: "De-bloated, accurate README"
      contains: "settings window"
  key_links:
    - from: "Duncedmaxxing/Core.lua"
      to: "Duncedmaxxing/Options.lua"
      via: "slash handler calls DMX:OpenOptions() which opens the Options window"
      pattern: "DMX:OpenOptions"
---

<objective>
Reduce the addon's slash interface to settings-only and de-bloat/de-stale the README.

`/dmax` and `/duncedmaxxing` should open the settings window for ANY input. All per-setting
slash subcommands are removed (configuration now lives entirely in the settings UI, which is
untouched and must keep working). The README is trimmed to match current reality: two display
modes (segmented bar or plain stack number), an always-showing tracker, and a single command
statement.

Purpose: Eliminate a bloated, drifting slash command surface and stale documentation. The
settings window already exposes every option, so the slash commands are redundant maintenance load.

Output: Trimmed `Duncedmaxxing/Core.lua` slash dispatcher + removed PrintHelp; refreshed `README.md`;
spec suite still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@Duncedmaxxing/Core.lua
@Duncedmaxxing/Options.lua
@Duncedmaxxing/Util.lua
@README.md
@spec/run.cjs

# Dead-code safety facts already established during planning (do not re-derive):
# - Trim, Clamp, ParseHexColor, ParseOnOff are OWNED BY Util.lua and used by Options.lua + spec/util_spec.lua.
#   Util.lua is NOT touched by this plan. Do NOT remove anything from Util.lua.
# - In Core.lua header (lines 4-7): after trimming the dispatcher, the local aliases
#   `Clamp`, `ParseHexColor`, `ParseOnOff` become unused. The `Trim` alias STAYS (still normalizes input).
# - Module methods RefreshTip / DMX:ResetTipStyle / Tip:SetTestStacks / Tip:ResetPosition / Tip:ApplyLock
#   are ALL still referenced by Options.lua and/or spec/tip_spec.lua. KEEP THEM ALL.
#   The file-local RefreshTip(tip) and DMX:RefreshTip()/DMX:ResetTipStyle() must remain (used by Options + tip_spec).
# - CopyDefaults must remain (used by NormalizeDB, ResetTipStyle, and DMX._test).
# - No spec exercises slash subcommands or PrintHelp (only wow_stubs.lua resets SLASH_* globals).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce slash dispatcher to settings-only and remove PrintHelp</name>
  <files>Duncedmaxxing/Core.lua</files>
  <action>
In the `SlashCmdList.DUNCEDMAXXING` handler (currently ~lines 191-308), replace the entire
if/elseif command-branch chain with a settings-only dispatcher. For ANY input, call
`DMX:OpenOptions()`. Keep the graceful fallback when `DMX.OpenOptions` is unavailable, but that
fallback must NOT call PrintHelp (it is being removed) — print a short message via `DMX:Print`
such as a settings-UI-unavailable notice, or do nothing.

Keep BOTH slash token registrations exactly as-is: `SLASH_DUNCEDMAXXING1 = "/duncedmaxxing"` and
`SLASH_DUNCEDMAXXING2 = "/dmax"`.

Remove ALL subcommand branches: options/config, help/commands, lock, unlock/move, reset, show,
hide, test, scale, mode, size/barsize, border, color/fill, empty/emptyalpha,
resetstyle/defaultstyle, and the numeric stack-preview `else` fallback.

The simplest correct handler body is: register both SLASH tokens, then
`SlashCmdList.DUNCEDMAXXING = function() ... end` that calls `DMX:OpenOptions()` (guarded by
`if DMX.OpenOptions then ... else <short message or nothing> end`). The input arg is no longer
parsed, so the `Trim(input)` / `input:match` / `command`/`rest` parsing inside the handler is
removed along with the branches.

Remove the now-unused `PrintHelp` function (currently ~lines 154-158).

DEAD-CODE CLEANUP (Core.lua header only): since the dispatcher no longer parses input or mutates
settings, the local aliases at the top of Core.lua become unused — remove `local Clamp = DMX.Util.Clamp`,
`local ParseHexColor = DMX.Util.ParseHexColor`, and `local ParseOnOff = DMX.Util.ParseOnOff`.
The `local Trim = DMX.Util.Trim` alias is only kept if the final handler still references Trim;
since the trimmed handler ignores its input, REMOVE the `Trim` alias too. Confirm by grepping
Core.lua after editing (see verify) that none of Clamp/ParseHexColor/ParseOnOff/Trim remain referenced
in Core.lua before deleting their alias lines.

DO NOT remove or alter: `RefreshTip` (file-local), `DMX:RefreshTip`, `DMX:ResetTipStyle`,
`CopyDefaults`, `MergeDefaults`, `NormalizeDB`, the `DMX._test` table, or anything in Util.lua —
all are still used by Options.lua and/or specs (verified during planning). DO NOT touch
display-mode logic, tracker rendering, or hideWhenEmpty.
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && grep -nE 'resetstyle|barsize|PrintHelp|mode bar' Duncedmaxxing/Core.lua | grep -v '^[0-9]*:[[:space:]]*--' ; test -z "$(grep -nE 'resetstyle|barsize|PrintHelp|mode bar' Duncedmaxxing/Core.lua)" && echo PASS-no-stale-branches</automated>
  </verify>
  <done>
The handler calls `DMX:OpenOptions()` for any input (with a non-PrintHelp fallback); both SLASH
tokens remain registered; PrintHelp is gone; unused Core.lua header aliases removed; Options.lua,
Util.lua, and all module methods untouched. `grep -nE 'resetstyle|barsize|PrintHelp|mode bar'
Duncedmaxxing/Core.lua` returns nothing.
  </done>
</task>

<task type="auto">
  <name>Task 2: De-bloat and de-stale README.md</name>
  <files>README.md</files>
  <action>
Refresh README.md to match current reality. Make these changes:

(1) DELETE the entire "## Restriction notes" section (currently the last section, ~lines 50-52).

(2) Fix stale content referencing REMOVED features:
   - The addon has exactly TWO display modes: a segmented bar OR a plain stack number. There is
     NO icon mode. Remove every mention of "three icons", icon mode, icon size, and icon spacing.
   - The tracker now ALWAYS shows — remove "combat-only" visibility language.
   - The line describing the display as "split into three Tip segments" describes the bar mode;
     keep an accurate bar description but ensure no "icons" framing remains. Two modes only.

(3) Replace the ENTIRE "## Commands" section (the ~15-item list, currently ~lines 31-48) with a
   single concise statement: `/dmax` (alias `/duncedmaxxing`) opens the movable settings window,
   and ALL configuration is done there. No command list.

(4) Update the "## Settings" bullet list (~lines 21-29) to drop icon mode, combat-only, icon size,
   and icon spacing so it matches the two-mode, always-show reality. Keep it tight — no bloat.
   The "v1-style" framing line may stay or be trimmed; do not introduce new bloat.

Keep the README accurate and concise. Do not invent features. Do not touch the "## Tip of the Spear"
mechanics section's spell-ID list (it is accurate).
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && test -z "$(grep -niE 'icon|combat.?only|restriction' README.md)" && echo PASS-readme-clean || (echo "STALE REFS FOUND:" && grep -niE 'icon|combat.?only|restriction' README.md)</automated>
  </verify>
  <done>
"## Restriction notes" section is gone; no icon / combat-only / icon-size / icon-spacing mentions;
"## Commands" replaced by a single settings-window statement; Settings bullets reflect two display
modes and an always-show tracker. `grep -niE 'icon|combat.?only|restriction' README.md` returns nothing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm specs still pass and adjust any slash/PrintHelp assertions</name>
  <files>spec/core_spec.lua, spec/tip_spec.lua, spec/util_spec.lua</files>
  <action>
Run the spec suite (see verify). Planning established that NO spec exercises the removed slash
subcommands or PrintHelp — `spec/support/wow_stubs.lua` only resets `SLASH_DUNCEDMAXXING1/2` globals,
and `spec/tip_spec.lua` tests `DMX:RefreshTip()` directly (which is retained). So no spec changes are
expected.

If, and only if, a test fails because it referenced a removed slash subcommand or PrintHelp, remove
or adjust ONLY that assertion to match the settings-only behavior. Do NOT weaken any unrelated coverage
(Util, Tip state machine, RefreshTip, migration). If all tests pass unchanged, make no edits here.
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && npx -y -p fengari@0.1.5 node spec/run.cjs 2>&1 | tail -20</automated>
  </verify>
  <done>
`npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failures. Any spec edits (if needed) only
touched assertions tied to removed slash subcommands or PrintHelp; all other coverage intact.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/cela/code_stuff/Duncedmaxxing && npx -y -p fengari@0.1.5 node spec/run.cjs` — 0 failures (plain `node spec/run.cjs` fails; npx form required).
2. `grep -nE 'resetstyle|barsize|PrintHelp|mode bar' Duncedmaxxing/Core.lua` — returns nothing.
3. `grep -niE 'icon|combat.?only|restriction' README.md` — returns nothing.
4. Sanity: `grep -nE 'SLASH_DUNCEDMAXXING1|SLASH_DUNCEDMAXXING2|DMX:OpenOptions' Duncedmaxxing/Core.lua` — both tokens and the OpenOptions call present.
5. Sanity: Options.lua untouched (`git diff --stat` shows only Core.lua, README.md, and possibly spec files changed).
</verification>

<success_criteria>
- `/dmax` and `/duncedmaxxing` open the settings window for any input; no subcommands remain.
- PrintHelp removed; unused Core.lua header aliases removed; Util.lua and Options.lua intact.
- README de-bloated and accurate: two display modes, always-show tracker, single command statement,
  no "Restriction notes" section.
- Spec suite passes with 0 failures.
</success_criteria>

<output>
Create `.planning/quick/260624-0hx-reduce-slash-interface-to-settings-only-/260624-0hx-SUMMARY.md` when done.
</output>
