---
phase: quick-260709-3v5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
autonomous: true
requirements: [LINT-DEADCODE]

must_haves:
  truths:
    - "luacheck no longer reports 'unused function LayoutPipBorder'"
    - "grep for LayoutPipBorder returns zero hits across Duncedmaxxing/ and spec/"
    - "fengari test suite passes with 0 failures (no behavior change)"
  artifacts:
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "TipOfTheSpear module with LayoutPipBorder removed"
      contains: "local function EnsureFrame"
  key_links: []
---

<objective>
Remove the dead `local function LayoutPipBorder(pip, size)` from `Duncedmaxxing/Modules/TipOfTheSpear.lua`. It has zero call sites (a leftover the Phase 7 dead-code sweep missed) and is the sole luacheck warning failing the dev-push lint CI.

Purpose: Get the luacheck CI job (`.github/workflows/lint.yml`) green by eliminating its one and only finding.
Output: `TipOfTheSpear.lua` with the LayoutPipBorder definition deleted; no behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Duncedmaxxing/Modules/TipOfTheSpear.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead LayoutPipBorder function</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <action>Delete the entire `local function LayoutPipBorder(pip, size)` definition (lines 263–289 inclusive: from the `local function LayoutPipBorder(pip, size)` header through its closing `end`), plus the single blank line 290 that trailed it. The function body ends at the `end` on line 289, immediately before the blank line and then `local function EnsureFrame(tip)`. After deletion, `SetPipBordersShown` (ending on line 261) must be followed by exactly one blank line, then `local function EnsureFrame(tip)`. Do NOT modify SetPipBordersShown, EnsureFrame, or any other function — this is a pure removal of never-called code. Verified by orchestrator: this is the only reference to LayoutPipBorder anywhere in Duncedmaxxing/ and spec/, so no call sites or imports need updating.</action>
  <verify>
    <automated>test -z "$(grep -rn 'LayoutPipBorder' Duncedmaxxing/ spec/)" && npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>`grep -rn 'LayoutPipBorder' Duncedmaxxing/ spec/` returns zero hits; the fengari suite (`npx -y -p fengari@0.1.5 node spec/run.cjs`) reports 0 failed; `SetPipBordersShown`'s closing `end` is separated from `local function EnsureFrame(tip)` by exactly one blank line.</done>
</task>

</tasks>

<verification>
- `grep -rn 'LayoutPipBorder' Duncedmaxxing/ spec/` → zero hits
- `npx -y -p fengari@0.1.5 node spec/run.cjs` → 0 failed
- luacheck (in CI) no longer reports the `unused function LayoutPipBorder` warning
</verification>

<success_criteria>
LayoutPipBorder is fully removed from TipOfTheSpear.lua, the fengari suite is green, and the surrounding functions (SetPipBordersShown, EnsureFrame) are untouched.
</success_criteria>

<output>
Create `.planning/quick/260709-3v5-remove-dead-unused-local-function-layout/260709-3v5-SUMMARY.md` when done
</output>
