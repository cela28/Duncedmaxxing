---
phase: 03-bug-fixes-with-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Core.lua
  - spec/core_spec.lua
  - spec/tip_spec.lua
autonomous: true
requirements: [BUG-01, BUG-02, QUAL-03]

must_haves:
  truths:
    - "auraVerifyPending is false after the serial-mismatch early return in ScheduleAuraVerify timer callback"
    - "Switching display modes out of combat shows stack count synced from live aura state"
    - "NormalizeDB does not map deprecated barWidth/barHeight/spacing fields when the DB is already migrated"
    - "busted spec/ passes with zero failures after all changes"
  artifacts:
    - path: "spec/tip_spec.lua"
      provides: "BUG-01 serial-mismatch flag test, BUG-02 mode-switch SyncFromAura test"
      contains: "auraVerifyPending"
    - path: "spec/core_spec.lua"
      provides: "QUAL-03 updated deprecated-field tests confirming dead block removal"
      contains: "does not map barWidth"
    - path: "Duncedmaxxing/Core.lua"
      provides: "Dead block removed from NormalizeDB, RefreshTip calls SyncFromAura out of combat"
  key_links:
    - from: "Duncedmaxxing/Core.lua"
      to: "Tip:SyncFromAura"
      via: "RefreshTip local function"
      pattern: "tip:SyncFromAura"
---

<objective>
Fix BUG-01 (stuck auraVerifyPending flag — verify already fixed, add regression test), BUG-02 (stale display on mode switch — add SyncFromAura to RefreshTip), and QUAL-03 (dead migration fallback removal + test updates).

Purpose: Three targeted low-risk fixes that harden aura verification, eliminate stale displays on settings changes, and remove unreachable dead code — all confirmed by the existing and extended test suite.
Output: Updated Core.lua with dead block removed and RefreshTip enhanced; extended tip_spec.lua with BUG-01 and BUG-02 regression tests; updated core_spec.lua deprecated-field tests.
</objective>

<execution_context>
@/home/cela/.claude/get-shit-done/workflows/execute-plan.md
@/home/cela/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bug-fixes-with-test-coverage/03-CONTEXT.md
@.planning/phases/03-bug-fixes-with-test-coverage/03-RESEARCH.md
@.planning/phases/03-bug-fixes-with-test-coverage/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Duncedmaxxing/Core.lua (lines 173-183):
```lua
local function RefreshTip(tip)
    if tip and tip.RefreshLayout then
        tip:RefreshLayout()
    elseif tip and tip.Update then
        tip:Update()
    end
end

function DMX:RefreshTip()
    RefreshTip(self:GetModule("tip"))
end
```

From Duncedmaxxing/Core.lua (lines 98-106, dead block to remove):
```lua
if tip.barWidth then
    tip.width = tip.barWidth
end
if tip.barHeight then
    tip.height = tip.barHeight
end
if tip.spacing and not tip.borderSize then
    tip.borderSize = tip.spacing
end
```

From spec/tip_spec.lua (lines 267-314, ScheduleCastVerify serial-mismatch pattern):
```lua
-- Spy pattern for counting SyncFromAura calls
local syncCallCount = 0
local originalSync  = Tip.SyncFromAura
Tip.SyncFromAura    = function(self)
    syncCallCount = syncCallCount + 1
    return originalSync(self)
end
```

From spec/core_spec.lua (lines 200-216, migratedDB helper):
```lua
local function migratedDB(tipOverrides)
    local db = {
        settingsMigration = "0.3.2-fontfix",
        tip = { enabled = true, displayMode = "bar", x = 0, y = -160, scale = 1, optionsX = 360, optionsY = 170 },
    }
    if tipOverrides then
        for k, v in pairs(tipOverrides) do db.tip[k] = v end
    end
    return db
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-01 regression test and QUAL-03 dead block removal with test updates</name>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (lines 425-440 — ScheduleAuraVerify timer callback to confirm flag placement)
    - Duncedmaxxing/Core.lua (lines 74-111 — NormalizeDB including migration gate and dead block)
    - spec/tip_spec.lua (lines 264-335 — existing ScheduleCastVerify serial-mismatch tests for pattern)
    - spec/core_spec.lua (lines 193-242 — deprecated field migration describe block with 4 tests to update)
  </read_first>
  <files>spec/tip_spec.lua, Duncedmaxxing/Core.lua, spec/core_spec.lua</files>
  <action>
**BUG-01 — regression test (no code change needed per D-06):**
The live code at TipOfTheSpear.lua line 431 already places `self.auraVerifyPending = false` before the serial-mismatch check at line 432. CONCERNS.md may describe an older state. Write a test that explicitly verifies the flag is cleared on the early-return path.

In spec/tip_spec.lua, add a new describe block "Tip:ScheduleAuraVerify — auraVerifyPending flag (BUG-01)" after the existing ScheduleCastVerify block. The test should:
- Call `Tip:ApplySpell("generator")` to trigger ScheduleCastVerify which calls ScheduleAuraVerify
- Set `Tip.inCombat = true`
- Call `Tip:ApplySpell("generator")` again to increment castVerifySerial (making the first timer's serial stale)
- Advance the mock clock past `AURA_VERIFY_DELAY + 0.1` to fire the first timer
- Assert `Tip.auraVerifyPending == false` — the flag must be cleared even though the serial-mismatch early return fired
- Follow the existing `before_each` pattern: `DMX, Tip, clock = loader.load(); loader.resetTipState(Tip, clock)`

**QUAL-03 — dead block removal (per D-08, D-09):**
In Duncedmaxxing/Core.lua, delete the 9 lines at lines 98-106 (the three `if tip.barWidth`/`if tip.barHeight`/`if tip.spacing` blocks). Do NOT change the `SETTINGS_MIGRATION` constant value. The migration gate at lines 77-96 already clears barWidth/barHeight/spacing and sets settingsMigration, making the deleted block unreachable for any migrated DB.

In spec/core_spec.lua, update the `"NormalizeDB — deprecated field migration (always runs post-gate)"` describe block. Rename the describe to `"NormalizeDB — deprecated fields ignored post-migration (QUAL-03)"`. Update all 4 `it()` blocks:

1. "migrates barWidth to width" becomes "does not map barWidth to width when already migrated" — assert `db.tip.width` is nil (not 300). The migratedDB helper provides `settingsMigration = "0.3.2-fontfix"` so the migration gate is skipped, and the dead block is gone — barWidth has no effect.
2. "migrates barHeight to height" becomes "does not map barHeight to height when already migrated" — assert `db.tip.height` is nil.
3. "migrates spacing to borderSize when borderSize is absent" becomes "does not map spacing to borderSize when already migrated" — assert `db.tip.borderSize` is nil (spacing has no effect).
4. "does not overwrite existing borderSize with spacing" stays as-is — still assert `db.tip.borderSize == 2` (spacing should not affect it, same as before).

Also add one new test "NormalizeDB idempotency — double call does not wipe settings" that calls `NormalizeDB` twice on the same db table and asserts that `db.tip.enabled`, `db.tip.displayMode`, `db.tip.x`, `db.tip.y`, `db.tip.scale` remain unchanged after the second call.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing && busted spec/tip_spec.lua && busted spec/core_spec.lua</automated>
  </verify>
  <acceptance_criteria>
    - spec/tip_spec.lua contains `auraVerifyPending` assertion in a BUG-01 test block
    - The BUG-01 test passes without any code change to TipOfTheSpear.lua (confirming fix was already present)
    - Duncedmaxxing/Core.lua no longer contains `tip.barWidth` outside the migration gate block (lines 77-96)
    - grep -c "tip.barWidth" Duncedmaxxing/Core.lua returns 1 (only inside migration gate, line ~91 where it is cleared to nil)
    - spec/core_spec.lua contains "does not map barWidth" in an it() description
    - spec/core_spec.lua contains an idempotency test calling NormalizeDB twice
    - busted spec/tip_spec.lua exits 0 with 0 failures
    - busted spec/core_spec.lua exits 0 with 0 failures
  </acceptance_criteria>
  <done>BUG-01 has a regression test proving the flag clears on serial-mismatch. QUAL-03 dead block is removed from Core.lua. All 4 deprecated-field tests updated to confirm no mapping occurs. NormalizeDB idempotency test passes.</done>
</task>

<task type="auto">
  <name>Task 2: BUG-02 — out-of-combat SyncFromAura on RefreshTip and regression test</name>
  <read_first>
    - Duncedmaxxing/Core.lua (lines 173-183 — RefreshTip local function and DMX:RefreshTip method)
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (lines 697-706 — PLAYER_REGEN_ENABLED handler showing existing out-of-combat SyncFromAura pattern)
    - spec/tip_spec.lua (lines 130-145 — SyncFromAura describe block for mockAura pattern reference)
    - spec/support/init.lua (lines 52-66 — resetTipState for mockAura.impl reset)
  </read_first>
  <files>Duncedmaxxing/Core.lua, spec/tip_spec.lua</files>
  <action>
**BUG-02 code fix (per D-07):**
In Duncedmaxxing/Core.lua, modify the `RefreshTip` local function (currently lines 173-179 after Task 1's edits). Add an unconditional out-of-combat SyncFromAura call before the existing layout refresh logic. The updated function should:
1. Check `if tip and not tip.inCombat then tip:SyncFromAura() end` as the first statement
2. Then proceed with the existing `if tip and tip.RefreshLayout then tip:RefreshLayout() elseif tip and tip.Update then tip:Update() end`

This triggers on ALL non-combat RefreshTip calls (mode switch, size change, color change, border change) rather than mode-change-only. This is simpler and covers all stale-display paths. `ReadLiveState` already handles API absence gracefully via the `if not GetPlayerAuraBySpellID then return nil, nil end` guard, so no additional protection is needed. The `not tip.inCombat` gate prevents fighting with the combat prediction system.

**BUG-02 regression test:**
In spec/tip_spec.lua, add a new describe block "RefreshTip — out-of-combat aura sync (BUG-02)" at the end of the file. Use the same `before_each` pattern (`DMX, Tip, clock = loader.load(); loader.resetTipState(Tip, clock)`).

Add these tests:
1. "syncs stale stacks to 0 on RefreshTip when out of combat and aura is absent" — set `Tip.stacks = 2`, `Tip.expiresAt = clock.now + 5`, `Tip.inCombat = false`. The default `stubs.mockAura.impl` returns nil (absent aura). Call `DMX:RefreshTip()`. Assert `Tip.stacks == 0` and `Tip.expiresAt == 0`.
2. "syncs stale stacks to live aura on RefreshTip when out of combat" — set `Tip.stacks = 0`, `Tip.inCombat = false`, override `stubs.mockAura.impl` to return `stubs.makeAuraData({ applications = 2, expirationTime = clock.now + 8 })`. Call `DMX:RefreshTip()`. Assert `Tip.stacks == 2`.
3. "does not call SyncFromAura during combat on RefreshTip" — set `Tip.stacks = 2`, `Tip.inCombat = true`, keep mockAura returning nil. Spy on `Tip.SyncFromAura`. Call `DMX:RefreshTip()`. Assert the spy was not called and `Tip.stacks` is still 2.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing && busted spec/tip_spec.lua && busted spec/core_spec.lua && busted</automated>
  </verify>
  <acceptance_criteria>
    - Duncedmaxxing/Core.lua RefreshTip function contains `tip:SyncFromAura()` gated on `not tip.inCombat`
    - spec/tip_spec.lua contains describe block with "BUG-02" in the description
    - spec/tip_spec.lua contains "syncs stale stacks to 0" test
    - spec/tip_spec.lua contains "does not call SyncFromAura during combat" test
    - busted spec/tip_spec.lua exits 0 with 0 failures
    - busted (full suite) exits 0 with 0 failures and 0 pending (pending count may still be 1 if BUG-04 pending is not yet resolved — that is Plan 2's job)
  </acceptance_criteria>
  <done>RefreshTip calls SyncFromAura when out of combat before refreshing layout. Three BUG-02 regression tests pass: absent aura zeroes stacks, present aura syncs stacks, combat mode blocks the sync. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SavedVariables → NormalizeDB | Persisted DB could contain arbitrary deprecated fields from older addon versions |
| WoW API → SyncFromAura | Aura API returns may be nil or malformed; ReadLiveState wraps in pcall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | NormalizeDB dead block removal | accept | Dead block was unreachable for migrated DBs; migration gate already clears deprecated fields. No behavior change for any user who has loaded the addon at least once. |
| T-03-02 | Denial of Service | RefreshTip SyncFromAura call | accept | SyncFromAura is gated on `not inCombat`; ReadLiveState wraps API call in pcall. No combat-path performance impact. |
| T-03-03 | Information Disclosure | SavedVariables | accept | No PII stored; stale deprecated fields (barWidth etc.) are harmless remnants. No new data exposed. |
</threat_model>

<verification>
1. `busted` — full suite passes with 0 failures
2. `luacheck Duncedmaxxing/ --no-unused-args` — 0 warnings
3. `grep -c "tip.barWidth" Duncedmaxxing/Core.lua` — returns exactly 1 (only the nil-clearing line inside migration gate)
4. `grep -c "SyncFromAura" Duncedmaxxing/Core.lua` — returns >= 1 (RefreshTip BUG-02 fix)
</verification>

<success_criteria>
- BUG-01: Regression test passes confirming auraVerifyPending is cleared on serial-mismatch path
- BUG-02: RefreshTip calls SyncFromAura out of combat; 3 tests cover absent, present, and combat-blocked scenarios
- QUAL-03: Dead block removed, 4 existing tests updated to confirm no mapping, idempotency test added
- Full test suite green, luacheck zero warnings
</success_criteria>

<output>
Create `.planning/phases/03-bug-fixes-with-test-coverage/03-01-SUMMARY.md` when done
</output>
