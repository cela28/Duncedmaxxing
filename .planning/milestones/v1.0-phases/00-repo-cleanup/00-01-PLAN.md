---
phase: 00-repo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04
  - CLN-05

must_haves:
  truths:
    - "No Zone.Identifier files exist in the git index or on disk"
    - "D-03: API_REFERENCES.md and DEVELOPMENT_NOTES.md are deleted outright with no content salvaging"
    - "D-01: .gitignore exists with comprehensive WoW addon patterns — *:Zone.Identifier, .DS_Store, Thumbs.db, .vscode/, *.swp, *~, *.bak, WTF/"
    - "D-02: .gitignore does NOT contain .planning/ — tracked on dev branches, excluded via /gsd:pr-branch"
    - "The addon root contains .toc and .lua files, a Modules/ directory, and a Media/ directory"
  artifacts:
    - path: ".gitignore"
      provides: "Prevents NTFS artifacts, OS junk, editor files, backups, and SavedVariables from being committed"
      contains: "*:Zone.Identifier"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "gitignore pattern matching"
      pattern: "\\*:Zone\\.Identifier"
---

<objective>
Remove all NTFS Zone.Identifier metadata files, delete stale documentation (API_REFERENCES.md, DEVELOPMENT_NOTES.md), create a comprehensive .gitignore for WoW addon development, and validate the folder structure against WoW addon conventions.

Purpose: Establish a clean repository baseline before any code changes begin. Zone.Identifier files are NTFS metadata artifacts that leak download-source information and have no place in a source repository. The stale docs duplicate content already captured in .planning/ artifacts.

Output: A clean working tree with .gitignore preventing future artifact accumulation, ready for Phase 1 utility extraction work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-repo-cleanup/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove artifact files and create .gitignore</name>
  <read_first>
    - .planning/phases/00-repo-cleanup/00-RESEARCH.md (complete file inventory and removal commands)
    - Duncedmaxxing.toc (verify no files being deleted are referenced in the load manifest)
  </read_first>
  <files>.gitignore</files>
  <action>
    Remove all Zone.Identifier files from the repository. There are exactly 8 files on disk: 7 tracked by git and 1 untracked.

    Step 1 — Remove 7 tracked Zone.Identifier files using a single git rm command (removes from index AND disk simultaneously). The 7 tracked paths are: Core.lua:Zone.Identifier, DEVELOPMENT_NOTES.md:Zone.Identifier, Duncedmaxxing.toc:Zone.Identifier, Media/duncedgers_pony.png:Zone.Identifier, Modules/TipOfTheSpear.lua:Zone.Identifier, Options.lua:Zone.Identifier, README.md:Zone.Identifier. Use double-quoted paths in the git rm call.

    Step 2 — Remove the 1 untracked Zone.Identifier file from disk only: API_REFERENCES.md:Zone.Identifier. This file is not in the git index, so use rm (not git rm).

    Step 3 — Remove the two stale documentation files per D-03. Use git rm (plain, NOT --cached) for both API_REFERENCES.md and DEVELOPMENT_NOTES.md. This removes them from the index AND disk in one operation.

    Step 4 — Create .gitignore in the repository root per D-01. The file must contain sections for: NTFS artifacts (*:Zone.Identifier), OS junk (.DS_Store, Thumbs.db), editor files (.vscode/, *.swp, *~), backups (*.bak), and WoW SavedVariables directories (WTF/). Per D-02, do NOT add .planning/ to .gitignore.

    IMPORTANT: Do NOT use git rm --cached for the doc files (Pitfall 1 from research). Do NOT use a glob like rm *.md (Pitfall 5). Name every file explicitly.
  </action>
  <verify>
    <automated>git ls-files "*:Zone.Identifier" | wc -l | grep -q "^0$" && echo "PASS: no Zone.Identifier in index" || echo "FAIL" ; find . -name "*:Zone.Identifier" -not -path "./.git/*" | wc -l | grep -q "^0$" && echo "PASS: no Zone.Identifier on disk" || echo "FAIL" ; git ls-files API_REFERENCES.md | wc -l | grep -q "^0$" && echo "PASS: API_REFERENCES.md gone from index" || echo "FAIL" ; [ ! -f API_REFERENCES.md ] && echo "PASS: API_REFERENCES.md gone from disk" || echo "FAIL" ; git ls-files DEVELOPMENT_NOTES.md | wc -l | grep -q "^0$" && echo "PASS: DEVELOPMENT_NOTES.md gone from index" || echo "FAIL" ; [ ! -f DEVELOPMENT_NOTES.md ] && echo "PASS: DEVELOPMENT_NOTES.md gone from disk" || echo "FAIL" ; [ -f .gitignore ] && echo "PASS: .gitignore exists" || echo "FAIL" ; grep -qF "*:Zone.Identifier" .gitignore && echo "PASS: Zone.Identifier pattern present" || echo "FAIL" ; grep -qF ".DS_Store" .gitignore && echo "PASS: .DS_Store pattern present" || echo "FAIL" ; grep -qF "*.bak" .gitignore && echo "PASS: bak pattern present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - git ls-files "*:Zone.Identifier" returns empty output (0 lines)
    - find . -name "*:Zone.Identifier" -not -path "./.git/*" returns empty output (0 lines)
    - git ls-files API_REFERENCES.md returns empty output
    - [ ! -f API_REFERENCES.md ] exits 0
    - git ls-files DEVELOPMENT_NOTES.md returns empty output
    - [ ! -f DEVELOPMENT_NOTES.md ] exits 0
    - .gitignore exists in repository root
    - .gitignore contains *:Zone.Identifier on its own line
    - .gitignore contains .DS_Store, Thumbs.db, .vscode/, *.swp, *~, *.bak, WTF/
    - .gitignore does NOT contain .planning/ anywhere (per D-02)
    - README.md, CLAUDE.md, Core.lua, Options.lua, Duncedmaxxing.toc all still exist on disk
  </acceptance_criteria>
  <done>All 8 Zone.Identifier files removed from index and disk, both stale doc files removed from index and disk, .gitignore created with all D-01 patterns, no non-target files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Validate folder structure and commit cleanup</name>
  <read_first>
    - Duncedmaxxing.toc (canonical file list — every file referenced here must still exist after cleanup)
    - .gitignore (just created in Task 1 — verify content before committing)
  </read_first>
  <files></files>
  <action>
    Validate the repository folder structure against WoW addon conventions for CLN-05. Run the following assertions and confirm all pass:

    1. At least one .toc file exists in the repository root (Duncedmaxxing.toc)
    2. At least one .lua file exists in the repository root (Core.lua, Options.lua)
    3. Modules/ directory exists and contains at least one .lua file (TipOfTheSpear.lua)
    4. Media/ directory exists and contains at least one file (duncedgers_pony.png)
    5. Every file referenced in the Duncedmaxxing.toc file list (Core.lua, Options.lua, Modules\TipOfTheSpear.lua) exists on disk

    After all assertions pass, stage .gitignore and all removals, then commit with message: chore(00): remove NTFS artifacts and stale docs, add .gitignore

    The commit must include: the 7 tracked Zone.Identifier deletions, the 2 doc file deletions (API_REFERENCES.md, DEVELOPMENT_NOTES.md), and the new .gitignore file. All removals were already staged by git rm in Task 1; only .gitignore needs explicit git add.
  </action>
  <verify>
    <automated>find . -maxdepth 1 -name "*.toc" | grep -q "." && echo "PASS: .toc present" || echo "FAIL" ; find . -maxdepth 1 -name "*.lua" | grep -q "." && echo "PASS: root .lua present" || echo "FAIL" ; [ -d Modules ] && echo "PASS: Modules/ exists" || echo "FAIL" ; [ -d Media ] && echo "PASS: Media/ exists" || echo "FAIL" ; [ -f Core.lua ] && [ -f Options.lua ] && [ -f "Modules/TipOfTheSpear.lua" ] && echo "PASS: TOC files exist" || echo "FAIL" ; git log -1 --oneline | grep -q "remove NTFS" && echo "PASS: commit exists" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - find . -maxdepth 1 -name "*.toc" produces at least one result
    - find . -maxdepth 1 -name "*.lua" produces at least two results (Core.lua, Options.lua)
    - [ -d Modules ] exits 0
    - [ -d Media ] exits 0
    - [ -f Core.lua ] && [ -f Options.lua ] && [ -f Modules/TipOfTheSpear.lua ] all exit 0
    - git log -1 --oneline contains "remove NTFS"
    - git status shows clean working tree after commit
  </acceptance_criteria>
  <done>Folder structure validated against WoW addon conventions, all cleanup changes committed in a single atomic commit, working tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase deletes files and creates .gitignore — no code executes, no input is processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | Zone.Identifier files | mitigate | Remove all Zone.Identifier files (CLN-01) — they leak NTFS download-zone metadata |
| T-00-02 | Tampering | .gitignore completeness | mitigate | .gitignore contains *:Zone.Identifier to prevent re-introduction; verification command asserts pattern presence |
</threat_model>

<verification>
Run the full CLN-01 through CLN-05 assertion battery after the commit:

```bash
# CLN-01: No Zone.Identifier in git index
test "$(git ls-files '*:Zone.Identifier' | wc -l)" -eq 0

# CLN-01: No Zone.Identifier on disk
test "$(find . -name '*:Zone.Identifier' -not -path './.git/*' | wc -l)" -eq 0

# CLN-02: API_REFERENCES.md absent
test "$(git ls-files API_REFERENCES.md | wc -l)" -eq 0 && [ ! -f API_REFERENCES.md ]

# CLN-03: DEVELOPMENT_NOTES.md absent
test "$(git ls-files DEVELOPMENT_NOTES.md | wc -l)" -eq 0 && [ ! -f DEVELOPMENT_NOTES.md ]

# CLN-04: .gitignore contains Zone.Identifier pattern
grep -qF '*:Zone.Identifier' .gitignore

# CLN-05: WoW addon structure
[ -f Duncedmaxxing.toc ] && [ -f Core.lua ] && [ -f Options.lua ] && [ -d Modules ] && [ -d Media ]
```
</verification>

<success_criteria>
- Zero Zone.Identifier files in git index and on disk
- API_REFERENCES.md and DEVELOPMENT_NOTES.md absent from repository (index and disk)
- .gitignore exists with all D-01 patterns, does NOT contain .planning/ (D-02)
- Folder structure contains .toc, .lua files, Modules/, and Media/
- All TOC-referenced source files (Core.lua, Options.lua, Modules/TipOfTheSpear.lua) still exist
- Single atomic commit containing all changes
- Working tree clean after commit
</success_criteria>

<output>
Create `.planning/phases/00-repo-cleanup/00-01-SUMMARY.md` when done
</output>
