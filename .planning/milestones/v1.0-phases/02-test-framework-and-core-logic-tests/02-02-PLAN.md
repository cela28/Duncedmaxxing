---
phase: 02-test-framework-and-core-logic-tests
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Duncedmaxxing/Core.lua
  - spec/core_spec.lua
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "NormalizeDB runs migration when settingsMigration does not match SETTINGS_MIGRATION constant"
    - "NormalizeDB preserves x, y, scale, optionsX, optionsY during migration"
    - "NormalizeDB resets displayMode and other style fields to defaults during migration"
    - "NormalizeDB skips migration when settingsMigration already matches"
    - "NormalizeDB clears deprecated fields barWidth, barHeight, spacing"
    - "NormalizeDB validates displayMode to bar/icons/number"
    - "MergeDefaults fills nil slots without overwriting existing values"
  artifacts:
    - path: "Duncedmaxxing/Core.lua"
      provides: "DMX._test table exposing NormalizeDB, MergeDefaults, CopyDefaults, SETTINGS_MIGRATION"
      contains: "DMX._test"
    - path: "spec/core_spec.lua"
      provides: "Unit tests for NormalizeDB and MergeDefaults"
      min_lines: 80
  key_links:
    - from: "spec/core_spec.lua"
      to: "Duncedmaxxing/Core.lua"
      via: "DMX._test.NormalizeDB and DMX._test.MergeDefaults"
      pattern: "DMX%._test%.NormalizeDB"
    - from: "spec/core_spec.lua"
      to: "spec/support/init.lua"
      via: "require('spec.support.init')"
      pattern: "require.*spec%.support%.init"
---

## Phase Goal

**As a** WoW addon developer, **I want to** run offline unit tests against all pure-logic functions, **so that** bugs get regression coverage without launching the WoW client.

<objective>
Expose Core.lua local functions for test access and write comprehensive NormalizeDB and MergeDefaults tests covering migration, field preservation, deprecated field cleanup, and displayMode validation.

Purpose: NormalizeDB is the highest-risk untested function -- a bad migration corrupts saved settings across sessions. Direct unit testing confirms migration correctness before Phase 3 removes the dead fallback block.

Output: `busted spec/core_spec.lua` passes with full migration coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-CONTEXT.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-RESEARCH.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-PATTERNS.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Core.lua that this plan tests. -->

From Duncedmaxxing/Core.lua:
  SETTINGS_MIGRATION = "0.3.2-fontfix"  (line 16)
  DEFAULTS table (lines 18-41): tip subtable with enabled, showOnlyInCombat, hideWhenEmpty,
    x(0), y(-160), scale(1), width(247), height(10), borderSize(1), displayMode("bar"),
    iconSize(28), iconSpacing(4), numberFontSize(22), fillColor, emptyColor, borderColor,
    textColor, optionsX(360), optionsY(170)

  local function CopyDefaults(defaults)  -- line 45: deep copy
  local function MergeDefaults(defaults, target)  -- line 57: recursive merge, fills nil slots only
  local function NormalizeDB(db)  -- line 74:
    - Migration gate: db.settingsMigration ~= SETTINGS_MIGRATION
    - On migration: preserves x, y, scale, optionsX, optionsY; resets all other tip fields to fresh CopyDefaults(DEFAULTS.tip); clears barWidth, barHeight, spacing to nil; sets db.locked = true
    - Post-migration (always runs): deprecated field migration (barWidth->width, barHeight->height, spacing->borderSize); displayMode validation (must be bar/icons/number, default to "bar")

From spec/support/init.lua (Plan 01 output):
  loader.load() returns DMX, Tip, mockClock
  loader.resetTipState(Tip, clock) resets runtime tracking fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose Core.lua local functions via DMX._test and update init.lua</name>
  <files>Duncedmaxxing/Core.lua, spec/support/init.lua</files>
  <read_first>
    Duncedmaxxing/Core.lua
    spec/support/init.lua
  </read_first>
  <action>
    Add a `DMX._test` block at the bottom of `Duncedmaxxing/Core.lua`, after the `coreFrame:SetScript("OnEvent", ...)` closure (after line 349, before EOF). This exposes local functions for direct unit testing per RESEARCH.md Open Question 1 resolution.

    The block to add:
    A comment line: `-- Test-only escape hatch: exposes local functions for spec/core_spec.lua`
    A comment line: `-- Do not use in production addon code.`
    Then assign `DMX._test` as a table with keys: `MergeDefaults` mapped to the local `MergeDefaults`, `NormalizeDB` mapped to the local `NormalizeDB`, `CopyDefaults` mapped to the local `CopyDefaults`, `SETTINGS_MIGRATION` mapped to the local `SETTINGS_MIGRATION`.

    Then update `spec/support/init.lua` to use `DMX._test` for the ADDON_LOADED bootstrap sequence instead of the fallback approach from Plan 01. In the `load()` function, after all three `loadAddon` calls complete, the bootstrap should: set `_G.DuncedmaxxingDB = {}`, call `DMX._test.MergeDefaults(DMX.defaults, _G.DuncedmaxxingDB)`, call `DMX._test.NormalizeDB(_G.DuncedmaxxingDB)`, set `DMX.db = _G.DuncedmaxxingDB`, set `DMX.ready = true`, then call `DMX:ForEachModule("Initialize", DMX)`. Remove any fallback logic that was used when `DMX._test` was absent.

    Verify that existing util_spec.lua still passes after these changes by running `busted spec/util_spec.lua`.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing &amp;&amp; grep -c "DMX._test" Duncedmaxxing/Core.lua &amp;&amp; busted spec/util_spec.lua</automated>
  </verify>
  <acceptance_criteria>
    - `Duncedmaxxing/Core.lua` contains `DMX._test = {` assignment with `MergeDefaults`, `NormalizeDB`, `CopyDefaults`, `SETTINGS_MIGRATION` keys
    - The `DMX._test` block appears after the `coreFrame:SetScript` closure, before EOF
    - `spec/support/init.lua` load() function calls `DMX._test.MergeDefaults` and `DMX._test.NormalizeDB` in the bootstrap sequence
    - `busted spec/util_spec.lua` still passes (no regression)
  </acceptance_criteria>
  <done>Core.lua exposes DMX._test with all four local symbols. init.lua uses DMX._test for bootstrap. Existing tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write NormalizeDB and MergeDefaults tests</name>
  <files>spec/core_spec.lua</files>
  <read_first>
    Duncedmaxxing/Core.lua
    spec/support/init.lua
    spec/support/wow_stubs.lua
  </read_first>
  <action>
    Create `spec/core_spec.lua` with two main `describe` blocks: one for `MergeDefaults` and one for `NormalizeDB`. Each block calls `loader.load()` in `before_each` for full isolation per D-06.

    MergeDefaults tests (at least 4 cases):
    - Fills nil slots in target: MergeDefaults({a=1, b=2}, {a=10}) produces {a=10, b=2} (does NOT overwrite a)
    - Deep merges nested tables: MergeDefaults({tip={x=0, y=-160}}, {tip={x=50}}) preserves tip.x=50 and fills tip.y=-160
    - Creates nested table when target has nil: MergeDefaults({tip={x=0}}, {}) produces tip subtable with x=0
    - Returns the target table (same reference): local t = {}; MergeDefaults({a=1}, t) returns t
    - Handles nil target: MergeDefaults({a=1}, nil) returns a table with a=1

    NormalizeDB tests — migration branch (at least 5 cases, settingsMigration set to "old-version" to trigger):
    - Runs migration when settingsMigration does not match: after NormalizeDB, db.settingsMigration == "0.3.2-fontfix"
    - Preserves position fields during migration: set db.tip.x=50, y=-100, scale=1.5, optionsX=400, optionsY=200 pre-migration; after NormalizeDB, these five fields retain their values
    - Resets style fields to defaults: set db.tip.displayMode="icons" pre-migration; after NormalizeDB, db.tip.displayMode == "bar" (default)
    - Sets db.locked = true during migration
    - Clears deprecated fields: set db.tip.barWidth=300, barHeight=20, spacing=2 pre-migration; after NormalizeDB, all three are nil

    NormalizeDB tests — already migrated branch (settingsMigration == "0.3.2-fontfix"):
    - Skips migration: set db.tip.displayMode="icons"; after NormalizeDB, displayMode is still "icons"

    NormalizeDB tests — deprecated field migration (always runs, post-migration-gate):
    - barWidth migrates to width: set db.tip.barWidth=300 on an already-migrated DB; after NormalizeDB, db.tip.width == 300
    - barHeight migrates to height: set db.tip.barHeight=20; after NormalizeDB, db.tip.height == 20
    - spacing migrates to borderSize when borderSize is absent: set db.tip.spacing=3 and remove borderSize; after NormalizeDB, db.tip.borderSize == 3

    NormalizeDB tests — displayMode validation (always runs):
    - Invalid displayMode reset to default: set db.tip.displayMode="invalid" on an already-migrated DB; after NormalizeDB, displayMode == "bar"
    - Valid modes preserved: "bar", "icons", "number" all pass through unchanged

    Use `DMX._test.NormalizeDB(db)` for direct calls. Construct test DBs manually with the required `tip` subtable rather than going through the full init sequence. Each test should build its own `db` table to ensure isolation.

    Run `busted spec/core_spec.lua` and iterate until all tests pass. Then run `busted spec/` to confirm the full suite passes.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing &amp;&amp; busted spec/core_spec.lua &amp;&amp; busted spec/</automated>
  </verify>
  <acceptance_criteria>
    - `busted spec/core_spec.lua` exits 0 with all tests passing
    - spec/core_spec.lua contains at least 12 `it()` blocks across MergeDefaults and NormalizeDB describe blocks
    - Migration branch tested with settingsMigration="old-version" (triggers migration per Pitfall 3)
    - Position preservation tested (x, y, scale, optionsX, optionsY)
    - Deprecated field clearing tested (barWidth, barHeight, spacing)
    - displayMode validation tested (invalid -> "bar", valid -> preserved)
    - Already-migrated branch tested (settingsMigration="0.3.2-fontfix" -> no reset)
    - `busted spec/` exits 0 (full suite including util_spec.lua still green)
  </acceptance_criteria>
  <done>All NormalizeDB and MergeDefaults tests pass. busted spec/ exits 0 with both core_spec and util_spec green. TEST-05 is delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No security-relevant surface; exposes local functions for test access only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | DMX._test exposing internals | accept | Test-only table; not usable by other addons (no addon would reference DMX._test); no secrets exposed |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `grep -c "DMX._test" Duncedmaxxing/Core.lua` returns > 0
2. `busted spec/core_spec.lua` exits 0
3. `busted spec/` exits 0 (full suite green)
</verification>

<success_criteria>
- DMX._test table exposes NormalizeDB, MergeDefaults, CopyDefaults, SETTINGS_MIGRATION
- NormalizeDB migration branch, preservation, clearing, and validation all tested
- MergeDefaults deep merge, nil-fill, and no-overwrite behavior tested
- Full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/02-test-framework-and-core-logic-tests/02-02-SUMMARY.md` when done
</output>
