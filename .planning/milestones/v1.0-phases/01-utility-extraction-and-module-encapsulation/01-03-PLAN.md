---
phase: 01-utility-extraction-and-module-encapsulation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
  - spec/tip_spec.lua
  - spec/support/init.lua
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Casting Kill Command on a Survival Hunter raises the displayed stack count to its correct final value with no transient over-count (no '3 for one instant then 2' flicker)"
    - "The Kill Command (generator) stack grant is independent of the Twin Fangs talent flag"
    - "Twin Fangs continues to affect ONLY the Takedown consumer path (grant 3, consume 1)"
  artifacts:
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Primal-Surge-aware generator grant decoupled from hasTwinFangs; HasPrimalSurge() detection helper"
      contains: "hasPrimalSurge"
    - path: "spec/tip_spec.lua"
      provides: "Regression tests proving the generator grant ignores hasTwinFangs and follows Primal Surge state"
      contains: "hasPrimalSurge"
  key_links:
    - from: "Duncedmaxxing/Modules/TipOfTheSpear.lua ApplySpell generator branch"
      to: "Tip.hasPrimalSurge field"
      via: "generator grant derives from self.hasPrimalSurge, not self.hasTwinFangs"
      pattern: "hasPrimalSurge"
---

<objective>
Fix the Kill Command stack-overshoot flicker reported in Phase 01 UAT Test 3: "when I press Kill command the stacks go to 3 for one instant before dropping back to 2."

Root cause (diagnosed in .planning/debug/kill-command-stack-overshoot.md): the generator branch of Tip:ApplySpell predicts the Kill Command grant as 3 when the Twin Fangs flag is set, else 2. Twin Fangs is a Takedown (consumer) modifier, NOT a Kill Command modifier. When Twin Fangs is talented, the generator path over-predicts +3 on a Kill Command cast whose real grant is 2 (Primal Surge). The display jumps to the predicted 3, then SyncFromAura reads the true aura (2 applications) and snaps it back to 2 — the visible flicker.

This plan decouples the Kill Command (generator) grant from hasTwinFangs, leaves Twin Fangs scoped to the Takedown consumer path (already correct at lines 699-703), and derives the generator grant from a new Primal-Surge-aware field (base 1, +1 with Primal Surge) via a HasPrimalSurge() helper modeled on the existing HasTwinFangs().

Purpose: Eliminate the predictive over-count so the display reaches the correct value directly with no flicker.
Output: Corrected Tip:ApplySpell generator branch, a HasPrimalSurge() helper plus Tip.hasPrimalSurge field wired into the same lifecycle events as hasTwinFangs, and regression tests in spec/tip_spec.lua.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/kill-command-stack-overshoot.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-UAT.md

# The file being fixed and its existing test suite
@Duncedmaxxing/Modules/TipOfTheSpear.lua
@spec/tip_spec.lua
@spec/support/init.lua
@spec/support/wow_stubs.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve and record the Primal Surge spell ID</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <action>
Resolve the Survival Hunter "Primal Surge" talent spell ID. Primal Surge makes Kill Command grant a second Tip of the Spear stack (base Kill Command grants 1; with Primal Surge it grants 2). This ID is load-bearing for the detection helper added in Task 2 and the planner could not verify it offline.

Resolution order (stop at the first that yields a confirmed numeric ID):
1. Search warcraft.wiki.gg (the project's canonical WoW source per CLAUDE.md) for "Primal Surge" Survival Hunter — confirm it is the Kill-Command / Tip-of-the-Spear talent (NOT an unrelated Restoration/Evoker spell of the same name) and read its spell ID.
2. Search wowhead.com for the Survival Hunter talent "Primal Surge" and confirm the ID.
3. Cross-check the ID resolves to the correct Survival Hunter talent on a second source.

If a verified ID is found: add it to TipOfTheSpear.lua as a module-level constant beside the TAKEDOWN / TWIN_FANGS declarations (after line 18): a local named PRIMAL_SURGE set to the confirmed numeric ID, with a trailing comment noting the talent name and source. Record the ID and source in the SUMMARY.

If NO ID can be verified from the canonical sources: do NOT invent one and do NOT add the constant. Instead set a flag for Task 2 to use the fallback path (flat grant of 2, no Primal Surge detection helper) and record in the SUMMARY that Primal Surge detection was skipped because the ID was unverifiable. The flat-2 grant still fully closes the reported flicker for the user (whose correct value is 2).

Make no other code changes in this task.
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing &amp;&amp; { grep -q 'PRIMAL_SURGE' Duncedmaxxing/Modules/TipOfTheSpear.lua &amp;&amp; echo OK_ID_ADDED || echo OK_FALLBACK_PATH; }</automated>
  </verify>
  <done>
    - Either a verified Primal Surge spell ID is added as a local PRIMAL_SURGE constant in TipOfTheSpear.lua (with source recorded in the SUMMARY), OR the SUMMARY records that the ID was unverifiable and Task 2 uses the flat-2 fallback.
    - No invented or unverified spell ID was committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decouple generator grant from Twin Fangs; add Primal Surge detection</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua, spec/tip_spec.lua, spec/support/init.lua</files>
  <behavior>
    Let BASE be the no-Primal-Surge generator grant Task 1 settled on:
    - Resolved-ID path (Task 1 added the PRIMAL_SURGE constant): BASE = 1, and the Primal-Surge grant = 2.
    - Flat-2 fallback path (Task 1 could not verify the ID): BASE = 2, and the Primal-Surge grant is also 2 (the flag is unused).

    Regression tests to ADD in spec/tip_spec.lua inside the existing describe("Tip:ApplySpell", ...) block:
    - Generator grant is INDEPENDENT of Twin Fangs: with Tip.hasTwinFangs = true and Tip.hasPrimalSurge = false, Tip:ApplySpell("generator") from 0 stacks does NOT reach 3 (it yields BASE). This is the core regression for the reported flicker.
    - Generator grant with Primal Surge: with Tip.hasPrimalSurge = true (hasTwinFangs either value), Tip:ApplySpell("generator") from 0 stacks yields 2.
    - Generator grant without Primal Surge: with Tip.hasPrimalSurge = false and Tip.hasTwinFangs = false, Tip:ApplySpell("generator") from 0 stacks yields BASE.

    Pre-existing GENERATOR tests that the new grant formula INVALIDATES and that this task MUST update or remove (do NOT leave them under a blanket "all tests pass"):
    - Lines 30-33 "adds 2 stacks on generator from zero" (`assert.equals(2, Tip.stacks)`): the hardcoded 2 only holds on the flat-2 fallback. On the resolved-ID path the grant from 0 is BASE = 1 → change the assertion to `assert.equals(BASE, ...)` (1 on resolved-ID, 2 on fallback), OR fold this case into the new "without Primal Surge" test and delete it. The test name "adds 2 stacks" is also stale on the resolved-ID path — rename to reflect BASE.
    - Line 117-124 "expiry timer fires after BUFF_DURATION and zeroes stacks": line 119 `assert.equals(2, Tip.stacks)` is an intermediate pre-advance check of the generator grant from 0. On the resolved-ID path this becomes BASE = 1 → change line 119 to `assert.equals(BASE, Tip.stacks)` (1 on resolved-ID, 2 on fallback). Do NOT change the post-advance `assert.equals(0, ...)` checks (lines 122-123) — those are unaffected.
    - Lines 127-131 "grants 2 stacks on generator without Twin Fangs (BUG-03 baseline)" (`assert.equals(2, Tip.stacks)`): redundant with the new "without Primal Surge" test and wrong on the resolved-ID path. Either update its assertion to BASE and set Tip.hasPrimalSurge = false explicitly, OR remove it in favor of the new test.
    - Lines 133-138 "grants 3 stacks on generator with Twin Fangs active (BUG-03)" (`assert.equals(3, Tip.stacks)`): DIRECTLY CONTRADICTS the fix on BOTH Task 1 paths (the new generator grant never yields 3 from 0 because Twin Fangs no longer affects the generator). REMOVE this test (its premise — that Twin Fangs raises the generator grant — is the bug being fixed). It is replaced by the new "independent of Twin Fangs" regression test.
    - Lines 140-146 "caps at MAX_STACKS on generator with Twin Fangs from 1 stack (BUG-03)" (`assert.equals(MAX_STACKS, Tip.stacks)`, i.e. 3): relied on Twin Fangs adding to the generator grant (1 + 3 → clamp 3). With the fix, from 1 stack the result is clamp(1 + BASE) = 2 on resolved-ID (1+1) or 3 on fallback (1+2). REMOVE this test (its premise is the bug). General generator clamping is already covered by the retained "caps stacks at 3 when already at 2 (generator)" (lines 36-40) and "keeps stacks at 3 when already at MAX_STACKS" (lines 43-47), both of which stay valid because 2+BASE and 3+BASE clamp to 3 on either path.

    Tests that MUST remain UNCHANGED:
    - The Takedown / Twin Fangs CONSUMER tests (lines 148-188): Twin Fangs still grants 3-then-consume-1 = 2 on Takedown. The consumer branch is untouched.
    - The non-generator-grant generator tests: "caps stacks at 3 when already at 2" (36-40), "keeps stacks at 3 when already at MAX_STACKS" (43-47), the expiresAt/lastPredict/timer assertions (63-114) — none of these assert the from-0 grant value.
  </behavior>
  <action>
RED first — add the regression tests described in the behavior block to spec/tip_spec.lua, run busted on the spec, confirm the new Twin-Fangs-independence test FAILS against the current generator grant formula (the one that returns 3 when hasTwinFangs is set, else 2).

Then implement the fix in Duncedmaxxing/Modules/TipOfTheSpear.lua:

1. If Task 1 added a PRIMAL_SURGE constant: add a HasPrimalSurge() file-scoped local function immediately after HasTwinFangs() (lines 35-43), mirroring its exact structure — try C_SpellBook.IsSpellKnown(PRIMAL_SURGE), fall back to IsPlayerSpell(PRIMAL_SURGE), else return false, with the same boolean normalization. If Task 1 used the flat-2 fallback: skip the helper.

2. Add Tip.hasPrimalSurge = false to the Tip field initializers next to Tip.hasTwinFangs = false (line 55). Add this field even on the fallback path so the tests and resetTipState stay consistent (it stays false and unused on the fallback path).

3. In the Tip:ApplySpell generator branch (lines 694-697), replace the existing grant line (the one deriving grant from self.hasTwinFangs, returning 3 or 2) with a Primal-Surge-derived grant: use `local grant = self.hasPrimalSurge and 2 or 1` when Task 1 resolved the ID, or `local grant = 2` on the fallback path. Keep the ClampStacks line and the expiresAt assignment unchanged. ClampStacks/MAX_STACKS still bound the result at 3. The generator branch must NOT reference self.hasTwinFangs.

4. Leave the consumer branch (lines 698-709) untouched — Twin Fangs stays scoped to the Takedown path exactly as-is.

5. If the HasPrimalSurge() helper exists, set self.hasPrimalSurge = HasPrimalSurge() everywhere self.hasTwinFangs = HasTwinFangs() is set: Initialize (line 777), PLAYER_SPECIALIZATION_CHANGED (line 741), and PLAYER_TALENT_UPDATE / TRAIT_CONFIG_UPDATED (line 748). Add the line directly beside each existing hasTwinFangs assignment so talent changes refresh both flags. (On the fallback path, leave hasPrimalSurge at its initialized false.)

6. Update spec/support/init.lua resetTipState to zero the new field: add Tip.hasPrimalSurge = false next to the existing Tip.hasTwinFangs = false (line 61) so per-test isolation holds.

7. RECONCILE the now-stale GENERATOR tests in spec/tip_spec.lua enumerated in the behavior block — this is mandatory, not optional cleanup. The new grant formula invalidates them, so a passing run is impossible until they are fixed. Using the BASE value Task 1 settled on:
   - Lines 30-33: change the assertion from a hardcoded 2 to BASE (1 on resolved-ID, 2 on fallback) and rename the test, OR delete it in favor of the new "without Primal Surge" test.
   - Line 119: change `assert.equals(2, Tip.stacks)` to `assert.equals(BASE, Tip.stacks)`; leave the post-advance zero assertions intact.
   - Lines 127-131: update its assertion to BASE with Tip.hasPrimalSurge = false set explicitly, OR remove it (redundant with the new test).
   - Lines 133-138 ("grants 3 stacks ... with Twin Fangs"): REMOVE — its premise is the bug being fixed.
   - Lines 140-146 ("caps at MAX_STACKS ... with Twin Fangs from 1 stack"): REMOVE — its premise is the bug being fixed; generator clamping is still covered by the retained tests at lines 36-40 and 43-47.
   Do NOT touch the consumer tests (148-188) or the non-grant generator tests.

GREEN — run busted on the spec (if available; see verify fallback) and confirm every test passes: the new regression tests, the reconciled generator tests, and the untouched consumer tests. There must be no leftover test asserting a Twin-Fangs-driven generator grant. The C_SpellBook and IsPlayerSpell globals are already in .luacheckrc read_globals (lines 28-29), so no luacheck config change is needed.

Do NOT alter SyncFromAura, the consumer up-sync grace window, or any frame/render code.
  </action>
  <verify>
    <!-- planner-discipline-allow: hasTwinFangs and 3 or 2 -->
    <!-- busted gate, degrades to grep structural checks if busted is absent. Note the in-game UAT re-test is still required either way. -->
    <automated>cd /home/cela/code_stuff/Duncedmaxxing &amp;&amp; if command -v busted >/dev/null 2>&amp;1; then busted spec/tip_spec.lua 2>&amp;1 | tail -3; else echo "BUSTED_ABSENT — falling back to grep structural checks; in-game UAT Test 3 re-test required"; test "$(grep -c 'grants 3 stacks on generator with Twin Fangs' spec/tip_spec.lua)" -eq 0 &amp;&amp; test "$(grep -c 'caps at MAX_STACKS on generator with Twin Fangs from 1 stack' spec/tip_spec.lua)" -eq 0 &amp;&amp; echo OK_STALE_GENERATOR_TESTS_REMOVED; fi</automated>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing &amp;&amp; test "$(grep -c 'hasTwinFangs and 3 or 2' Duncedmaxxing/Modules/TipOfTheSpear.lua)" -eq 0 &amp;&amp; echo OK_OLD_FORMULA_GONE</automated>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing &amp;&amp; grep -q 'hasPrimalSurge' Duncedmaxxing/Modules/TipOfTheSpear.lua &amp;&amp; grep -q 'hasPrimalSurge' spec/tip_spec.lua &amp;&amp; echo OK_PRIMAL_SURGE_WIRED</automated>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing &amp;&amp; if command -v luacheck >/dev/null 2>&amp;1; then luacheck Duncedmaxxing/Modules/TipOfTheSpear.lua 2>&amp;1 | tail -2; else echo "LUACHECK_ABSENT — skipped; structural greps above stand in"; fi</automated>
  </verify>
  <done>
    - The old hasTwinFangs-based generator grant formula no longer appears anywhere in TipOfTheSpear.lua (verify grep returns 0).
    - The generator branch of ApplySpell does not reference self.hasTwinFangs; it derives grant from self.hasPrimalSurge (or a flat 2 on the fallback path).
    - Tip.hasPrimalSurge exists and is initialized to false; on the resolved-ID path it is refreshed alongside hasTwinFangs in Initialize, PLAYER_SPECIALIZATION_CHANGED, PLAYER_TALENT_UPDATE, and TRAIT_CONFIG_UPDATED.
    - The stale generator tests are reconciled: the two "with Twin Fangs" generator tests (lines 133-138 and 140-146) are REMOVED, and the hardcoded grant=2 assertions at lines 30-33, 119, and 127-131 are updated to BASE (or folded into the new tests). No test asserting a Twin-Fangs-driven generator grant remains.
    - When busted is available: all tests in spec/tip_spec.lua pass, including the new regression test asserting a generator cast with hasTwinFangs=true and hasPrimalSurge=false does NOT reach 3 from 0 stacks, plus the reconciled generator tests and the untouched consumer tests. When busted is absent: the grep structural checks pass (stale generator tests removed, old formula gone, hasPrimalSurge wired) and the SUMMARY notes that in-game UAT Test 3 re-test is still required.
    - The pre-existing Takedown/Twin Fangs consumer tests (lines 148-188) are unchanged.
    - When luacheck is available it reports zero warnings for TipOfTheSpear.lua; when absent the gate is skipped without failing the plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client → addon | Spellcast events (UNIT_SPELLCAST_SUCCEEDED) and aura reads (UNIT_AURA) cross from the game engine into addon prediction logic. All values are engine-controlled, not attacker-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Generator grant prediction in ApplySpell | accept | Single-player addon, no remote input. The only "tampering" surface is the player's own talent loadout; the fix reads it via C_SpellBook/IsPlayerSpell which are read-only WoW API. No untrusted data path. |
| T-01-SC | Tampering | Package installs (npm/pip/cargo) | accept | No package installs in this plan — pure Lua source edit plus busted tests using the already-installed framework. No new dependencies. |
</threat_model>

<verification>
- busted spec/tip_spec.lua passes with zero failures (new regression tests + reconciled generator tests + untouched consumer tests) when busted is on PATH. If busted is absent, grep structural checks stand in (stale "with Twin Fangs" generator tests removed; old formula gone; hasPrimalSurge wired) and the in-game UAT re-test below becomes the primary functional gate.
- The stale generator tests are reconciled: the two "with Twin Fangs" generator tests are removed and the hardcoded grant=2 assertions are updated to BASE or folded into the new tests.
- The old hasTwinFangs-based generator grant formula is fully removed.
- hasPrimalSurge is wired into all four talent-refresh sites.
- luacheck on TipOfTheSpear.lua reports zero warnings when luacheck is on PATH; skipped without failure otherwise.
- In-game UAT re-test (Phase 01 UAT Test 3) by the user: casting Kill Command raises stacks to the correct value with no transient 3. Required in all cases, and the sole functional confirmation when busted is unavailable.
</verification>

<success_criteria>
- UAT Test 3 gap closed: no "3 for one instant then 2" flicker on Kill Command cast.
- Generator grant decoupled from Twin Fangs; Twin Fangs remains scoped to the Takedown consumer path.
- Regression coverage in spec/tip_spec.lua prevents reintroduction.
</success_criteria>

<output>
Create `.planning/phases/01-utility-extraction-and-module-encapsulation/01-03-SUMMARY.md` when done.
</output>
