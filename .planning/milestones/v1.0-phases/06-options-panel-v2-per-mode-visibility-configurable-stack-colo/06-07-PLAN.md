---
phase: 06-options-panel-v2-per-mode-visibility-configurable-stack-colo
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - Duncedmaxxing/Core.lua
  - spec/core_spec.lua
autonomous: true
requirements: [DISP-06]

must_haves:
  truths:
    - "Existing non-position tip customizations survive the migration token bump"
    - "A legacy DB on the shipped v1.0.0 token ('0.3.2-fontfix') with a customized displayMode, width, borderColor, numberFontSize, and colorByStack=false retains every one of those values after NormalizeDB"
    - "Legacy positional stackColors ([1]/[2]/[3]/[4] keys) are converted to named-key r/g/b/a form so pickers read real colors"
    - "The full fengari test suite passes with 0 failures, including a new regression test that exercises the real migration branch with the OLD token"
  artifacts:
    - path: "Duncedmaxxing/Core.lua"
      provides: "NormalizeDB targeted stackColors re-seed replacing the blanket CopyDefaults(DEFAULTS.tip) overwrite; StackColorsAreLegacyFormat helper"
      contains: "StackColorsAreLegacyFormat"
    - path: "spec/core_spec.lua"
      provides: "Regression test proving the migration-branch path preserves non-position customizations"
      contains: "0.3.2-fontfix"
  key_links:
    - from: "spec/core_spec.lua"
      to: "Duncedmaxxing/Core.lua"
      via: "DMX._test.NormalizeDB on a DB seeded with the OLD settingsMigration token"
      pattern: "DMX._test\\.NormalizeDB"
    - from: "Duncedmaxxing/Core.lua NormalizeDB"
      to: "DEFAULTS.tip.stackColors"
      via: "targeted CopyDefaults(DEFAULTS.tip.stackColors) only when legacy positional shape detected"
      pattern: "CopyDefaults\\(DEFAULTS\\.tip\\.stackColors\\)"
---

<objective>
Close the SC-6 BLOCKER: the `SETTINGS_MIGRATION` bump adopted in 06-05 activates `NormalizeDB`'s pre-existing blanket `CopyDefaults(DEFAULTS.tip)` overwrite for every real user still on the v1.0.0-shipped `"0.3.2-fontfix"` token, silently discarding all customized `tip.*` fields except x/y/scale/optionsX/optionsY on next login. Replace the blanket overwrite with a targeted re-seed of only the `stackColors` field (the one whose shape actually changed), and add a regression test that genuinely exercises the migration branch with the old token and non-position customizations present.

Purpose: A live data-loss regression violating ROADMAP SC-6 ("no settings wipe") and REQUIREMENTS DISP-06. Independently confirmed by 06-VERIFICATION.md (direct reproduction) and 06-REVIEW.md (CR-01, BLOCKER).
Output: A `NormalizeDB` that preserves every previously-customized field across the token bump, plus regression coverage that would fail against the current blanket-wipe code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-REVIEW.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md
</context>

<security_note>
This is a client-side WoW addon with no network, authentication, or external-input surface — settings load from the game client's own SavedVariables file. A formal STRIDE threat model is not applicable. The only integrity concern in scope is data loss of user settings, which is exactly what this plan fixes.
</security_note>

<tasks>

<task type="auto">
  <name>Task 1: Add failing regression test for migration-branch settings preservation (RED)</name>
  <files>spec/core_spec.lua</files>
  <read_first>
    - spec/core_spec.lua (study the existing "NormalizeDB — migration branch" describe block at lines 75-164 and the D-11 "no wipe" block at lines 212-270 — note the D-11 test hardcodes the CURRENT token so it never enters the migration branch)
    - Duncedmaxxing/Core.lua (NormalizeDB lines 72-99, DEFAULTS.tip.stackColors lines 30-35, SETTINGS_MIGRATION line 10)
  </read_first>
  <action>
    Add a new describe block to spec/core_spec.lua (e.g. "NormalizeDB — migration branch preserves user customizations (SC-6 regression)") using the existing `loader.load()` + `DMX._test.NormalizeDB` harness pattern. The test MUST seed a DB whose `settingsMigration` is the OLD shipped token `"0.3.2-fontfix"` (NOT the current token — this is what forces entry into the migration branch and is the exact scenario that wipes real users). Seed `db.tip` with realistic non-default customizations beyond the five preserved position fields: `displayMode = "number"`, `hideWhenEmpty = true`, `width = 400`, `borderSize = 3`, `numberFontSize = 40`, a custom `borderColor` (e.g. r=0.5), `colorByStack = false`, and a legacy positional `stackColors` table (array-indexed tuples: `[0] = {1,1,1,1}`, `[1] = {0.18039,0.8,0.44314,1}`, `[2] = {1,0.94118,0,1}`, `[3] = {1,0.29804,0.18824,1}` — positional keys, no r/g/b/a). Call `DMX._test.NormalizeDB(db)` directly (do NOT run MergeDefaults first — MergeDefaults would fill named keys onto the legacy table and mask the legacy-format detection). Assert: (a) each customized field SURVIVES — `displayMode=="number"`, `hideWhenEmpty==true`, `width==400`, `borderSize==3`, `numberFontSize==40`, `borderColor.r` near 0.5, `colorByStack==false`; (b) `stackColors[0].r` is a number (converted to named-key form); (c) `db.settingsMigration` equals `DMX._test.SETTINGS_MIGRATION`. Do NOT modify or delete any existing test in this file. No fenced implementation beyond the assertions themselves — follow the file's existing `assert.equals`/`assert.near`/`assert.is_true`/`assert.is_false` style.
  </action>
  <verify>
    <automated>node spec/run.cjs 2>/dev/null | tail -3 || npx -y -p fengari@0.1.5 node spec/run.cjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The suite runs and the new describe block's assertions FAIL against the current (unmodified) Core.lua — specifically the customized-field-survival assertions fail because the blanket `CopyDefaults(DEFAULTS.tip)` overwrite resets them to defaults. This is the expected RED state proving the test exercises the real migration branch.
    - All 124 previously-passing tests still pass (only the new test fails at this point).
    - The seeded `settingsMigration` literal in the new test is `"0.3.2-fontfix"` (the OLD token), confirmable via grep.
  </acceptance_criteria>
  <done>New regression test exists, seeds the OLD token plus non-position customizations and legacy positional stackColors, and fails against the current blanket-wipe NormalizeDB while leaving all existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace blanket migration wipe with targeted stackColors re-seed (GREEN)</name>
  <files>Duncedmaxxing/Core.lua</files>
  <read_first>
    - Duncedmaxxing/Core.lua (NormalizeDB lines 72-99, CopyDefaults lines 43-53, MergeDefaults lines 55-70, DEFAULTS.tip.stackColors lines 30-35)
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-REVIEW.md (CR-01 fix sketch — the authoritative StackColorsAreLegacyFormat + targeted-reseed approach)
  </read_first>
  <behavior>
    - A DB on any non-current token with customized displayMode/width/borderColor/numberFontSize/colorByStack keeps all of those values after NormalizeDB (no wipe).
    - A DB whose stackColors is in legacy positional shape gets stackColors re-seeded to the named-key DEFAULTS form.
    - A DB already in named-key form is left untouched (StackColorsAreLegacyFormat returns false).
    - The deprecated-field clearing (barWidth/barHeight/spacing → nil), the `db.locked = true` set, and the `settingsMigration` bump all still occur inside the migration gate (existing migration-branch tests continue to pass).
    - Invalid/unknown displayMode still normalizes to "bar" via the always-runs validation.
  </behavior>
  <action>
    Add a file-scoped `local function StackColorsAreLegacyFormat(stackColors)` helper above NormalizeDB that returns true only when stackColors exists and its `[0]` entry is a table with no `r` key but a positional `[1]` value (i.e. detects the old array-indexed tuple shape vs the new named r/g/b/a shape) — per the 06-REVIEW.md CR-01 sketch. In NormalizeDB, add a guarded re-seed: when `StackColorsAreLegacyFormat(tip.stackColors)` is true, set `tip.stackColors = CopyDefaults(DEFAULTS.tip.stackColors)`. Then REMOVE the blanket-overwrite mechanism from the migration gate: delete the `local fresh = CopyDefaults(DEFAULTS.tip)` line, the `for key, value in pairs(fresh) do tip[key] = value end` loop, and the five `tip.x = x or fresh.x` / `tip.y` / `tip.scale` / `tip.optionsX` / `tip.optionsY` restore lines plus the now-orphaned `local x, y, scale = ...` / `local optionsX, optionsY = ...` captures. KEEP inside the `if db.settingsMigration ~= SETTINGS_MIGRATION then` gate exactly these side effects (existing core_spec migration-branch tests assert them): `tip.barWidth = nil`, `tip.barHeight = nil`, `tip.spacing = nil`, `db.locked = true`, and `db.settingsMigration = SETTINGS_MIGRATION`. Do NOT change the SETTINGS_MIGRATION token value (keep `"0.3.3-stackcolorfmt"`) and do NOT touch the always-runs displayMode validation block. Net effect: customized tip.* fields are never overwritten; only the format-broken stackColors field is repaired, and only when it is provably in the legacy shape.
  </action>
  <verify>
    <automated>node spec/run.cjs 2>/dev/null | tail -3 || npx -y -p fengari@0.1.5 node spec/run.cjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node spec/run.cjs` (or the npx fallback) reports 0 failures and total >= 125 (all prior 124 plus the new regression test now passing).
    - `grep -c 'CopyDefaults(DEFAULTS.tip)' Duncedmaxxing/Core.lua` returns 0 (the blanket-tip overwrite is gone); the more specific `grep -c 'CopyDefaults(DEFAULTS.tip.stackColors)' Duncedmaxxing/Core.lua` returns >= 1.
    - `grep -c 'StackColorsAreLegacyFormat' Duncedmaxxing/Core.lua` returns >= 2 (definition + call site).
    - `grep -c '0.3.3-stackcolorfmt' Duncedmaxxing/Core.lua` returns 1 (token unchanged).
    - All ten existing "NormalizeDB — migration branch" tests still pass (deprecated-field clearing, db.locked, x/y/scale/options preservation, settingsMigration bump).
  </acceptance_criteria>
  <done>NormalizeDB preserves every customized tip.* field across the token bump, repairs only legacy-format stackColors, and the full fengari suite is green with the new regression test passing.</done>
</task>

</tasks>

<verification>
- `node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs` → 0 failed, total >= 125.
- `grep -n 'CopyDefaults(DEFAULTS.tip)' Duncedmaxxing/Core.lua` → no hits (blanket wipe removed).
- `grep -n 'StackColorsAreLegacyFormat' Duncedmaxxing/Core.lua` → helper defined and called.
- Manual trace: a DB with `settingsMigration = "0.3.2-fontfix"` and `displayMode = "number"` retains `"number"` after NormalizeDB.
</verification>

<success_criteria>
SC-6 is satisfied: a fresh/legacy DB loads cleanly, the new color/toggle fields default correctly, and NO customized setting is wiped when the migration token advances. Regression coverage exists that exercises the real migration branch with the shipped v1.0.0 token and fails against the pre-fix code.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-07)
- `Duncedmaxxing/Core.lua`: new file-scoped `StackColorsAreLegacyFormat(stackColors)` helper; targeted `CopyDefaults(DEFAULTS.tip.stackColors)` re-seed inside NormalizeDB; blanket `CopyDefaults(DEFAULTS.tip)` overwrite removed. `SETTINGS_MIGRATION` unchanged at `"0.3.3-stackcolorfmt"`.
- `spec/core_spec.lua`: new describe block "NormalizeDB — migration branch preserves user customizations (SC-6 regression)" seeding the OLD `"0.3.2-fontfix"` token.
</artifacts_produced>

<output>
Create `.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-07-SUMMARY.md` when done
</output>
