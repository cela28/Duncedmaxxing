---
phase: 06-options-panel-v2-per-mode-visibility-configurable-stack-colo
plan: 06
type: execute
wave: 2
depends_on: ["06-04"]
files_modified:
  - Duncedmaxxing/Options.lua
autonomous: true
gap_closure: true
requirements: [DISP-07]
must_haves:
  truths:
    - "The options panel has no large dead region in either mode (the Bar-mode upper-right empty block is reclaimed or the window is tightened)"
    - "Sections are evenly spaced and columns balanced across both Bar and Number modes"
    - "The window remains draggable, combat-guarded, and functionally identical — only positions/size change"
  artifacts:
    - path: "Duncedmaxxing/Options.lua"
      provides: "Rebalanced absolute layout (repositioned widgets and/or tightened window height, optionally via extracted offset constants)"
  key_links:
    - from: "Duncedmaxxing/Options.lua"
      to: "Duncedmaxxing/Options.lua"
      via: "window:SetSize + per-widget SetPoint offsets define the grid"
      pattern: "SetSize"
---

<objective>
Close the layout/empty-space gap from Phase 6 UAT (test 5): the fixed 386x484 two-column window (left x=16, right x=204) leaves a large dead region in the upper-right in Bar mode (y=-120..-238, where the Number section is hidden) and has uneven section spacing. Rebalance the layout now that the earlier removals have freed grid slots.

Purpose: The user asked to organize the empty space better. This is cosmetic polish and MUST run last because the final coordinates depend on which widgets remain after plan 06-04 (Enabled deleted, position Reset deleted, Scale and Border color moved to bar-only).
Output: Repositioned widgets and/or a tightened window in Options.lua, with the same functional behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-UAT.md
@Duncedmaxxing/Options.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance the options-panel layout and tighten empty space</name>
  <files>Duncedmaxxing/Options.lua</files>
  <action>
FIRST read the current Options.lua BuildWindow in full — plan 06-04 has already deleted the Enabled checkbox, deleted the position Reset button, and moved Scale and Border color into the bar-only group, so the on-disk coordinates and remaining widgets differ from the original UAT snapshot. Work from the actual current file, not the pre-06-04 line numbers.

Rebalance the absolute two-column grid so there is no large dead region in either mode:
- The reported dead region is the Bar-mode upper-right block (right column x=204, roughly y=-120..-238) which is empty because the Number section is hidden in Bar mode. Reclaim it — e.g. lift the right-column "Colors" section (Border/Fill/Empty% in bar mode) up into that space so Bar mode fills the right column, and/or pull the Number section up so Number mode is compact.
- Reclaim the vacated rows from removed widgets: the ~y=-80 row (former Enabled checkbox) and the button-row slot (former position Reset button). Close those gaps so spacing reads evenly.
- Even out vertical section spacing (Position / Bar / Number / Colors / buttons) so gaps look consistent between headers and their inputs.
- Consider shrinking the window height (window:SetSize) from 484 to whatever the tightest laid-out content needs, keeping width 386. Do not grow the window.
- Optionally extract row/column offset constants (ALL_CAPS_SNAKE_CASE per project convention) to make the absolute positioning maintainable, if it does not balloon the diff.

Constraints — do NOT change behavior:
- Keep every remaining widget, its getValue/setValue closures, and its group membership exactly as-is; only SetPoint coordinates and window size may change.
- Keep the OnShow/OnDragStart/OnDragStop scripts, the combat guard (Options:CanChange / InCombat), ApplyPosition, and the Refresh show/hide engine untouched.
- Keep the mode buttons (Bar/Number) at the top and the active-button highlight behavior intact.
- Both Bar and Number modes must lay out cleanly with the shared window size — verify each mentally against the group assignments (both / bar / number).
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && test "$(grep -c 'window:SetSize' Duncedmaxxing/Options.lua)" = "1" && (node spec/run.cjs 2>/dev/null || npx -y -p fengari@0.1.5 node spec/run.cjs 2>&1) | tail -3</automated>
  </verify>
  <done>Options.lua has exactly one window:SetSize call (still width 386, height unchanged or reduced), every remaining widget keeps its closures and group, and the full fengari suite still passes. No large empty block remains in either mode (confirmed visually at UAT re-run).</done>
</task>

</tasks>

<verification>
Full regression: `cd /home/cela/code_stuff/Duncedmaxxing && node spec/run.cjs 2>/dev/null || npx -y -p fengari@0.1.5 node spec/run.cjs` — 0 failed. Layout is pure repositioning so the suite must remain green (no logic touched).

Visual confirmation (out of automated scope, recorded for the UAT re-run): open the panel in Bar mode and confirm the upper-right region beside Position is no longer empty; switch to Number mode and confirm sections are compact and evenly spaced; drag the window and confirm it still moves and saves position.
</verification>

<success_criteria>
- No large dead region in either Bar or Number mode; columns balanced and spacing even (DISP-07).
- Window remains 386 wide, height unchanged or tightened; still draggable and combat-guarded.
- All widget behavior identical; the full fengari regression suite passes.
</success_criteria>

<output>
Create `.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-06-SUMMARY.md` when done.
</output>