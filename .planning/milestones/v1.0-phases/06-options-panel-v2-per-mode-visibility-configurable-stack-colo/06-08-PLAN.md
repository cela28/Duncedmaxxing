---
phase: 06-options-panel-v2-per-mode-visibility-configurable-stack-colo
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DISP-05, DISP-07]

must_haves:
  truths:
    - "The SC-1 and SC-2 deviations are formally recorded as accepted overrides so re-verification does not re-flag them as functional defects"
    - "ROADMAP Phase 6 SC-1/SC-2 text and REQUIREMENTS DISP-05 text describe the mode-relevant-controls-only reality (no Enabled checkbox, Border color bar-only, button-only mode switching)"
  artifacts:
    - path: ".planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md"
      provides: "overrides: frontmatter block with two accepted entries; overrides_applied bumped to 2"
      contains: "overrides:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 SC-1 and SC-2 wording aligned with user-approved reality"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DISP-05 text aligned with the removed-Enabled / bar-only-Border-color reality"
  key_links:
    - from: ".planning/phases/06-.../06-VERIFICATION.md overrides"
      to: "06-UAT.md tests 1-2"
      via: "reason field cites the explicit UAT feedback that drove each deviation"
      pattern: "06-UAT"
---

<objective>
Close the SC-1 and SC-2 paperwork gaps. Both fail verification only on literal ROADMAP wording: the Enabled checkbox was intentionally deleted and Border color moved to bar-only (06-UAT.md tests 1-2), and `/dmax mode bar|number` was intentionally dropped in favor of button-only switching ("I do not want any slash commands for modes"). These are correct, user-approved deviations with no formal override recorded. Record the overrides via the GSD mechanism (VERIFICATION.md `overrides:` frontmatter) and align ROADMAP/REQUIREMENTS wording so future verification runs do not re-flag them.

Purpose: Traceability, not code. No functional defect exists here — the implementation matches what the user asked for.
Output: Two accepted override records and aligned success-criteria/requirement text.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-UAT.md
</context>

<security_note>
Documentation-only changes to planning artifacts. No runtime surface, no threat model applicable.
</security_note>

<tasks>

<task type="auto">
  <name>Task 1: Record formal SC-1 and SC-2 overrides in 06-VERIFICATION.md</name>
  <files>.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md (frontmatter `overrides_applied: 0`; the two suggested `overrides:` blocks at lines 167-185 in the body)
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-UAT.md (the tests 1-2 feedback the reason fields must cite)
  </read_first>
  <action>
    Edit 06-VERIFICATION.md frontmatter: add an `overrides:` list with two entries mirroring the two suggested blocks already drafted in the report body (SC-1: Enabled checkbox removed + Border color regrouped to bar-only per 06-UAT.md tests 1-2; SC-2: slash-command mode switching dropped in favor of button-only per 06-UAT.md test 2 "I do not want any slash commands for modes"). Each entry has `must_have`, `reason` (citing the specific UAT feedback), `accepted_by` (use the project git author "cela28"), and `accepted_at` (current ISO timestamp). Bump `overrides_applied` from 0 to 2. Do NOT change the `status`, `score`, `gaps:`, or `re_verification:` blocks — the verifier owns re-verification status; this task only records the accepted overrides so the next verification run reads them.
  </action>
  <verify>
    <automated>grep -c 'overrides:' .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `overrides_applied: 2` present in 06-VERIFICATION.md frontmatter.
    - Two override entries exist, one referencing the Enabled/Border-color deviation (SC-1) and one referencing the slash-command-mode deviation (SC-2); each has must_have, reason, accepted_by, accepted_at.
    - Each reason cites 06-UAT.md as the source of the user direction.
    - The `gaps:` and `status:` blocks are unchanged from the pre-edit file.
  </acceptance_criteria>
  <done>06-VERIFICATION.md carries two accepted override records for SC-1 and SC-2 with overrides_applied: 2, without altering the verifier-owned status/gaps fields.</done>
</task>

<task type="auto">
  <name>Task 2: Align ROADMAP SC-1/SC-2 and REQUIREMENTS DISP-05 wording with the approved reality</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 6 Success Criteria block, SC-1 at line 230 and SC-2 at line 231)
    - .planning/REQUIREMENTS.md (DISP-05 at line 58)
  </read_first>
  <action>
    Use scoped Edit (never Write) on both files. In ROADMAP.md Phase 6 Success Criteria: revise SC-1 so the "shown in both modes" list reads Position and Hide empty (remove the Enabled clause) and states that Border color is a Bar-only control (not shown in both) — reflecting the UAT-approved regrouping; keep the Bar-only / Number-only control lists intact. Revise SC-2 to describe mode switching as button-only (Bar/Number buttons via Options:SetMode) and remove the `/dmax mode ...` slash clause, noting slash-command mode switching was intentionally dropped per user direction. In REQUIREMENTS.md DISP-05: update the "Position/Enabled/Hide empty/Border color show in both" clause to "Position and Hide empty show in both; Border color is Bar-only; the Enabled checkbox was removed from the panel." Add a short parenthetical in each edited spot pointing to the 06-VERIFICATION.md overrides / 06-UAT.md as the rationale source. Do not alter any other phase's criteria or any other requirement line.
  </action>
  <verify>
    <automated>grep -n 'button' .planning/ROADMAP.md | grep -i 'mode' | head -3</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 6 SC-1 no longer lists "Enabled" among controls shown in both modes and states Border color is Bar-only.
    - ROADMAP.md Phase 6 SC-2 describes button-only mode switching and no longer requires a `/dmax mode ...` slash path.
    - REQUIREMENTS.md DISP-05 text reflects that Border color is Bar-only and the Enabled checkbox was removed.
    - No other phase criteria or requirement lines are modified (diff scoped to the SC-1/SC-2 and DISP-05 lines plus their rationale parentheticals).
  </acceptance_criteria>
  <done>ROADMAP Phase 6 SC-1/SC-2 and REQUIREMENTS DISP-05 describe the actual, user-approved control set and switching mechanism, each pointing back to the recorded overrides.</done>
</task>

</tasks>

<verification>
- `grep -c 'overrides:' 06-VERIFICATION.md` → 1 (frontmatter key present).
- ROADMAP Phase 6 SC-1 excludes Enabled from "both modes" and marks Border color Bar-only.
- ROADMAP Phase 6 SC-2 reads button-only, no slash-mode requirement.
- REQUIREMENTS DISP-05 text matches the approved control set.
</verification>

<success_criteria>
SC-1 and SC-2 are formally accepted as intentional deviations: the override records exist and the ROADMAP/REQUIREMENTS wording matches the shipped, user-approved reality, so re-verification does not re-flag them as defects.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-08)
- `06-VERIFICATION.md`: `overrides:` frontmatter block (2 entries), `overrides_applied: 2`.
- `.planning/ROADMAP.md`: revised Phase 6 SC-1 (Enabled removed, Border color bar-only) and SC-2 (button-only switching).
- `.planning/REQUIREMENTS.md`: revised DISP-05 control-visibility text.
</artifacts_produced>

<output>
Create `.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-08-SUMMARY.md` when done
</output>
