---
phase: 07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified: []
autonomous: false
requirements: [D-01, D-04, D-05]

must_haves:
  truths:
    - "No orphaned reference to any removed symbol (hasPrimalSurge, spellTexture, CacheSpellTexture, FALLBACK_ICON, ParseOnOff) remains anywhere in Duncedmaxxing/ or spec/"
    - "The full fengari suite is green (0 failed) after all Wave 1 removals"
    - "luacheck reports zero warnings on Duncedmaxxing/ — verified where luacheck is available, otherwise recorded as a blocking human-verify because luacheck/luarocks/lua are absent in the execution sandbox (per RESEARCH Pitfall 5 / VALIDATION Manual-Only Verifications)"
  artifacts: []
  key_links: []
---

<objective>
Phase gate: prove the cleanup left no orphaned references and did not regress the suite, and satisfy the project's "luacheck zero warnings" quality bar. This plan makes no source changes — it is the whole-tree verification sweep that runs after both Wave 1 removal plans (07-01, 07-02) complete.

Purpose: In a codebase with no build step and no compiler, grep-before/after-delete across the WHOLE tree plus the fengari suite are the only static + dynamic safety nets. luacheck is the project's lint gate but is not runnable in this sandbox, so it is gated behind a blocking human-verify.

Output: A clean grep-absence sweep, a green full suite, and a recorded lint verification (automated if luacheck is installed, else human-confirmed in an environment where it is).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-CONTEXT.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-RESEARCH.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-tree grep-absence sweep and full-suite regression</name>
  <files>none — verification-only, no files modified</files>
  <read_first>
    - .planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-01-SUMMARY.md (confirm Wave 1 plan 01 removals landed)
    - .planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-02-SUMMARY.md (confirm Wave 1 plan 02 removals landed)
  </read_first>
  <action>
    Run the combined grep-absence sweep across the whole tree (not just edited files): `grep -rn "hasPrimalSurge\|spellTexture\|CacheSpellTexture\|FALLBACK_ICON\|ParseOnOff" Duncedmaxxing/ spec/`. Every one of these symbols must return zero matches. If any match remains, it is an orphaned reference the Wave 1 plans missed — report it as a gate failure with the file:line so it can be fixed before proceeding.
    Then run the full fengari suite `npx -y -p fengari@0.1.5 node spec/run.cjs` and confirm 0 failed. The expected total = baseline 126 + 1 (new 1262293 test) - 2 (tautological Primal Surge tests) - 2 (spellTexture tests) - 15 (ParseOnOff tests) = 108. Report the actual pass/total.
  </action>
  <acceptance_criteria>
    - `grep -rn "hasPrimalSurge\|spellTexture\|CacheSpellTexture\|FALLBACK_ICON\|ParseOnOff" Duncedmaxxing/ spec/` returns zero matches.
    - `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed.
    - The reported total test count reflects the expected arithmetic (108, given no other tests were added/removed).
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "hasPrimalSurge\|spellTexture\|CacheSpellTexture\|FALLBACK_ICON\|ParseOnOff" Duncedmaxxing/ spec/ ; test $? -eq 1 && npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>The grep-absence sweep is clean for all five removed symbols and the full fengari suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: luacheck zero-warnings gate (human-verify)</name>
  <files>Duncedmaxxing/ (lint target — no files modified by this task)</files>
  <action>Verification-only, human-gated. Run luacheck against Duncedmaxxing/ in an environment where it is installed and confirm zero warnings, because luacheck/luarocks/lua are absent in this execution sandbox. Do not add lint suppressions for this phase — the deletion-only changes cannot introduce warnings, so any warning found predates this phase and should be reported, not silenced.</action>
  <what-built>All Wave 1 deletions are complete and the fengari suite is green. The remaining project quality gate is "luacheck zero warnings" on Duncedmaxxing/. luacheck, luarocks, and lua are absent in this execution sandbox (confirmed during Phase 7 research), so luacheck cannot be run here automatically. Because this phase is deletion-only, it can only reduce the addon's lint surface — it cannot introduce new warnings — but the zero-warnings gate is a stated project requirement (CLAUDE.md / CONTEXT.md) and must be confirmed before merge.</what-built>
  <how-to-verify>
    1. If luacheck is installed in your environment, run `luacheck Duncedmaxxing/` from the repo root and confirm it reports zero warnings. If it is not installed, install via `luarocks install luacheck` (Lua 5.1) or run the check on your dev machine / CI where luacheck is available.
    2. The repo's `.luacheckrc` configures `std = lua51` with a curated globals list; use it as-is (do not add suppressions for this phase).
    3. Confirm zero warnings, OR report any warning with its file:line so it can be addressed.
  </how-to-verify>
  <resume-signal>Type "approved" once luacheck reports zero warnings (state the environment used), or describe any warnings found.</resume-signal>
</task>

</tasks>

<verification>
- Grep-absence sweep across Duncedmaxxing/ and spec/ returns zero matches for all five removed symbols.
- `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed.
- luacheck zero warnings confirmed (automated if available, else via the blocking human-verify checkpoint).
</verification>

<success_criteria>
- No orphaned references to any removed symbol remain in the tree.
- The full suite is green.
- The luacheck zero-warnings gate is satisfied and recorded.
</success_criteria>

<output>
Create `.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-03-SUMMARY.md` when done.
</output>
