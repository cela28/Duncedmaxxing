---
milestone: v1.0
audited: 2026-07-09T00:00:00Z
reaudit_of: 2026-07-08T22:11:00Z
status: tech_debt
scores:
  requirements: 31/31
  phases: 8/8
  integration: 8/8
  flows: 5/5
gaps:
  requirements: []   # no unsatisfied or orphaned requirements
  integration: []    # 8/8 wired, re-confirmed by a fresh gsd-integration-checker run against post-Phase-7 code
  flows: []          # cast -> predict -> render (bar/number) -> aura-reconcile -> expiry all intact
tech_debt:  # non-blocking; the only substantive item is the still-uncommitted lint CI + never-run luacheck
  - phase: 07-address-v1-0-tech-debt-remove-dead-code
    items:
      - "luacheck zero-warnings gate — NOW DURABLY WIRED (2026-07-09, commit 091dca4). The prior audit recorded this as 'Closed (durable via CI)' but the .github/workflows/lint.yml file had never actually been committed (was '?? untracked' on dev, absent from main). It is now committed on dev and tracked, so the gate runs on push to main/dev and PRs to main via the curated .luacheckrc. Residual: luacheck cannot execute in this sandbox (absent), so the first *green* CI run has not yet been observed — that requires a push. Deletion-only Phase 7 cannot have introduced warnings, so risk is low; confirm the first CI run is green on next push."
  - phase: 06-options-panel-v2
    items:
      - "WR-01 (accepted): cfg.scale still applies to Number-mode rendering, but the Scale control is bar-only — a non-1.0 scale set in Bar mode persists into Number mode with no UI path to correct it from there. Narrow, does not fail any must-have."
      - "WR-02 (accepted): a returning user whose SavedVariables has tip.enabled=false has no UI path to re-enable (Enabled checkbox removed, slash mode subcommand dropped). Narrow edge case."
  - phase: repo-hygiene
    items:
      - "Working tree has uncommitted milestone paperwork before completion: modified .planning/REQUIREMENTS.md and .planning/v1.0-MILESTONE-AUDIT.md, and untracked .github/ (lint.yml), .claude/, and .planning/phases/06/.gitkeep. commit_docs is enabled — these should be committed as part of /gsd-complete-milestone v1.0."
  - phase: nyquist-coverage
    items:
      - "Phases 04 and 06 carry nyquist_compliant:false, but both are status:validated with wave_0_complete:true and full sign-off — the flag reflects justified manual-only items (04: PERF-02 retired + CICD-01 is CI-only; 06: DISP-05/07 are Options.lua UI behaviors the fengari harness structurally cannot load), each backed by human-UAT evidence. Not a coverage gap; recorded for transparency."
nyquist:
  compliant_phases: 6   # 00, 01, 02, 03, 05, 07
  partial_phases: 2     # 04, 06 (validated w/ justified manual-only; sign-off complete)
  missing_phases: 0
  overall: substantially_complete
---

# Milestone v1.0 Audit (Re-Audit, 2026-07-09) — Duncedmaxxing (Polish Pass)

**Audited:** 2026-07-09T00:00:00Z
**Re-audit of:** 2026-07-08T22:11:00Z (prior status: `tech_debt`)
**Status:** `tech_debt` — all 31/31 requirements satisfied, no critical blockers, cross-phase integration re-confirmed clean; a thin, low-risk set of deferred items remains, one of which corrects a prior-audit overstatement.

## Why this re-audit

This audit was re-run after Phase 7's validation-strategy finalization commits (2026-07-09) and independently re-checks cross-phase wiring against the current post-Phase-7 code via a fresh `gsd-integration-checker` run. It confirms the prior audit's requirement and integration conclusions **and corrects one closure claim**: the luacheck CI gate that the prior audit marked "Closed (durable via CI)" is in fact still open, because the `lint.yml` file was never committed.

## Summary

The milestone achieved its definition of done. All **31/31** v1 requirements are satisfied and verified, all **8/8** phases have passing/verified VERIFICATION.md reports, cross-phase wiring is **8/8 WIRED** end-to-end (0 orphaned, 0 broken — independently re-run this audit), the fengari suite is green (**111 passed / 0 failed**), and the addon is structurally clean with the dead code removed. No unsatisfied or orphaned requirements. No broken flows.

The remaining findings are non-blocking deferred items: an **uncommitted + never-run luacheck gate** (the one genuinely open verification item), two accepted narrow Phase-06 UI edge cases, uncommitted milestone paperwork to fold into completion, and two Nyquist phases whose "non-compliant" flag reflects justified manual-only verification rather than a coverage gap.

## Requirements Coverage (3-source cross-reference)

Sources: phase VERIFICATION.md status + requirements tables; SUMMARY.md `requirements-completed` frontmatter (populated inconsistently on phases 01–05 — VERIFICATION.md tables used as authority where empty); REQUIREMENTS.md traceability.

| REQ-ID | Phase | Verification | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|--------------|---------------------|-----------------|--------------|
| CLN-01..05 | 0 | passed 5/5 | listed (00-01) | `[x]` | **satisfied** |
| QUAL-01, QUAL-02, QUAL-04, QUAL-05 | 1 | verified (structural) + UAT | (empty; VERIFICATION authority) | `[x]` | **satisfied** |
| QUAL-03 | 3 | passed 9/9 | (empty) | `[x]` | **satisfied** |
| BUG-01..04 | 3 | passed 9/9 | (empty) | `[x]` | **satisfied** |
| TEST-01..07 | 2 | passed 7/7 | partial (02-03) | `[x]` | **satisfied** |
| PERF-01 | 4 | passed 11/11 | (empty) | `[x]` | **satisfied** |
| PERF-02 | 4 | passed 11/11 (impl later retired Phase 7) | (empty) | `[x]` | **satisfied** (see doc note) |
| CICD-01 | 4 | passed 11/11 | (empty) | `[x]` | **satisfied** (release.yml lives on `main`) |
| DISP-01..04 | 5 | passed 6/6 | listed (05-01) | `[x]` | **satisfied** |
| DISP-05, DISP-06, DISP-07 | 6 | verified 7/7 (2 accepted overrides, human_verified) | listed (06-*) | `[x]` | **satisfied** |

**31/31 satisfied. 0 unsatisfied. 0 orphaned.** FAIL gate not triggered.

- **Phase 01 note:** the 4 structural requirements (QUAL-01/02/04/05) were VERIFIED. The 2 originally-unverified must-haves were runtime UAT behaviors (Kill Command with Twin Fangs; Raptor Strike under Aspect of the Eagle) — both resolved by the full-milestone human smoke test (7 passed, 1 user-skipped, 0 issues).
- **PERF-02 note:** satisfied and tested when Phase 4 shipped (texture resolved/cached once at Initialize). Phase 5 removed icon mode (the sole consumer), orphaning the cache; Phase 7 deleted it as dead code. The requirement was met; its code no longer exists. REQUIREMENTS.md now annotates this ("Complete → retired (Phase 7)").
- **CICD-01 note:** the release workflow (`.github/workflows/release.yml`) exists on the **`main`** branch — the production branch where releases are cut by pushing a `v*` tag. It was intentionally removed from `dev` (commit `58d3907`, "chore: remove stale release workflow from dev"). Confirmed this audit: `git ls-tree main -- .github/` → `release.yml` present; `git ls-tree dev` → none. Satisfied, not a gap.
- **Phase 06 overrides (2, accepted by cela28):** Enabled checkbox removed from panel (DB field retained), Border color is Bar-only, mode switching is button-only. Aligned into ROADMAP/REQUIREMENTS by plan 06-08.

## Phase Verification Rollup

| Phase | Status | Score | Overrides |
|-------|--------|-------|-----------|
| 00 Repo Cleanup | passed | 5/5 | 0 |
| 01 Utility Extraction | human_needed → resolved via UAT | 7/9 (2 runtime, UAT-confirmed) | 0 |
| 02 Test Framework | passed | 7/7 | 0 |
| 03 Bug Fixes | passed | 9/9 | 0 |
| 04 Performance & CI/CD | passed | 11/11 | 0 |
| 05 Display Modes Refactor | passed | 6/6 | 0 |
| 06 Options Panel v2 | verified (human_verified) | 7/7 | 2 (accepted) |
| 07 Address v1.0 Tech Debt | passed | 7/7 | 0 |

## Cross-Phase Integration (fresh run, gsd-integration-checker, sonnet)

**Result: 8/8 connections WIRED, 0 orphaned, 0 missing, 0 broken flows.** Suite re-run green (111/111).

Verified end-to-end against current post-Phase-7 code:
1. TOC load order → `Util.lua` defines `DMX.Util` (Clamp, ParseHexColor, Trim) before Core/Options; Options consumes it (QUAL-01)
2. Module registry: `moduleOrder` → `ForEachModule` (ordered) → `Tip:Initialize` (QUAL-04)
3. Config lifecycle: `DEFAULTS` → `MergeDefaults` → `NormalizeDB` (legacy `stackColors` re-seed via `StackColorsAreLegacyFormat`/`ConvertLegacyStackColors`; displayMode unknown→`bar` fallback) → live `GetCfg()` (QUAL-02/03, DISP-05..07)
4. Render branching: `cfg.displayMode` → bar vs number only; zero "icon" hits in Lua source (DISP-01..04)
5. E2E combat flow: `UNIT_SPELLCAST_SUCCEEDED` → `ClassifySpellID` → `ApplySpell` (predict) → `Update` render → `ScheduleCastVerify` → `SyncFromAura` reconcile → expiry; `auraVerifyPending` cleared as the first statement in the timer callback, before the serial-mismatch early return (BUG-01..04)
6. Options per-mode visibility (`SetMode`/`Refresh`) with `InCombatLockdown` guard intact (`CanChange`, `OnShow`, `PLAYER_REGEN_DISABLED` hide) (DISP-05/07)
7. Per-stack color config: Options inputs ↔ `DEFAULTS.tip.stackColors` named keys ↔ number-mode render (`colorByStack` ON = per-stack, OFF = flat `textColor`) (DISP-06)
8. Phase 7 removals clean: grep-absence for `hasPrimalSurge`, `spellTexture`, `CacheSpellTexture`, `FALLBACK_ICON`, `ParseOnOff` → zero hits across `Duncedmaxxing/` and `spec/`; no live path depends on any removed symbol (D-01..D-07)

The checker flagged QUAL-05 (pcall removal — an internal-quality req with no distinct cross-phase wire, expected) and CICD-01 (CI file outside its source scope) for the milestone auditor; both resolved above (QUAL-05 verified structurally in 01-VERIFICATION; CICD-01 confirmed present on `main`).

## Broken Flows

None. `cast spell → predicted stack update → render (bar/number) → aura reconcile → expiry` traces without breaks, backed by passing specs.

## Nyquist Coverage

The validate-phase step hook is active. Coverage is substantially complete — 6/8 phases fully compliant, 2 partial-but-validated, 0 missing.

| Phase | VALIDATION.md | nyquist_compliant | Notes |
|-------|---------------|-------------------|-------|
| 00 Repo Cleanup | approved | **true** | — |
| 01 Utility Extraction | approved | **true** | — |
| 02 Test Framework | approved | **true** | — |
| 03 Bug Fixes | approved | **true** | — |
| 04 Performance & CI/CD | validated | false | PARTIAL — PERF-01 automated; PERF-02 retired (Phase 7); CICD-01 CI-only. Sign-off complete. |
| 05 Display Modes | approved | **true** | — |
| 06 Options Panel v2 | validated | false | PARTIAL — DISP-06 automated; DISP-05/07 are Options.lua UI behaviors the fengari harness structurally cannot load (`init.lua` skips `Options.lua`), recorded manual-only with human-UAT evidence (06-UAT 4/4). Sign-off complete. |
| 07 Address Tech Debt | approved | **true** | — |

The two `false` phases are not coverage gaps — each is `status: validated`, `wave_0_complete: true`, with a completed sign-off; their manual-only items are inherent to the target (external CI, or WoW Widget UI outside the offline harness), not deferred work.

## Tech Debt (highest-value first)

1. **luacheck zero-warnings gate — NOW DURABLY WIRED (commit `091dca4`, 2026-07-09).** The prior audit claimed this was closed via `.github/workflows/lint.yml`, but that file had never been committed (untracked on `dev`, absent from `main`). It has now been committed on `dev` and is tracked, so the gate runs on push to `main`/`dev` and PRs to `main` using the curated `.luacheckrc`. **Residual:** luacheck cannot run in this sandbox (absent), so the first *green* CI run has not yet been observed — confirm it on the next push. Risk is low (Phase 7 was deletion-only).
2. **Uncommitted milestone paperwork.** Working tree has modified `.planning/REQUIREMENTS.md` and this audit file, plus untracked `.github/` (lint.yml), `.claude/`, and a Phase-06 `.gitkeep`. Fold these into `/gsd-complete-milestone v1.0` (commit_docs enabled).
3. **Phase 06 accepted edge cases (WR-01/WR-02)** — scale persists into Number mode with no in-mode control; `enabled=false` users have no UI re-enable path. Both narrow, both accepted; revisit only if a user hits them.

---

_Re-audited: 2026-07-09 — Claude (gsd-audit-milestone). Integration re-run: gsd-integration-checker (sonnet), 8/8 WIRED, suite 111/111 green._
