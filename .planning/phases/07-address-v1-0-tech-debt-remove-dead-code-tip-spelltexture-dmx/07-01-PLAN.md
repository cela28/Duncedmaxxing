---
phase: 07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
  - spec/tip_spec.lua
  - spec/support/init.lua
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06]

must_haves:
  truths:
    - "D-06: Tip._test.ClassifySpellID is exported from TipOfTheSpear.lua and spec/tip_spec.lua asserts ClassifySpellID returns 'consumer' for 265189, 1262293, and 1262343"
    - "D-01: hasPrimalSurge no longer exists anywhere in Duncedmaxxing/ or spec/ (grep returns zero matches)"
    - "D-02: no test references hasPrimalSurge; the 3 tautological Primal Surge tests are gone and genuine BASE/Twin-Fangs generator coverage is retained"
    - "D-03: the generator-branch comment in TipOfTheSpear.lua states the grant is always 2 stacks, with no 'base 1 / +1 Primal Surge' framing and no 'field reserved' note"
    - "D-04: Tip.spellTexture, CacheSpellTexture, and FALLBACK_ICON are fully removed; BOTH CacheSpellTexture call sites (Tip:OnEvent PLAYER_LOGIN branch and Tip:Initialize) are gone; no spellTexture tests remain"
    - "The full fengari suite is green after each task commit"
  artifacts:
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Tip._test test-only export table (new symbol) exposing ClassifySpellID"
      contains: "Tip._test"
    - path: "spec/tip_spec.lua"
      provides: "Hardened consumer regression tests (265189/1262293/1262343) with ClassifySpellID membership assertions"
      contains: "Tip._test.ClassifySpellID"
  key_links:
    - from: "spec/tip_spec.lua"
      to: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      via: "spec calls Tip._test.ClassifySpellID(...) to assert CONSUMERS membership"
      pattern: "Tip\\._test\\.ClassifySpellID"
---

<objective>
Clean the dead code and false-confidence tests concentrated in the Tip module and its spec, and harden the consumer regression tests. Covers D-06 (add Tip._test export + ClassifySpellID membership assertions), D-01/D-02/D-03 (remove hasPrimalSurge field, its resets, its tautological tests, and rewrite the self-contradictory generator comment), and D-04 (remove Tip.spellTexture, CacheSpellTexture, FALLBACK_ICON, and BOTH call sites).

Purpose: Retire dead symbols that outlived their features (icon mode removed in Phase 5; Primal Surge spell ID never resolved) and replace tautological tests with assertions that actually guard CONSUMERS-table membership.

Output: A trimmed TipOfTheSpear.lua, a hardened/trimmed spec/tip_spec.lua, and a trimmed spec/support/init.lua reset harness — with the fengari suite green at every commit and no orphaned references to any removed symbol.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

## Artifacts this phase produces

This is a deletion-and-hardening phase — it removes symbols and does not add runtime features. The ONLY new symbol created across the entire phase is:

- **`Tip._test`** (test-only export table) in `Duncedmaxxing/Modules/TipOfTheSpear.lua` — mirrors the existing `DMX._test` convention in `Core.lua`; exposes the file-local `ClassifySpellID` function to `spec/tip_spec.lua` so D-06's CONSUMERS-membership assertions are reachable. Not for production use.

Everything else this phase touches is a removal (D-01 hasPrimalSurge, D-04 spellTexture/CacheSpellTexture/FALLBACK_ICON, D-05 ParseOnOff), a comment rewrite (D-03), a comment addition (D-07), or additive test assertions (D-06).

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-CONTEXT.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-RESEARCH.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Tip._test export and harden consumer regression tests (D-06)</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua, spec/tip_spec.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (read lines 70-90 for the ClassifySpellID local function and the CONSUMERS table near line 24-27; read lines 755-761 for the file tail where DMX:RegisterModule("tip", Tip) sits)
    - Duncedmaxxing/Core.lua (read lines 234-241 for the existing DMX._test escape-hatch pattern to replicate exactly, including its two-line "Test-only escape hatch / Do not use in production" comment)
    - spec/tip_spec.lua (read lines 200-219 for the existing 265189 and 1262343 regression tests to extend)
  </read_first>
  <action>
    Re-grep before editing (line numbers may have shifted): run `grep -n "RegisterModule\|ClassifySpellID" Duncedmaxxing/Modules/TipOfTheSpear.lua`.
    In Duncedmaxxing/Modules/TipOfTheSpear.lua, add a Tip._test table immediately BEFORE the final `DMX:RegisterModule("tip", Tip)` line, mirroring the DMX._test convention in Core.lua: include the same two-line "Test-only escape hatch: exposes local functions for spec/tip_spec.lua. / Do not use in production addon code." comment, then assign `Tip._test = { ClassifySpellID = ClassifySpellID }`. The table must be defined after the ClassifySpellID local function definition so the upvalue resolves.
    In spec/tip_spec.lua, extend the existing test named for 265189 by adding, as its first assertion, an equals-check that Tip._test.ClassifySpellID(265189) is "consumer" (keep the existing Tip.stacks=2 / ApplySpell("consumer",265189) / equals 1 assertions — this is additive per D-06, NOT a rewrite through event dispatch). Apply the identical classify assertion to the existing 1262343 test. Add ONE new minimal it() block for 1262293 (base Raptor Swipe) asserting classify == "consumer" plus a decrement (stacks=2 then ApplySpell("consumer",1262293) yields 1), for parity with its siblings — 1262293 currently has zero spec coverage (it appears only in the CONSUMERS table).
  </action>
  <acceptance_criteria>
    - `grep -c "Tip._test" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns at least 1 and the assignment `Tip._test = {` appears before `DMX:RegisterModule("tip", Tip)`.
    - `grep -c "Tip._test.ClassifySpellID" spec/tip_spec.lua` returns at least 3 (one per spell ID: 265189, 1262293, 1262343).
    - spec/tip_spec.lua contains an it() block referencing 1262293 that did not exist before.
    - `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed; total is >= baseline 126 (new 1262293 test increases the count).
  </acceptance_criteria>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>The suite is green with the 1262293 test added; all three consumer regression tests assert ClassifySpellID(...) == "consumer" via Tip._test, and existing decrement assertions are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hasPrimalSurge and rewrite the generator comment (D-01, D-02, D-03)</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua, spec/support/init.lua, spec/tip_spec.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (read line 64 for the field init and lines 652-661 for the generator branch and its comment)
    - spec/support/init.lua (read lines 60-66 for the resetTipState hasPrimalSurge line)
    - spec/tip_spec.lua (read lines 30-32 for the before_each reset at line 31, and lines 117-152 for the four generator tests: the expiry test at 118, the Twin-Fangs-independence test at 130-136, and the two tautological Primal Surge tests at 139-143 and 146-151)
  </read_first>
  <action>
    Re-grep first: run `grep -rn "hasPrimalSurge" Duncedmaxxing/ spec/` and eliminate EVERY match (production, reset harness, tests, and comments), then confirm zero.
    In Duncedmaxxing/Modules/TipOfTheSpear.lua: delete the `Tip.hasPrimalSurge = false` field init. Rewrite the generator-branch comment (the two-to-three comment lines above `local grant = 2` and the inline comment on the grant line) per D-03 so it plainly states Kill Command (generator) always grants 2 stacks and that Twin Fangs is a Takedown/consumer modifier that must not affect this path — the new comment must NOT contain the retired field name or the "base 1, +1 with Primal Surge" / "reserved for future ID resolution" framing. Leave `local grant = 2` and the stack/expiry math unchanged (no logic change).
    In spec/support/init.lua: delete the `Tip.hasPrimalSurge = false` reset line inside resetTipState.
    In spec/tip_spec.lua: delete the before_each reset assignment of the retired field; delete the two tautological Primal Surge it() blocks (the one that sets the field true asserting 2, and the one that sets it false asserting BASE). KEEP the Twin-Fangs-independence test but remove only its retired-field toggle line (retain its hasTwinFangs=true setup and both the not_equals(3) and equals(2) assertions). Remove any explanatory comment text that names the retired field, including parentheticals on retained BASE=2 comments (e.g. the "flat-2 fallback path" comment on the expiry test must drop the retired-field parenthetical).
  </action>
  <acceptance_criteria>
    - `grep -rn "hasPrimalSurge" Duncedmaxxing/ spec/` returns zero matches.
    - The generator branch in TipOfTheSpear.lua still contains `local grant = 2` (logic unchanged) and `grep -c "grant = 2" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns at least 1.
    - The Twin-Fangs-independence test still exists (it() block asserting the generator grant does not reach 3 with hasTwinFangs true) and still asserts equals 2.
    - `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed (total drops by exactly 2 tautological tests relative to Task 1's count).
  </acceptance_criteria>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>No occurrence of the retired field remains in Duncedmaxxing/ or spec/, the generator comment is non-contradictory, the flat-2 grant logic is unchanged, and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Tip.spellTexture, CacheSpellTexture, FALLBACK_ICON, and both call sites (D-04)</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua, spec/support/init.lua, spec/tip_spec.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (read line 12 for FALLBACK_ICON, line 65 for the spellTexture field, lines 147-159 for the CacheSpellTexture comment+definition, line 696 for the call inside Tip:OnEvent PLAYER_LOGIN/PLAYER_ENTERING_WORLD branch, and line 743 for the SECOND call inside Tip:Initialize — this second call site is NOT listed in CONTEXT.md and MUST also be removed, per RESEARCH Pitfall 1)
    - spec/support/init.lua (read line 64 for the spellTexture reset)
    - spec/tip_spec.lua (read lines 479-528: the describe block titled "Caching -- isSurvival and spellTexture" that starts at line 482 — KEEP the 4 isSurvival/spec-change/talent tests; delete ONLY the two spellTexture it() blocks at ~518 and ~524 and scrub "spellTexture" from the describe title and the surrounding comments at lines 480 and 517)
  </read_first>
  <action>
    Re-grep first: run `grep -rn "spellTexture\|CacheSpellTexture\|FALLBACK_ICON" Duncedmaxxing/ spec/` and eliminate EVERY match, then confirm zero.
    In Duncedmaxxing/Modules/TipOfTheSpear.lua: delete the FALLBACK_ICON constant, the `Tip.spellTexture = nil` field init, the entire CacheSpellTexture function (its comment header through its `end`), and BOTH `CacheSpellTexture(self)` call sites — the one in the Tip:OnEvent PLAYER_LOGIN/PLAYER_ENTERING_WORLD branch AND the one in Tip:Initialize. Deleting only one call site leaves a nil-call reference that fails every loader.load() in the suite.
    In spec/support/init.lua: delete the `Tip.spellTexture = nil` reset line inside resetTipState.
    In spec/tip_spec.lua: delete the two it() blocks that assert on Tip.spellTexture (non-nil-after-Initialize and equals-132275). Retain the describe block and its 4 non-spellTexture tests (isSurvival-after-Initialize, PLAYER_SPECIALIZATION_CHANGED x2, PLAYER_TALENT_UPDATE). Rename the describe title to drop "and spellTexture" and scrub the two surrounding comment lines that mention the field, so no occurrence of the removed symbol names survives the grep-absence gate.
  </action>
  <acceptance_criteria>
    - `grep -rn "spellTexture\|CacheSpellTexture\|FALLBACK_ICON" Duncedmaxxing/ spec/` returns zero matches (including the describe-block title and comments).
    - Both CacheSpellTexture call sites are gone: `grep -c "CacheSpellTexture" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns 0.
    - The four retained isSurvival/spec/talent tests still exist in tip_spec.lua (the describe block was trimmed, not deleted).
    - `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed (total drops by exactly 2 relative to Task 2's count; no nil-call error surfaces from loader.load()).
  </acceptance_criteria>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>All three removed symbols are absent from Duncedmaxxing/ and spec/, both call sites are gone, the isSurvival caching tests are retained, and the suite is green with no nil-call runtime error.</done>
</task>

</tasks>

<verification>
- Run `npx -y -p fengari@0.1.5 node spec/run.cjs` after every task — expect 0 failed at each commit.
- After the final task, run the combined grep-absence sweep: `grep -rn "hasPrimalSurge\|spellTexture\|CacheSpellTexture\|FALLBACK_ICON" Duncedmaxxing/ spec/` returns zero matches.
- Confirm the new symbol exists: `grep -c "Tip._test" Duncedmaxxing/Modules/TipOfTheSpear.lua` >= 1.
</verification>

<success_criteria>
- hasPrimalSurge, Tip.spellTexture, CacheSpellTexture, and FALLBACK_ICON are fully removed with zero orphaned references in Duncedmaxxing/ or spec/.
- The generator-branch comment is non-contradictory and the flat-2 grant logic is unchanged.
- Tip._test.ClassifySpellID export exists and the three consumer regression tests (265189, 1262293, 1262343) assert CONSUMERS membership.
- The fengari suite is green after each task.
</success_criteria>

<output>
Create `.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-01-SUMMARY.md` when done.
</output>
