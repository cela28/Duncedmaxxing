---
phase: 07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Util.lua
  - spec/util_spec.lua
  - Duncedmaxxing/Core.lua
autonomous: true
requirements: [D-05, D-07]

must_haves:
  truths:
    - "D-05: DMX.Util.ParseOnOff is fully removed — the local function, the Util.ParseOnOff export, and the entire ParseOnOff describe block in spec/util_spec.lua are gone (grep returns zero matches in Duncedmaxxing/ and spec/)"
    - "D-07: the db.locked = true line in the NormalizeDB migration block is RETAINED with an intent comment documenting that the frame deliberately starts locked after every settings-migration upgrade so it cannot be accidentally dragged"
    - "The full fengari suite is green after each task commit"
  artifacts:
    - path: "Duncedmaxxing/Util.lua"
      provides: "Utility module with ParseOnOff removed (Trim, Clamp, ParseHexColor retained)"
    - path: "Duncedmaxxing/Core.lua"
      provides: "NormalizeDB migration block with documented db.locked intent (behavior unchanged)"
      contains: "db.locked = true"
  key_links: []
---

<objective>
Remove the last dead exported utility and document a correct-but-previously-unexplained migration side effect. Covers D-05 (delete DMX.Util.ParseOnOff — the slash on/off parser that no longer has a caller since the slash interface became settings-only) and D-07 (keep the db.locked = true migration line, adding an intent comment so it is not re-flagged in a future audit).

Purpose: ParseOnOff is dead exported surface flagged by the integration checker; deleting it reduces the addon's string-parsing surface with zero behavior change. D-07 requires NO logic change — a passing test (spec/core_spec.lua:141-145) already locks in db.locked = true after migration; this only adds a comment.

Output: A trimmed Util.lua and util_spec.lua with no ParseOnOff references, and a Core.lua NormalizeDB block whose db.locked line carries a short intent comment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-CONTEXT.md
@.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove DMX.Util.ParseOnOff fully (D-05)</name>
  <files>Duncedmaxxing/Util.lua, spec/util_spec.lua</files>
  <read_first>
    - Duncedmaxxing/Util.lua (read lines 14-45: the ParseOnOff local function at 18-24 and the Util.ParseOnOff export at line 42 — the Trim, Clamp, ParseHexColor definitions and exports must all stay)
    - spec/util_spec.lua (read line 2 for the file-header comment that lists ParseOnOff, and lines 120-185 for the entire `describe("DMX.Util.ParseOnOff", ...)` block of 15 it() tests to delete)
  </read_first>
  <action>
    Re-grep first: run `grep -rn "ParseOnOff" Duncedmaxxing/ spec/` and eliminate EVERY match, then confirm zero.
    In Duncedmaxxing/Util.lua: delete the entire `local function ParseOnOff(value) ... end` definition and the `Util.ParseOnOff = ParseOnOff` export line. Leave Trim, Clamp, and ParseHexColor and their exports untouched.
    In spec/util_spec.lua: delete the entire `describe("DMX.Util.ParseOnOff", function() ... end)` block (all 15 it() tests). Also remove the word naming ParseOnOff from the file-header comment on line 2 so the grep-absence gate passes (the header currently lists the tested functions).
    Confirm zero production callers before deleting: `grep -rn "ParseOnOff" Duncedmaxxing/` must only show Util.lua's own definition/export prior to deletion (RESEARCH confirmed no other production caller — slash is settings-only since quick task 260624-0hx).
  </action>
  <acceptance_criteria>
    - `grep -rn "ParseOnOff" Duncedmaxxing/ spec/` returns zero matches (including the util_spec.lua header comment).
    - The retained utilities are still exported: `grep -c "Util.Trim\|Util.Clamp\|Util.ParseHexColor" Duncedmaxxing/Util.lua` returns at least 3.
    - `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed (total drops by exactly 15 ParseOnOff tests).
  </acceptance_criteria>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>ParseOnOff is absent from Duncedmaxxing/ and spec/, the other three utilities remain exported and tested, and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Document the db.locked migration intent (D-07)</name>
  <files>Duncedmaxxing/Core.lua</files>
  <read_first>
    - Duncedmaxxing/Core.lua (read lines 112-130: the NormalizeDB function and its migration-gate block; the `db.locked = true` line is at line 123 inside the `if db.settingsMigration ~= SETTINGS_MIGRATION then` block)
    - spec/core_spec.lua (read lines 141-145: the existing passing test "sets db.locked = true during migration" — this test needs NO change and confirms the behavior stays)
  </read_first>
  <action>
    Re-grep first: run `grep -n "db.locked" Duncedmaxxing/Core.lua` to confirm the current line position (may have shifted).
    In Duncedmaxxing/Core.lua, add a brief intent comment on or immediately above the `db.locked = true` line inside NormalizeDB's migration-gate block, stating that the frame is deliberately re-locked after every settings-migration upgrade so it cannot be accidentally dragged. Make NO logic change — the line itself and all surrounding migration statements (stackColors re-seed, barWidth/barHeight/spacing nil-out, settingsMigration bump) stay exactly as they are. Comment length is at discretion (one line or a short block).
  </action>
  <acceptance_criteria>
    - `grep -c "db.locked = true" Duncedmaxxing/Core.lua` returns exactly 1 (line retained, unchanged).
    - A comment referencing the deliberate post-migration lock appears on or adjacent to that line (the line is no longer bare/undocumented).
    - No other statement in the NormalizeDB migration block was added or removed (diff shows only a comment addition).
    - `npx -y -p fengari@0.1.5 node spec/run.cjs` reports 0 failed and the existing "sets db.locked = true during migration" test still passes.
  </acceptance_criteria>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>The db.locked = true migration line is retained with a documented intent comment, no logic changed, and the suite (including the pre-existing db.locked migration test) is green.</done>
</task>

</tasks>

<verification>
- Run `npx -y -p fengari@0.1.5 node spec/run.cjs` after each task — expect 0 failed.
- Grep-absence sweep: `grep -rn "ParseOnOff" Duncedmaxxing/ spec/` returns zero matches.
- Behavior-retained check: `grep -c "db.locked = true" Duncedmaxxing/Core.lua` returns 1.
</verification>

<success_criteria>
- DMX.Util.ParseOnOff is fully removed with zero orphaned references; Trim/Clamp/ParseHexColor remain intact and tested.
- The db.locked = true migration line is retained and documented as designed, with no logic change.
- The fengari suite is green after each task.
</success_criteria>

<output>
Create `.planning/phases/07-address-v1-0-tech-debt-remove-dead-code-tip-spelltexture-dmx/07-02-SUMMARY.md` when done.
</output>
