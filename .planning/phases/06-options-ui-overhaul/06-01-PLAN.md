---
phase: 06-options-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Core.lua
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
  - spec/core_spec.lua
  - spec/tip_spec.lua
autonomous: true
requirements:
  - PHASE-06-GOAL
must_haves:
  truths:
    - "db.tip.stackColors contains 4 color entries (indices 0-3) after MergeDefaults on a fresh DB"
    - "db.tip does not contain an enabled field after NormalizeDB migration"
    - "Tip:Update() reads stack colors from GetCfg().stackColors, falling back to STACK_COLORS constant"
    - "shouldShow expression does not reference cfg.enabled — tracker visibility is gated on isSurvival alone"
    - "SETTINGS_MIGRATION version is bumped to trigger fresh defaults for upgrading users"
    - "All tests pass via busted spec/"
  artifacts:
    - path: "Duncedmaxxing/Core.lua"
      provides: "stackColors in DEFAULTS.tip, enabled removed, migration bump"
      contains: "stackColors"
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Config-driven stack color read, simplified shouldShow"
    - path: "spec/core_spec.lua"
      provides: "Updated fixtures without enabled, new stackColors merge test"
    - path: "spec/tip_spec.lua"
      provides: "Updated number mode tests, config-driven color test"
  key_links:
    - from: "Duncedmaxxing/Core.lua"
      to: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      via: "DEFAULTS.tip.stackColors populated by MergeDefaults, read by Tip:Update() via GetCfg()"
      pattern: "stackColors"
---

<objective>
Add per-stack color configuration to the DB schema and switch Tip:Update() from hardcoded STACK_COLORS to config-driven colors. Remove the enabled field from DEFAULTS and shouldShow visibility logic. Bump the settings migration version to propagate schema changes to existing saves. Implements D-01, D-02, D-06.

Purpose: Establishes the data layer that Plan 02 (Options UI restructure) will build settings widgets against. Ensures the rendering path reads user-configurable stack colors and the tracker is always active for survival spec without an enabled toggle.

Output: Updated Core.lua (DEFAULTS + migration), updated TipOfTheSpear.lua (shouldShow + color read), updated test fixtures and new assertions in spec/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-options-ui-overhaul/06-CONTEXT.md
@.planning/phases/06-options-ui-overhaul/06-RESEARCH.md
@.planning/phases/06-options-ui-overhaul/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stackColors to DEFAULTS, remove enabled, bump migration, simplify shouldShow, switch color read</name>
  <files>Duncedmaxxing/Core.lua, Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <read_first>
    Duncedmaxxing/Core.lua
    Duncedmaxxing/Modules/TipOfTheSpear.lua
    .planning/phases/06-options-ui-overhaul/06-RESEARCH.md
    .planning/phases/06-options-ui-overhaul/06-PATTERNS.md
  </read_first>
  <behavior>
    - Test: MergeDefaults on fresh DB populates stackColors[0] through stackColors[3] as {r,g,b,a} tables
    - Test: stackColors[0].r equals 1 (white), stackColors[1].r is near 0.18039 (green), stackColors[3].r equals 1 (red-orange)
    - Test: NormalizeDB migration branch fires when settingsMigration differs from new SETTINGS_MIGRATION value, resetting tip fields and populating stackColors from fresh defaults
    - Test: NormalizeDB idempotency check no longer asserts db.tip.enabled (field removed from DEFAULTS)
    - Test: Tip:Update() with custom db.tip.stackColors reads color from config, not hardcoded STACK_COLORS
    - Test: shouldShow is true when Tip.isSurvival is true, without needing cfg.enabled
  </behavior>
  <action>
    **Core.lua changes (per D-02, D-06):**

    1. In DEFAULTS.tip (around line 14), remove the line `enabled = true`.

    2. After the `textColor` entry (around line 28), add the stackColors table with explicit 0-based indexing:
       - Key [0]: r=1, g=1, b=1, a=1 (white, 0 stacks)
       - Key [1]: r=0.18039, g=0.80000, b=0.44314, a=1 (green)
       - Key [2]: r=1, g=0.94118, b=0, a=1 (gold)
       - Key [3]: r=1, g=0.29804, b=0.18824, a=1 (red-orange)
       These values match the existing STACK_COLORS constant in TipOfTheSpear.lua.

    3. Change SETTINGS_MIGRATION (line 10) from "0.3.2-fontfix" to "0.3.2-stackcolors". This triggers the NormalizeDB migration branch for existing users, resetting tip fields to the new defaults (which now include stackColors and exclude enabled).

    **TipOfTheSpear.lua changes (per D-02, D-06):**

    4. In Tip:Update() at line 595, change the shouldShow expression from:
       `local shouldShow = unlocked or self.testMode or (cfg.enabled and self.isSurvival)`
       to:
       `local shouldShow = unlocked or self.testMode or self.isSurvival`
       This removes the cfg.enabled gate entirely.

    5. In Tip:Update() number mode branch (lines 621-622), replace the hardcoded STACK_COLORS read with a config-driven read using the existing ColorTuple function for format normalization. The STACK_COLORS constant (lines 32-37) is kept as a nil-safety fallback. Read the config stackColors via GetCfg().stackColors, fall back to STACK_COLORS if nil. For the individual stack entry, fall back to index [0] of whichever table is active, then to STACK_COLORS[0] as final safety. Use ColorTuple to normalize the {r,g,b,a} named-key format from config into positional (r,g,b,a) values, since STACK_COLORS uses array format {1,2,3,4} while config uses named-key format {r,g,b,a}.
  </action>
  <verify>
    <automated>busted spec/</automated>
  </verify>
  <acceptance_criteria>
    - Core.lua DEFAULTS.tip contains stackColors table with keys [0], [1], [2], [3]; each value is a table with r, g, b, a fields
    - Core.lua DEFAULTS.tip does NOT contain an enabled field
    - Core.lua SETTINGS_MIGRATION equals "0.3.2-stackcolors"
    - TipOfTheSpear.lua line containing shouldShow does NOT contain "cfg.enabled" or "enabled"
    - TipOfTheSpear.lua number mode branch calls ColorTuple for stack color normalization and reads from GetCfg().stackColors
    - STACK_COLORS constant remains unchanged at lines 32-37 as a fallback
    - busted spec/ exits 0
  </acceptance_criteria>
  <done>Core.lua has stackColors in DEFAULTS, no enabled field, bumped migration. TipOfTheSpear.lua reads config-driven stack colors and shouldShow omits cfg.enabled. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update test fixtures and add new assertions for stackColors and enabled removal</name>
  <files>spec/core_spec.lua, spec/tip_spec.lua</files>
  <read_first>
    spec/core_spec.lua
    spec/tip_spec.lua
    Duncedmaxxing/Core.lua
    Duncedmaxxing/Modules/TipOfTheSpear.lua
  </read_first>
  <action>
    **spec/core_spec.lua updates:**

    1. Remove `enabled = true` from the fixture helper at line 63 (the tip table inside the migratedDB helper). The field no longer exists in DEFAULTS.
    2. Remove `enabled = true` from the fixture at line 154 (second migratedDB variant).
    3. Remove `enabled = true` from the fixture at line 200 (migration test fixture).
    4. Remove `enabled = true` from the fixture at line 270 (another migration fixture).
    5. Remove the assertion `assert.equals(true, db.tip.enabled)` at line 243 — this was the NormalizeDB idempotency test checking that enabled survived migration; the field no longer exists.
    6. Add a new test in the MergeDefaults describe block: assert that MergeDefaults on a fresh {tip = {}} target populates stackColors[0] through stackColors[3] from defaults. Assert stackColors[0] is a table, stackColors[3] is a table, and stackColors[0].r equals 1 (white). Assert stackColors[1].r is near 0.18039 using assert.near with tolerance 0.001.
    7. Add a new test in the NormalizeDB describe block: assert that after NormalizeDB runs the migration path (settingsMigration mismatch), db.tip.stackColors is populated with 4 entries matching the defaults. Use DMX._test.SETTINGS_MIGRATION by reference, never hardcode the version string.

    **spec/tip_spec.lua updates:**

    8. Remove `db.tip.enabled = true` at line 593 in the number mode color test setup. After enabled removal, visibility is gated by Tip.isSurvival = true which is already set in the test.
    9. Add a new test: set db.tip.stackColors to a custom color table with [0] = {r=1, g=0, b=0, a=1} (red). Call Tip:Update() with stacks = 0. Assert that Tip.numberText._textColor contains {1, 0, 0, 1} (the custom red, not the default white). This verifies config-driven color reads.
    10. Add a second new test: verify that when db.tip.stackColors is nil, Tip:Update() falls back to the hardcoded STACK_COLORS constant. Set stacks = 1 and verify the green color {0.18039, 0.80000, 0.44314, 1} is applied.
  </action>
  <verify>
    <automated>busted spec/</automated>
  </verify>
  <acceptance_criteria>
    - spec/core_spec.lua contains no reference to enabled = true in any fixture helper
    - spec/core_spec.lua does not assert db.tip.enabled
    - spec/core_spec.lua contains a test that verifies stackColors[0] through stackColors[3] are populated after MergeDefaults
    - spec/core_spec.lua contains a test that verifies stackColors survives NormalizeDB migration using DMX._test.SETTINGS_MIGRATION by reference
    - spec/tip_spec.lua line 593 does not contain db.tip.enabled
    - spec/tip_spec.lua contains a test asserting custom stackColors override the hardcoded STACK_COLORS
    - spec/tip_spec.lua contains a test asserting fallback to STACK_COLORS when stackColors is nil
    - busted spec/ exits 0 with all tests passing
  </acceptance_criteria>
  <done>All test fixtures updated for enabled removal. New stackColors merge, migration, and config-driven color tests pass. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User hex input | Hex strings entered in options inputs; already validated by ParseHexColor which returns nil for invalid input |
| SavedVariables | Persisted DB read by WoW client on load; MergeDefaults + NormalizeDB enforce schema on every load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | db.tip.stackColors | low | accept | WoW SavedVariables are local files on the player's machine; tampering affects only the tamperer's UI. MergeDefaults ensures missing sub-keys get defaults. NormalizeDB migration resets the full tip table when version mismatches. |
| T-06-02 | Information Disclosure | stackColors config values | low | accept | Color values are purely cosmetic display preferences with no sensitive data. No network transmission occurs. |

No network surface, no authentication, no cross-user data, no secrets. ASVS L1 requires mitigating critical/high threats; none exist in this addon's threat model.
</threat_model>

<verification>
1. `busted spec/` passes with all existing + new tests green
2. `grep -c "enabled" Duncedmaxxing/Core.lua` returns 0 for the DEFAULTS.tip section (no enabled field)
3. `grep "stackColors" Duncedmaxxing/Core.lua` returns hits in DEFAULTS.tip
4. `grep "cfg.enabled" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns 0 hits
5. `grep "ColorTuple" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns a hit in the number mode branch of Tip:Update()
</verification>

<success_criteria>
The data layer for per-stack color customization is complete: DEFAULTS contains stackColors, the migration version is bumped, Tip:Update() reads from config with hardcoded fallback, shouldShow no longer references cfg.enabled, and all tests pass with updated fixtures and new assertions.
</success_criteria>

## Artifacts this phase produces

**Core.lua:**
- `DEFAULTS.tip.stackColors` — table with keys [0]-[3], each `{r, g, b, a}` color entry
- `SETTINGS_MIGRATION` updated from `"0.3.2-fontfix"` to `"0.3.2-stackcolors"`
- Removal of `DEFAULTS.tip.enabled`

**TipOfTheSpear.lua:**
- Modified `shouldShow` expression (removed `cfg.enabled` gate)
- Modified number mode color read in `Tip:Update()` (config-driven via `GetCfg().stackColors`)

**spec/core_spec.lua:**
- New test: `"populates stackColors[0] through stackColors[3] from defaults"`
- New test: `"stackColors survives NormalizeDB migration"`

**spec/tip_spec.lua:**
- New test: `"reads stack color from db.tip.stackColors when set"`
- New test: `"falls back to STACK_COLORS when stackColors is nil"`

<output>
Create `.planning/phases/06-options-ui-overhaul/06-01-SUMMARY.md` when done
</output>
