---
phase: 06-options-panel-v2-per-mode-visibility-configurable-stack-colo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Options.lua
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
autonomous: true
gap_closure: true
requirements: [DISP-05, DISP-07]
must_haves:
  truths:
    - "In Number mode the panel hides Scale and Border color (text has no border and sizes via Text size)"
    - "The Enabled checkbox no longer appears in the options panel"
    - "The position 'Reset' button no longer appears; 'Reset Style' remains"
    - "Mode switching stays button-only — no /dmax mode subcommand exists"
    - "tip.enabled default remains true so the shouldShow gate is still satisfied"
  artifacts:
    - path: "Duncedmaxxing/Options.lua"
      provides: "Options window with Scale + Border color moved to bar-only group, Enabled checkbox removed, position Reset button removed"
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Module without the orphaned ResetPosition method"
  key_links:
    - from: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      to: "Duncedmaxxing/Core.lua"
      via: "shouldShow gate reads cfg.enabled — DEFAULTS.tip.enabled must stay true"
      pattern: "cfg.enabled"
---

<objective>
Close the widget-removal and per-mode-visibility gaps from Phase 6 UAT (tests 1 and 2): move Scale and Border color to the bar-only visibility group, delete the Enabled checkbox, and delete the position "Reset" button (plus its now-orphaned handler). Also record the already-resolved slash-command gap as an explicit no-op verification.

Purpose: The user asked for a tighter, mode-aware options panel — text mode has no border and no scale, the Enabled checkbox is unwanted, and the position reset control was previously requested for removal.
Output: Updated Options.lua (widget grouping + two deletions) and TipOfTheSpear.lua (orphaned method removed). No data-layer or default changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-UAT.md
@Duncedmaxxing/Options.lua
@Duncedmaxxing/Modules/TipOfTheSpear.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Scale and Border color to the bar-only visibility group</name>
  <files>Duncedmaxxing/Options.lua</files>
  <action>
The Refresh show/hide engine (Options.lua:505-514) is correct and must NOT change — it already shows the "both" group in every mode, "bar" only in bar mode, "number" only in number mode. The only defect is that Scale and Border color are registered in the wrong group.

Change the group argument for the Scale input registration (Options.lua:303) from the shared group to the bar-only group so Scale hides in Number mode. Change the group argument for the Border color input registration (Options.lua:363) from the shared group to the bar-only group so Border color hides in Number mode.

Leave all other AddToGroup calls untouched: hide-empty, the Position header, and the X/Y inputs must remain in the shared group; Width/Height/Border-size/Fill/Empty% stay bar-only; Text size/Color-by-stack/stack pickers stay number-only.

Do NOT reposition any widget in this task — final coordinate rebalancing is handled last in plan 06-06 after all removals land. This task only reassigns two group memberships.
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && node spec/run.cjs 2>/dev/null || npx -y -p fengari@0.1.5 node spec/run.cjs 2>&1 | tail -3</automated>
  </verify>
  <done>Scale and Border color are members of the bar-only group; grep confirms the shared-group registrations now number four (hide-empty, Position header, X, Y): `grep -c 'AddToGroup("both"' Duncedmaxxing/Options.lua` returns 4. The full fengari suite still reports all specs passing.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the Enabled checkbox widget (keep the DB field)</name>
  <files>Duncedmaxxing/Options.lua</files>
  <action>
Delete the Enabled checkbox widget block at Options.lua:266-269 in full: the CreateCheckbox call that binds to the enabled config field, both returned local widget handles, and the AddToGroup registration that adds those handles to the shared group. Nothing else references those two local handles, so removal is clean.

CRITICAL — do NOT touch the data layer. DEFAULTS.tip.enabled = true in Core.lua:15 MUST remain. It is read by the shouldShow gate in TipOfTheSpear.lua:595, which is the only downstream consumer. Removing the default would break the tracker visibility gate and two core_spec assertions (spec/core_spec.lua:265 and :326) that assert the field stays true after merge/normalize. This task removes the UI control only, not the setting.

Leave the "Hide empty" checkbox (Options.lua:270-273) intact.
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && grep -c 'GetCfg().enabled = value' Duncedmaxxing/Options.lua</automated>
  </verify>
  <done>The Enabled checkbox setter is gone from Options.lua (grep returns 0). The Hide-empty checkbox remains. DEFAULTS.tip.enabled = true is untouched in Core.lua (grep -c 'enabled = true' Duncedmaxxing/Core.lua returns 1 or more). The fengari suite still passes fully.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the position Reset button and its orphaned handler</name>
  <files>Duncedmaxxing/Options.lua, Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <action>
Delete the position-reset CreateButton block at Options.lua:424-429 (the button whose OnClick fetches the tip module and calls its position-reset method). Keep the "Reset Style" button block at Options.lua:430-434 exactly as-is — the user explicitly wants that one to stay.

The deleted button was the only caller of the position-reset method, so also delete the now-dead method definition at TipOfTheSpear.lua:551-558 (the function on the Tip table that resets x/y/scale to defaults and calls RefreshLayout). No spec references it (confirmed: no matches in spec/). Leave the surrounding methods (ApplyLock above it, SetTestStacks below it) untouched.

Do not reposition the remaining button row in this task — spacing is rebalanced in plan 06-06.

NO-OP VERIFICATION (gap 4, no code change): The /dmax mode bar|number slash subcommands were already removed in prior commit 53b1c42. The current handler (Core.lua:182-193) is argument-less and only opens the options window. Do NOT add or re-add any mode subcommand. This item is satisfied and is verified by the automated check below, not by an edit.
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/Duncedmaxxing && test "$(grep -c 'function Tip:ResetPosition' Duncedmaxxing/Modules/TipOfTheSpear.lua)" = "0" && test "$(grep -c 'tip:ResetPosition' Duncedmaxxing/Options.lua)" = "0" && test "$(grep -c 'Reset Style' Duncedmaxxing/Options.lua)" = "1" && test "$(grep -c 'mode' Duncedmaxxing/Core.lua)" = "0" && echo GATES_PASS</automated>
  </verify>
  <done>Position Reset button and its handler are gone (both greps return 0); "Reset Style" remains (grep returns 1); the Core.lua slash handler contains no mode subcommand token (grep returns 0); command prints GATES_PASS. The fengari suite still passes fully.</done>
</task>

</tasks>

<verification>
Run the full regression suite after all three tasks:
`cd /home/cela/code_stuff/Duncedmaxxing && node spec/run.cjs 2>/dev/null || npx -y -p fengari@0.1.5 node spec/run.cjs` — all specs must pass (the suite reported 124/124 before this plan; removing the untested ResetPosition method must not reduce that count).

Structural gates:
- `grep -c 'AddToGroup("both"' Duncedmaxxing/Options.lua` = 4
- `grep -c 'GetCfg().enabled = value' Duncedmaxxing/Options.lua` = 0
- `grep -c 'function Tip:ResetPosition' Duncedmaxxing/Modules/TipOfTheSpear.lua` = 0
- `grep -c 'Reset Style' Duncedmaxxing/Options.lua` = 1
- `grep -c 'enabled = true' Duncedmaxxing/Core.lua` >= 1 (data layer preserved)
</verification>

<success_criteria>
- Number mode hides Scale and Border color; Bar mode still shows them (per-mode visibility, DISP-05).
- The Enabled checkbox is removed from the panel; tip.enabled default preserved so the tracker still shows correctly.
- The position Reset button is removed; Reset Style remains; the orphaned ResetPosition method is deleted.
- No /dmax mode subcommand exists (gap 4 confirmed as a no-op).
- The full fengari regression suite passes.
</success_criteria>

<output>
Create `.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-04-SUMMARY.md` when done.
</output>
