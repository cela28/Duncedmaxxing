---
phase: 06-options-panel-v2-per-mode-visibility-configurable-stack-colo
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - spec/tip_spec.lua
  - spec/core_spec.lua
autonomous: true
requirements: [DISP-06]
must_haves:
  truths:
    - "Tests prove that with colorByStack ON the number render applies the config per-stack colors (defaults match today's white/green/yellow/red) (ROADMAP SC-4, SC-7)"
    - "Tests prove that with colorByStack OFF the number render applies the flat textColor fallback (ROADMAP SC-4, SC-7)"
    - "Tests prove a fresh/legacy DB default-merges colorByStack + stackColors with no wipe and no error (ROADMAP SC-6, SC-7)"
  artifacts:
    - path: "spec/tip_spec.lua"
      provides: "Config-driven stack-color + colorByStack OFF fallback assertions in the number-mode color coding describe block"
      contains: "colorByStack"
    - path: "spec/core_spec.lua"
      provides: "MergeDefaults/NormalizeDB assertions for the new colorByStack + stackColors fields (no wipe)"
      contains: "colorByStack"
  key_links:
    - from: "spec/tip_spec.lua"
      to: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      via: "Tip:Update number-mode render reads cfg.colorByStack/cfg.stackColors — asserted via numberText._textColor"
      pattern: "colorByStack"
    - from: "spec/core_spec.lua"
      to: "Duncedmaxxing/Core.lua"
      via: "DMX._test.MergeDefaults / NormalizeDB populate the new fields"
      pattern: "stackColors"
---

<objective>
Extend the fengari (Lua-VM-in-JS) spec suite with regression coverage for DISP-06: config-driven per-stack colors applied when `colorByStack` is ON, the flat `textColor` fallback applied when OFF, and default-merge of the new `colorByStack`/`stackColors` fields on fresh and legacy DBs with no settings wipe. This depends on plan 01 (the config fields + render read) but touches only spec files, so it runs in the same wave as plan 02 with no file overlap.

Purpose: Lock the DISP-06 behavior under regression so future edits cannot silently break the toggle or the no-wipe merge.
Output: New assertions in spec/tip_spec.lua and spec/core_spec.lua; the full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md
@spec/tip_spec.lua
@spec/core_spec.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add colorByStack ON/OFF number-color assertions to spec/tip_spec.lua</name>
  <files>spec/tip_spec.lua</files>
  <read_first>
    - spec/tip_spec.lua (the "Tip:Update number mode color coding" describe block at 585-649: before_each at 588-597 sets displayMode="number", the assertColor helper at 599-606, the per-stack ON tests at 608-634 asserting Tip.numberText._textColor, and the bar-mode no-color test at 636-648)
    - spec/support/init.lua and spec/support/wow_stubs.lua (loader.load / resetTipState; numberText._textColor capture — the SetTextColor stub records _textColor)
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (the plan-01 config-driven render read at ~613-625; the flat-textColor fallback path)
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md (D-02, D-03; Claude's Discretion on test file organization)
  </read_first>
  <action>
    In spec/tip_spec.lua, within the existing "Tip:Update number mode color coding" describe block (585-649), add new `it(...)` cases (reuse the existing `assertColor` helper and `before_each` setup which already sets displayMode="number", isSurvival=true, and calls RefreshLayout):
    (1) colorByStack ON explicit: set `DMX:GetDB().tip.colorByStack = true`, set Tip.stacks, call Tip:Update, and assert `Tip.numberText._textColor` equals the configured `db.tip.stackColors[N]` — verify the default config still yields white/green/yellow/red for stacks 0/1/2/3 (the existing four ON tests already cover this against defaults; add at least one case that mutates a `db.tip.stackColors[N]` entry to a distinct color and asserts the render reflects the edited value, proving config-driven behavior rather than hardcoded, per ROADMAP SC-3/SC-4).
    (2) colorByStack OFF fallback: set `DMX:GetDB().tip.colorByStack = false` and `db.tip.textColor` to a distinct RGBA (e.g. `{ r = 0.2, g = 0.4, b = 0.6, a = 1 }`), set Tip.stacks to a non-zero value (e.g. 2), call Tip:Update, and assert `Tip.numberText._textColor` equals that flat textColor for every stack value tested — NOT the per-stack color (ROADMAP SC-4, D-03). Add cases for at least stacks 1 and 3 to show the flat color is used regardless of stack count.
    Follow the existing assertColor tolerance pattern (0.001). Do not change the existing four ON tests or the bar-mode test.
  </action>
  <verify>
    <automated>node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'colorByStack' spec/tip_spec.lua` returns both an ON case and an OFF case.
    - Source: at least one ON case mutates `db.tip.stackColors[N]` and asserts the render reflects the edited color (proves config-driven, ROADMAP SC-3).
    - Source: the OFF case sets a distinct `db.tip.textColor` and asserts `numberText._textColor` matches it for multiple non-zero stack values (ROADMAP SC-4).
    - Behavior: `node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs` passes with the new cases green (ROADMAP SC-7).
  </acceptance_criteria>
  <done>tip_spec.lua asserts config-driven per-stack colors when colorByStack is ON (including an edited-color case) and the flat textColor fallback when OFF; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Add colorByStack/stackColors default-merge + no-wipe assertions to spec/core_spec.lua</name>
  <files>spec/core_spec.lua</files>
  <read_first>
    - spec/core_spec.lua (MergeDefaults describe at 8-50; NormalizeDB migration branch 52-141; already-migrated branch 143-187 with migratedDB() at 150-174 which builds a tip table WITHOUT the new fields; deprecated-fields branch at 189+)
    - Duncedmaxxing/Core.lua (DEFAULTS.tip with plan-01's colorByStack + stackColors; MergeDefaults at 48-63; NormalizeDB at 65-92; DMX._test exports at 210-213)
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md (D-11: no wipe, default-merge only, no migration bump)
  </read_first>
  <action>
    In spec/core_spec.lua, add assertions proving the new fields default-merge cleanly with no wipe (D-11, ROADMAP SC-6):
    (1) In the "MergeDefaults" describe block (8-50) or a new focused describe, add a case that merges a `DEFAULTS`-shaped table containing the nested `stackColors` sub-table into a target that lacks it, and assert the target gains `colorByStack == true` and all four `stackColors[0..3]` entries with the byte-for-byte default values. Add a second case where the target already has `colorByStack = false` and asserts MergeDefaults does NOT overwrite it (fill-missing-only semantics), and where the target has an edited `stackColors[1]` and asserts that edited entry is preserved.
    (2) Add a case simulating the legacy already-migrated DB path: build a `migratedDB()`-style table (like 150-174) whose `tip` lacks `colorByStack`/`stackColors`, run the real `ADDON_LOADED` merge sequence as the addon does — i.e. call `DMX._test.MergeDefaults(DMX._test-exposed DEFAULTS or DMX.defaults, db)` then `DMX._test.NormalizeDB(db)` — and assert: `db.tip.colorByStack == true`, `db.tip.stackColors` has four entries, existing fields (e.g. displayMode, x, y, scale) are unchanged (no wipe), `db.settingsMigration` is unchanged (no migration bump), and no error is raised. If DEFAULTS is not already exported via DMX._test, use `DMX.defaults` (set at Core.lua:34) as the merge source.
    Match the existing test style (before_each `loader.load()`, `assert.equals`, `assert.near` for color components). Do not alter existing displayMode/QUAL-03 tests.
  </action>
  <verify>
    <automated>node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'colorByStack\|stackColors' spec/core_spec.lua` returns the new merge + no-wipe cases.
    - Source: a case asserts MergeDefaults fills missing colorByStack (true) + all four stackColors defaults byte-for-byte (ROADMAP SC-6).
    - Source: a case asserts MergeDefaults does NOT overwrite a user-set colorByStack=false or an edited stackColors entry.
    - Source: a legacy-DB case runs MergeDefaults→NormalizeDB and asserts new fields present, existing fields unchanged, settingsMigration unchanged, no error (D-11, ROADMAP SC-6).
    - Behavior: `node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs` passes (ROADMAP SC-7).
  </acceptance_criteria>
  <done>core_spec.lua asserts the new fields default-merge onto fresh and legacy DBs with fill-missing-only semantics, no wipe, and no migration bump; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SavedVariables (legacy DuncedmaxxingDB) → addon load | Legacy config tables lacking the new fields must gain defaults without wiping user settings — verified here by test, not by runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | legacy-DB default-merge (regression coverage) | mitigate | New core_spec cases assert MergeDefaults→NormalizeDB preserves existing fields and settingsMigration on a legacy DB (no wipe), locking ROADMAP SC-6 under regression. |
| T-06-06 | Denial (silent behavior break) | colorByStack toggle render path | mitigate | New tip_spec cases assert both the ON (config-driven, edited-color) and OFF (flat textColor) paths, so a future edit that re-hardcodes or drops the toggle fails the suite. |
</threat_model>

<verification>
- `node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs` passes with all new cases green.
- `grep -n 'colorByStack' spec/tip_spec.lua spec/core_spec.lua` returns hits in both files.
</verification>

<success_criteria>
- tip_spec proves colorByStack ON applies config per-stack colors (incl. an edited-color case) and OFF applies flat textColor.
- core_spec proves the new fields default-merge onto fresh and legacy DBs with no wipe and no migration bump.
- The full fengari suite passes.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 03)

New spec assertions:
- spec/tip_spec.lua — colorByStack ON (config-driven, including edited-stackColors case) and OFF (flat textColor fallback) cases inside the "Tip:Update number mode color coding" describe block
- spec/core_spec.lua — MergeDefaults fill-missing + preserve-user-edit cases for colorByStack/stackColors, and a legacy-DB MergeDefaults→NormalizeDB no-wipe / no-migration-bump case

No new test files required (extends existing spec files); wow_stubs.lua unchanged (no new widget/API surface exercised — SetTextColor capture already exists).
</artifacts_produced>

<output>
Create `.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-03-SUMMARY.md` when done
</output>
