---
phase: 06-options-panel-v2-per-mode-visibility-configurable-stack-colo
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Duncedmaxxing/Options.lua
autonomous: true
requirements: [DISP-05, DISP-06, DISP-07]
must_haves:
  truths:
    - "In Bar mode the panel shows Width/Height/Border size/Fill/Empty % and hides Text size and all stack-color controls; in Number mode it shows Text size + stack-color controls and hides Bar-only controls; Position/Enabled/Hide empty/Border color show in both (ROADMAP SC-1)"
    - "Switching modes via the Bar/Number buttons or via /dmax mode ... updates widget visibility immediately with no Lua error and no combat-lockdown violation (ROADMAP SC-2)"
    - "A 'Color by stack' toggle plus 4 per-stack color inputs plus the flat Text picker exist; within Number mode all are always present and the toggle greys the inactive group (ROADMAP SC-3, D-08)"
    - "The 'Display:' text label is removed and the active mode button is visually highlighted, so no label overlaps the Bar button in either mode (ROADMAP SC-5, D-09); the window stays a fixed size (D-10)"
  artifacts:
    - path: "Duncedmaxxing/Options.lua"
      provides: "Per-mode widget visibility gating, colorByStack toggle + 4 stack color inputs + flat Text picker greying, mode-button highlight replacing the Display label"
      contains: "colorByStack"
  key_links:
    - from: "Duncedmaxxing/Options.lua"
      to: "Duncedmaxxing/Core.lua"
      via: "toggle/pickers read+write db.tip.colorByStack and db.tip.stackColors via GetCfg()"
      pattern: "colorByStack"
    - from: "Duncedmaxxing/Options.lua Options:SetMode"
      to: "Duncedmaxxing/Options.lua Options:Refresh"
      via: "SetMode triggers a panel refresh so slash-driven and button-driven mode changes both update visibility + highlight"
      pattern: "Refresh"
---

<objective>
Rework the options window (Options.lua) for DISP-05, DISP-06 (UI half), and DISP-07. Gate every widget's visibility by the active display mode; add the "Color by stack" toggle plus 4 per-stack color inputs and make the flat Text picker + stack pickers coexist with toggle-driven greying (D-08); remove the "Display:" label and highlight the active Bar/Number button instead (D-09); keep the window a fixed size (D-10). All Options.lua edits live in this single plan/wave to avoid file conflicts.

Purpose: This is the entire user-facing surface of the phase. It depends on plan 01 having added `db.tip.colorByStack` and `db.tip.stackColors`.
Output: A mode-aware options panel with configurable per-stack colors and a fixed layout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md
@Duncedmaxxing/Options.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add colorByStack toggle, 4 per-stack color inputs, and register widgets for mode-gating</name>
  <files>Duncedmaxxing/Options.lua</files>
  <read_first>
    - Duncedmaxxing/Options.lua (CreateInput at 104-139; CreateCheckbox at 78-102; ColorToHex at 24-36; ParseHexColor via DMX.Util.ParseHexColor at line 7; GetCfg at 38-41; existing color inputs at 320-352; existing Text picker at 337-344; existing "Other Modes"/Text size at 310-318; Options.checkboxes/Options.inputs registration at 98-101 and 135-138)
    - Duncedmaxxing/Core.lua (the new DEFAULTS.tip.colorByStack and DEFAULTS.tip.stackColors added by plan 01)
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md (D-06, D-07, D-08; Claude's Discretion on picker labeling and greying technique)
    - CLAUDE.md (combat safety: all mutations gated by Options:CanChange())
  </read_first>
  <action>
    In `Options:BuildWindow` (Duncedmaxxing/Options.lua), add the Number-mode color controls, reusing the existing widget factories verbatim:
    (1) A "Color by stack" checkbox via `CreateCheckbox` whose get/set read+write `GetCfg().colorByStack` (same closure pattern as the Enabled/Hide empty checkboxes at 251-256). CreateCheckbox already registers into `Options.checkboxes` and calls `RefreshTracker()` + `Options:Refresh()` on click, so toggling re-drives greying and the live number color.
    (2) Four per-stack color inputs via `CreateInput` (same pattern as the Fill/Border/Text hex inputs at 321-344), one per stack 0..3, labeled to make the stack count clear (e.g. "0 stacks".."3 stacks" or "Stack 0".."Stack 3"). Each input's get returns `ColorToHex(GetCfg().stackColors[N])` and its set does `local color = ParseHexColor(value); if not color then return false end; GetCfg().stackColors[N] = color; return true` (mirroring 321-328). Guard for a nil `stackColors` sub-table defensively (it is populated by plan 01's MergeDefaults, but read through GetCfg() which may be nil pre-init — reuse the existing `GetCfg()` nil-safety already implied by other setters).
    (3) Keep the existing flat "Text" color picker (337-344) — per D-07 it is a Number-only control and per D-08 it stays present alongside the stack pickers.
    To support per-mode gating in Task 2, make every widget factory return (or otherwise expose) its created frame so widgets can be tagged with a mode group. Concretely: extend `CreateInput`, `CreateCheckbox`, and `CreateText`/`CreateButton` call sites so each created widget (and its label) is captured into a new `Options.widgetGroups` structure keyed by mode-group ("both" | "bar" | "number"). Prefer capturing the frames returned/created by the factories (CreateInput currently returns nil — have it return the editBox and its label FontString; CreateCheckbox currently returns nil — have it return the check and its label; CreateText already returns the FontString; CreateButton already returns the button). Do NOT change the click/apply/combat-guard behavior of any factory. Assign each BuildWindow widget to its group per D-04/D-05/D-06: "both" = Position X/Y/Scale + Enabled + Hide empty + Border color + the "Position" section headers shared by both; "bar" = Width + Height + Border size + Fill + Empty % + the "Bar" section header; "number" = Text size + the "Color by stack" toggle + the 4 stack pickers + the flat Text picker + their section header. Leave the always-visible chrome (title, X button, Unlock/Lock/Preview/Reset/Reset Style buttons) ungrouped (always shown).
    Do NOT resize the window (`SetSize(386, 484)` at 188 stays fixed, D-10). Do NOT place fenced code in this file's edits beyond the Lua source itself.
  </action>
  <verify>
    <automated>node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'colorByStack' Duncedmaxxing/Options.lua` returns the checkbox get/set.
    - Source: four `CreateInput` calls read/write `GetCfg().stackColors[0..3]` through `ColorToHex`/`ParseHexColor` (ROADMAP SC-3).
    - Source: the flat Text picker (reads `GetCfg().textColor`) is still present (D-07/D-08).
    - Source: a `Options.widgetGroups` (or equivalently named) structure tags widgets as "both"/"bar"/"number"; every BuildWindow-created widget is assigned a group per D-04/D-05/D-06.
    - Source: `window:SetSize(386, 484)` is unchanged (fixed size, D-10).
    - Behavior: the fengari suite passes (no Lua parse/load errors introduced).
  </acceptance_criteria>
  <done>The toggle + 4 stack pickers + flat Text picker exist and read/write config through GetCfg(); every widget is tagged into a mode group for gating; window size is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Per-mode visibility gating, toggle-driven greying, and mode-button highlight in Options:Refresh</name>
  <files>Duncedmaxxing/Options.lua</files>
  <read_first>
    - Duncedmaxxing/Options.lua (Options:Refresh at 385-400; the modeText creation at 247 and the Bar/Number buttons at 248-249; Options:SetMode at 171-177; MODE_LABELS at 10-13; Options:CanChange at 141-148; CreateButton onClick already calls Options:Refresh at 73)
    - .planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-CONTEXT.md (D-04..D-10; integration point: SetMode is called by both mode buttons AND the /dmax mode ... slash path — verify the slash path re-refreshes the panel when open)
    - Duncedmaxxing/Core.lua (slash handler that calls Options:SetMode for /dmax mode ...)
    - CLAUDE.md (combat safety: visibility/highlight must run on the out-of-combat Refresh/SetMode path, never during InCombatLockdown UI mutation)
  </read_first>
  <action>
    Rework `Options:Refresh` (385-400) and `Options:SetMode` (171-177) in Duncedmaxxing/Options.lua:
    (1) Remove the "Display: X" label. Delete the `self.modeText = CreateText(window, "Display: Bar", 16, -48, ...)` line at 247 (the label colliding with the Bar button at x=108, DISP-07/D-09) and delete the `if self.modeText then ... modeText:SetText(...) end` block at 389-391. Keep the Bar button (108) and Number button (244); optionally shift the Bar button leftward now that no label occupies x=16 — but do NOT resize the window (D-10). Capture the two mode buttons as `Options.modeButtons.bar` / `.number` (or equivalent) so Refresh can highlight the active one.
    (2) In `Options:Refresh`, after the existing checkbox/input sync loops, drive per-mode visibility from `cfg.displayMode`: for each widget in the "both" group call `:Show()`; for the "bar" group `:Show()` iff mode == "bar" else `:Hide()`; for the "number" group `:Show()` iff mode == "number" else `:Hide()`. Show/Hide the widget AND its captured label together. Hidden controls leave empty space — no reflow, no resize (D-10).
    (3) Active-button highlight (D-09): highlight whichever of the Bar/Number buttons matches `cfg.displayMode` (e.g. via `LockHighlight()`/`UnlockHighlight()` on the UIPanelButtonTemplate buttons, or `SetButtonState("PUSHED", true)`, or a color/alpha treatment — pick a combat-safe, clearly-visible technique). Unhighlight the inactive one. This replaces the removed modeText.
    (4) Toggle-driven greying within Number mode (D-08): when mode == "number", if `cfg.colorByStack` is true → the 4 stack pickers are the active group and the flat Text picker renders greyed/disabled; if false → the flat Text picker is active and the 4 stack pickers render greyed/disabled. Both groups stay shown; only the enabled/greyed state swaps. Use a combat-safe technique (EditBox `:Disable()` + dimmed text, or reduced alpha on the input + label). Do NOT show/hide-swap between the two groups.
    (5) Ensure slash-driven mode changes refresh the panel: `Options:SetMode` must trigger a panel refresh when the window is open, so `/dmax mode bar|number` updates visibility + highlight (ROADMAP SC-2). Add a call so SetMode invokes `Options:Refresh()` (guarded by `self.window and self.window:IsShown()` if needed) after setting `cfg.displayMode`. The mode buttons already route through CreateButton's onClick which calls Refresh (73), so this closes the slash-path gap. All of this runs on the out-of-combat path; do not add any mutation that runs during InCombatLockdown.
  </action>
  <verify>
    <automated>node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'modeText\|Display: ' Duncedmaxxing/Options.lua` returns no hits — the label is fully removed (ROADMAP SC-5, D-09).
    - Source: Options:Refresh gates each mode group with Show/Hide keyed on `cfg.displayMode` (ROADMAP SC-1): "both" always shown, "bar" only in bar mode, "number" only in number mode.
    - Source: Options:Refresh highlights the active mode button and unhighlights the other (D-09).
    - Source: within number mode, `cfg.colorByStack` drives which of {flat Text picker} / {4 stack pickers} is greyed/disabled while both remain shown (D-08).
    - Source: Options:SetMode calls Options:Refresh (guarded by window-shown) so the /dmax mode ... slash path updates the panel (ROADMAP SC-2); no UI mutation runs during InCombatLockdown (all changes on the Refresh/SetMode out-of-combat path).
    - Source: `window:SetSize(386, 484)` unchanged (D-10).
    - Behavior: the fengari suite passes.
  </acceptance_criteria>
  <done>The Display label is gone and the active mode button is highlighted; per-mode visibility gating and toggle-driven greying run from Options:Refresh; both button and slash mode switches refresh the panel; window size is fixed; no combat-lockdown mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player input (slash tokens + hex color EditBox text) → db.tip | Player-controlled config values entered via the options UI or /dmax slash path |
| Options UI mutation ↔ combat lockdown | UI Show/Hide/Disable/highlight must not run while InCombatLockdown() is true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial (taint/error) | visibility + greying + highlight in Options:Refresh/SetMode | mitigate | All visibility/highlight/greying runs on the out-of-combat Refresh/SetMode path; existing Options:CanChange()/InCombatLockdown() guards on every setter are preserved — no UI mutation during combat lockdown (ROADMAP SC-2). |
| T-06-04 | Tampering | hex color EditBox input → GetCfg().stackColors[N] | mitigate | Each stack picker validates input through ParseHexColor and returns false on malformed hex (reusing the existing Fill/Border/Text pattern), so bad input is rejected without writing garbage. |
</threat_model>

<verification>
- `node spec/run.cjs || npx -y -p fengari@0.1.5 node spec/run.cjs` passes.
- `grep -n 'modeText\|Display: ' Duncedmaxxing/Options.lua` returns no hits.
- `grep -n 'colorByStack\|stackColors' Duncedmaxxing/Options.lua` returns the toggle + picker wiring.
- Manual in-game check (executor's human-verify note): switching Bar/Number via buttons and via `/dmax mode ...` with the window open toggles the correct control set, highlights the active button, and greys the inactive color group — no Lua error, no window resize.
</verification>

<success_criteria>
- Bar mode shows Width/Height/Border size/Fill/Empty %; Number mode shows Text size + toggle + 4 stack pickers + flat Text; Position/Enabled/Hide empty/Border color show in both.
- Mode switch (buttons or slash) updates visibility + highlight immediately, no error, no combat violation.
- Toggle greys the inactive color group within Number mode; both groups stay present.
- Display label removed; active mode button highlighted; window fixed size.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 02)

New symbols in Options.lua:
- "Color by stack" checkbox (get/set on `GetCfg().colorByStack`)
- Four per-stack color inputs (get/set on `GetCfg().stackColors[0..3]` via `ColorToHex`/`ParseHexColor`)
- `Options.widgetGroups` (or equivalent) — mode-group tagging structure ("both"/"bar"/"number")
- `Options.modeButtons.bar` / `Options.modeButtons.number` — captured mode-button references for highlight
- Per-mode visibility + toggle-greying + active-button-highlight logic added to `Options:Refresh`
- `Options:SetMode` now triggers a panel refresh (closes the /dmax mode slash-path gap)

Modified factory returns: `CreateInput` and `CreateCheckbox` now return their created frame(s) so widgets can be grouped.

Removed: `self.modeText` "Display: X" label (creation at line 247 and update block at 389-391).
</artifacts_produced>

<output>
Create `.planning/phases/06-options-panel-v2-per-mode-visibility-configurable-stack-colo/06-02-SUMMARY.md` when done
</output>
