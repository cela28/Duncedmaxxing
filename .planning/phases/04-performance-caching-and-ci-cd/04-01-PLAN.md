---
phase: 04-performance-caching-and-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
  - spec/tip_spec.lua
  - spec/support/init.lua
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Update() body contains no calls to RefreshActive() or IsSurvivalHunter()"
    - "RefreshLayout() and Update() contain no calls to ResolveSpellTexture()"
    - "Tip.isSurvival is true after Initialize when spec stub returns 3 (Survival)"
    - "Tip.isSurvival changes to false on PLAYER_SPECIALIZATION_CHANGED when stub returns non-Survival spec"
    - "Tip.spellTexture is non-nil after Initialize"
    - "All existing 102 tests continue to pass"
  artifacts:
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Spec state cache (Tip.isSurvival) and texture cache (Tip.spellTexture)"
      contains: "self.isSurvival"
    - path: "spec/tip_spec.lua"
      provides: "Caching regression tests"
      contains: "isSurvival"
    - path: "spec/support/init.lua"
      provides: "Extended resetTipState with cache field resets"
      contains: "Tip.isSurvival"
  key_links:
    - from: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      to: "Duncedmaxxing/Core.lua"
      via: "DMX:IsSurvivalHunter() call in Initialize and OnEvent"
      pattern: "DMX:IsSurvivalHunter"
    - from: "spec/tip_spec.lua"
      to: "spec/support/init.lua"
      via: "loader.resetTipState in before_each"
      pattern: "loader\\.resetTipState"
---

<objective>
Cache spec state and spell texture on the Tip table so Update() and RefreshLayout() never make WoW API calls during combat, and add regression tests proving the caches work correctly.

Purpose: Eliminate per-frame IsSurvivalHunter() and ResolveSpellTexture() calls during the combat hot path. These are the two performance bottlenecks identified in CONCERNS.md.
Output: Modified TipOfTheSpear.lua with event-driven caches, extended tip_spec.lua with caching tests, extended init.lua with cache field resets.
</objective>

## Phase Goal

**As a** Survival Hunter player, **I want** the addon to cache spec and texture state instead of making per-frame API calls, **so that** combat performance is optimal and I can install pre-packaged releases from GitHub.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-caching-and-ci-cd/04-CONTEXT.md
@.planning/phases/04-performance-caching-and-ci-cd/04-RESEARCH.md
@.planning/phases/04-performance-caching-and-ci-cd/04-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Duncedmaxxing/Core.lua (line 142):
function DMX:IsSurvivalHunter()
    -- Returns boolean: true if player is Survival Hunter (spec index 3)
    -- Uses dual-path: C_SpecializationInfo.GetSpecialization / GetSpecialization fallback

From Duncedmaxxing/Modules/TipOfTheSpear.lua:
-- Tip field declarations (lines 45-55):
Tip.stacks = 0
Tip.active = false
Tip.inCombat = false
Tip.testMode = false
Tip.testStacks = MAX_STACKS
Tip.expiresAt = 0
Tip.castVerifySerial = 0
Tip.auraVerifyPending = false
Tip.lastPredictAt = 0
Tip.lastPredictKind = nil
Tip.hasTwinFangs = false

-- RefreshActive (line 334-336):
function Tip:RefreshActive()
    self.active = DMX:IsSurvivalHunter()
end

-- ResolveSpellTexture (lines 137-147):
local function ResolveSpellTexture()
    if C_Spell and C_Spell.GetSpellTexture then
        return C_Spell.GetSpellTexture(TIP_OF_THE_SPEAR) or FALLBACK_ICON
    end
    if _G.GetSpellTexture then
        return _G.GetSpellTexture(TIP_OF_THE_SPEAR) or FALLBACK_ICON
    end
    return FALLBACK_ICON
end

-- Update hot-path (lines 598, 605):
self:RefreshActive()
local shouldShow = unlocked or self.testMode or (cfg.enabled and self.active)

-- ResolveSpellTexture call sites:
-- Line 493 (RefreshLayout icon mode): pip.fill:SetTexture(ResolveSpellTexture())
-- Line 651 (Update icon mode):        pip.fill:SetTexture(ResolveSpellTexture())

-- self.active read sites:
-- Line 605 (Update): cfg.enabled and self.active
-- Line 754 (OnEvent UNIT_SPELLCAST_SUCCEEDED): if not self.active then
-- Line 757 (OnEvent UNIT_SPELLCAST_SUCCEEDED): if not self.active then

-- RefreshActive call sites:
-- Line 598 (Update): self:RefreshActive()
-- Line 731 (OnEvent PLAYER_LOGIN): self:RefreshActive()
-- Line 740 (OnEvent PLAYER_SPECIALIZATION_CHANGED): self:RefreshActive()
-- Line 747 (OnEvent PLAYER_TALENT_UPDATE): self:RefreshActive()
-- Line 755 (OnEvent UNIT_SPELLCAST_SUCCEEDED): self:RefreshActive()

From spec/support/init.lua (lines 52-67):
local function resetTipState(Tip, clock)
    Tip.stacks            = 0
    Tip.expiresAt         = 0
    Tip.lastPredictAt     = 0
    Tip.lastPredictKind   = nil
    Tip.castVerifySerial  = 0
    Tip.auraVerifyPending = false
    Tip.expireTimer       = nil
    Tip.testMode          = false
    Tip.hasTwinFangs      = false
    clock:reset()
    clock.now = 100
    stubs.mockAura.impl = function(_spellID) return nil end
end

From spec/support/wow_stubs.lua:
-- C_SpecializationInfo stub returns spec 3 (Survival) by default
-- C_Spell.GetSpellTexture stub returns 132275 (FALLBACK_ICON) by default
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement spec cache (PERF-01) and texture cache (PERF-02) in TipOfTheSpear.lua</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (full file — 796 lines; understand all self.active, RefreshActive, ResolveSpellTexture locations)
    - Duncedmaxxing/Core.lua lines 142-155 (IsSurvivalHunter implementation — this is the function whose result gets cached)
    - .planning/phases/04-performance-caching-and-ci-cd/04-PATTERNS.md (exact analog patterns for field declaration, Initialize, OnEvent, Update)
  </read_first>
  <action>
    PERF-01 — Spec state cache (per D-01, D-02, D-03):

    1. Add `Tip.isSurvival = false` to the field declaration block (after `Tip.hasTwinFangs = false`, around line 55).

    2. Update `Tip:RefreshActive()` (line 334) to write `self.isSurvival` instead of `self.active`. The method stays as a convenience wrapper:
       `self.isSurvival = DMX:IsSurvivalHunter()`

    3. In `Tip:Initialize()` (around line 778): add `self.isSurvival = DMX:IsSurvivalHunter()` after `self.hasTwinFangs = HasTwinFangs()`. Alternatively, call `self:RefreshActive()` since it now writes `isSurvival`.

    4. In `Tip:Update()` (line 598): DELETE the `self:RefreshActive()` call entirely (per D-02 — OnEvent is the sole refresh path).

    5. In `Tip:Update()` (line 605): replace `self.active` with `self.isSurvival` in the shouldShow expression.

    6. In `Tip:OnEvent` PLAYER_LOGIN handler (line 731): keep `self:RefreshActive()` — this populates the cache on login.

    7. In `Tip:OnEvent` PLAYER_SPECIALIZATION_CHANGED handler (line 740): keep `self:RefreshActive()` — cache refresh on spec change. Add `self.isSurvival = DMX:IsSurvivalHunter()` is already handled by RefreshActive writing to isSurvival.

    8. In `Tip:OnEvent` PLAYER_TALENT_UPDATE handler (line 747): keep `self:RefreshActive()` — cache refresh on talent change.

    9. In `Tip:OnEvent` UNIT_SPELLCAST_SUCCEEDED handler (lines 754-757): replace BOTH `self.active` reads with `self.isSurvival`. Remove the lazy `self:RefreshActive()` call on line 755 — cache is always fresh from events now.

    PERF-02 — Texture cache (per D-04, D-05):

    10. Add `Tip.spellTexture = nil` to the field declaration block (after `Tip.isSurvival`).

    11. Replace `ResolveSpellTexture()` local function (lines 137-147) with a `CacheSpellTexture(tip)` local function that writes to `tip.spellTexture`. Preserve the dual-path API call pattern (C_Spell.GetSpellTexture first, _G.GetSpellTexture fallback, FALLBACK_ICON final). The result MUST always be non-nil (use `tex or FALLBACK_ICON`). Only the first return value of C_Spell.GetSpellTexture is captured (Lua discards second automatically in single-assignment context).

    12. In `Tip:Initialize()`: add `CacheSpellTexture(self)` after the isSurvival cache population.

    13. In `Tip:OnEvent` PLAYER_LOGIN handler (line 730-734): add `CacheSpellTexture(self)` as the first line of the branch (before RefreshActive), so texture is refreshed on login.

    14. In `Tip:RefreshLayout()` icon mode (line 493): replace `pip.fill:SetTexture(ResolveSpellTexture())` with `pip.fill:SetTexture(self.spellTexture)`.

    15. In `Tip:Update()` icon mode (line 651): replace `pip.fill:SetTexture(ResolveSpellTexture())` with `pip.fill:SetTexture(self.spellTexture)`.

    16. After both call sites are replaced, the old `ResolveSpellTexture` function definition should no longer exist — it was replaced by `CacheSpellTexture` in step 11.

    IMPORTANT — all 4 occurrences of `self.active` in TipOfTheSpear.lua MUST be renamed atomically to `self.isSurvival`:
    - Line 46 (field declaration `Tip.active = false` becomes `Tip.isSurvival = false` — or add alongside and remove active)
    - Line 335 (RefreshActive write)
    - Line 605 (Update read)
    - Lines 754, 757 (OnEvent UNIT_SPELLCAST_SUCCEEDED reads)
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing && busted spec/ && grep -c "RefreshActive\|IsSurvivalHunter" Duncedmaxxing/Modules/TipOfTheSpear.lua && grep -c "ResolveSpellTexture" Duncedmaxxing/Modules/TipOfTheSpear.lua && grep -c "self\.active" Duncedmaxxing/Modules/TipOfTheSpear.lua</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/ passes with 102 successes / 0 failures
    - grep for "RefreshActive" in TipOfTheSpear.lua returns hits ONLY in: the method definition (Tip:RefreshActive), and OnEvent handlers (PLAYER_LOGIN, PLAYER_SPECIALIZATION_CHANGED, PLAYER_TALENT_UPDATE) — NOT in Update()
    - grep for "IsSurvivalHunter" in TipOfTheSpear.lua returns hits ONLY in RefreshActive body — NOT in Update()
    - grep for "ResolveSpellTexture" in TipOfTheSpear.lua returns 0 hits (function removed, call sites replaced)
    - grep for "self\.active" in TipOfTheSpear.lua returns 0 hits (all renamed to self.isSurvival)
    - grep for "self\.isSurvival" in TipOfTheSpear.lua returns >= 4 hits (declaration, RefreshActive write, Update read, OnEvent reads)
    - grep for "self\.spellTexture" in TipOfTheSpear.lua returns >= 3 hits (declaration, RefreshLayout read, Update read)
    - grep for "CacheSpellTexture" in TipOfTheSpear.lua returns >= 3 hits (definition, Initialize call, PLAYER_LOGIN call)
  </acceptance_criteria>
  <done>Update() and RefreshLayout() read cached fields only — no API calls in hot path. All existing tests pass without modification.</done>
</task>

<task type="auto">
  <name>Task 2: Add caching regression tests and extend resetTipState</name>
  <files>spec/tip_spec.lua, spec/support/init.lua</files>
  <read_first>
    - spec/tip_spec.lua (full file — 485 lines; understand existing describe block patterns, before_each isolation, assertion styles)
    - spec/support/init.lua (full file — 72 lines; understand resetTipState field list and loader.load bootstrap)
    - spec/support/wow_stubs.lua lines 1-50 (understand C_SpecializationInfo stub default return and C_Spell stub)
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (post-Task-1 state — verify new field names and CacheSpellTexture function)
  </read_first>
  <action>
    Extend resetTipState in spec/support/init.lua (per D-12, D-13):

    1. Add `Tip.isSurvival = false` after `Tip.hasTwinFangs = false` in the resetTipState function.
    2. Add `Tip.spellTexture = nil` after `Tip.isSurvival = false`.

    Add new describe block in spec/tip_spec.lua (per D-12, D-13):

    3. Create `describe("Caching -- isSurvival and spellTexture", function() ... end)` following the existing `before_each` isolation pattern: `local DMX, Tip, clock` declared at describe scope, `before_each` calls `loader.load()` and `loader.resetTipState(Tip, clock)`.

    4. Add test: "Tip.isSurvival is true after Initialize (Survival Hunter stub)" — the default C_SpecializationInfo.GetSpecialization stub returns 3 (Survival), so after loader.load() which calls Initialize, Tip.isSurvival should be true. Assert: `assert.is_true(Tip.isSurvival)`.

    5. Add test: "Tip.isSurvival is false after PLAYER_SPECIALIZATION_CHANGED with non-Survival spec" — override `_G.C_SpecializationInfo.GetSpecialization` to return 1 (Beast Mastery), fire `Tip:OnEvent("PLAYER_SPECIALIZATION_CHANGED", "player")`, assert `assert.is_false(Tip.isSurvival)`.

    6. Add test: "Tip.isSurvival stays unchanged on PLAYER_SPECIALIZATION_CHANGED for non-player unit" — fire `Tip:OnEvent("PLAYER_SPECIALIZATION_CHANGED", "target")`, assert `assert.is_true(Tip.isSurvival)` (unchanged from Initialize default).

    7. Add test: "Tip.spellTexture is non-nil after Initialize" — assert `assert.is_not_nil(Tip.spellTexture)`. The C_Spell.GetSpellTexture stub returns 132275.

    8. Add test: "Tip.spellTexture equals expected icon ID after Initialize" — assert `assert.equals(132275, Tip.spellTexture)` (value from C_Spell stub).

    9. Add test: "Tip.isSurvival refreshes on PLAYER_TALENT_UPDATE" — override spec stub to return 1, fire `Tip:OnEvent("PLAYER_TALENT_UPDATE")`, assert `assert.is_false(Tip.isSurvival)`. Then restore stub to return 3, fire again, assert `assert.is_true(Tip.isSurvival)`.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing && busted spec/tip_spec.lua -v 2>&1 | tail -20 && busted spec/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - busted spec/ reports >= 108 successes / 0 failures / 0 errors (102 existing + 6 new caching tests)
    - spec/tip_spec.lua contains a describe block with "isSurvival" in its name
    - spec/support/init.lua resetTipState function contains `Tip.isSurvival = false` and `Tip.spellTexture = nil`
    - grep -c "isSurvival" spec/tip_spec.lua returns >= 6
    - grep -c "spellTexture" spec/tip_spec.lua returns >= 2
  </acceptance_criteria>
  <done>Six new caching regression tests pass. resetTipState resets both cache fields for per-test isolation. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW API to addon | C_SpecializationInfo and C_Spell return values are trusted (same-process sandbox) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Tip.isSurvival cache | accept | Field is a boolean on a local table; WoW sandbox prevents cross-addon mutation. Incorrect cache state causes wrong visibility, not a security issue. |
| T-04-02 | Denial of Service | CacheSpellTexture nil return | mitigate | CacheSpellTexture always falls through to FALLBACK_ICON (132275) — nil never reaches SetTexture. Pattern: `tip.spellTexture = tex or FALLBACK_ICON` |
| T-04-03 | Information Disclosure | isSurvival / spellTexture fields | accept | Fields contain non-sensitive game state (spec index, texture ID). No PII. Accessible only within WoW addon sandbox. |
</threat_model>

<verification>
1. `busted spec/` — full suite green with >= 108 tests
2. `grep -c "self:RefreshActive" Duncedmaxxing/Modules/TipOfTheSpear.lua` — returns count showing NO hit inside Update function
3. `grep -n "RefreshActive" Duncedmaxxing/Modules/TipOfTheSpear.lua` — verify only in method def and OnEvent handlers
4. `grep -c "ResolveSpellTexture" Duncedmaxxing/Modules/TipOfTheSpear.lua` — returns 0
5. `grep -c "self\.active" Duncedmaxxing/Modules/TipOfTheSpear.lua` — returns 0
</verification>

<success_criteria>
- Update() contains zero API calls for spec detection or texture resolution
- RefreshLayout() contains zero API calls for texture resolution
- Tip.isSurvival is populated at Initialize and refreshed on PLAYER_SPECIALIZATION_CHANGED and PLAYER_TALENT_UPDATE
- Tip.spellTexture is populated at Initialize and refreshed on PLAYER_LOGIN
- Six new tests validate caching behavior
- All existing 102 tests pass unchanged
</success_criteria>

<output>
Create `.planning/phases/04-performance-caching-and-ci-cd/04-01-SUMMARY.md` when done
</output>
