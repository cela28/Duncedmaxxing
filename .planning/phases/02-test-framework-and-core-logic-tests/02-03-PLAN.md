---
phase: 02-test-framework-and-core-logic-tests
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - spec/tip_spec.lua
  - .luacheckrc
autonomous: true
requirements: [TEST-03, TEST-04, TEST-07]
must_haves:
  truths:
    - "ApplySpell generator adds 2 stacks capped at MAX_STACKS (3)"
    - "ApplySpell consumer decrements stacks by 1 and clears expiresAt at 0"
    - "ApplySpell generator sets expiresAt to GetTime() + BUFF_DURATION (10)"
    - "Expiry timer fires after BUFF_DURATION and zeroes stacks"
    - "SyncFromAura suppresses consumer up-sync within CONSUMER_UPSYNC_GRACE (2.75) window"
    - "SyncFromAura returns false when ReadLiveState returns nil"
    - "ScheduleCastVerify serial-mismatch causes early return from verify callback"
    - "luacheck reports zero warnings on all addon source files"
    - "Twin Fangs test exists as pending (BUG-04 not yet implemented)"
  artifacts:
    - path: "spec/tip_spec.lua"
      provides: "Unit tests for ApplySpell, SyncFromAura, ScheduleExpiration, ScheduleCastVerify"
      min_lines: 120
    - path: ".luacheckrc"
      provides: "luacheck configuration with std=lua51 and curated WoW globals"
      contains: "std"
  key_links:
    - from: "spec/tip_spec.lua"
      to: "spec/support/init.lua"
      via: "require('spec.support.init')"
      pattern: "require.*spec%.support%.init"
    - from: "spec/tip_spec.lua"
      to: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      via: "Tip:ApplySpell and Tip:SyncFromAura methods"
      pattern: "Tip:ApplySpell"
    - from: ".luacheckrc"
      to: "Duncedmaxxing/*.lua"
      via: "luacheck static analysis target"
      pattern: "std.*lua51"
---

## Phase Goal

**As a** WoW addon developer, **I want to** run offline unit tests against all pure-logic functions, **so that** bugs get regression coverage without launching the WoW client.

<objective>
Write comprehensive tests for the Tip tracking logic (ApplySpell, SyncFromAura, ScheduleExpiration, ScheduleCastVerify) using the mock clock for timer simulation, configure luacheck for zero-warning static analysis, and run the complete phase verification.

Purpose: The tracking logic (stack application, aura reconciliation, timer scheduling) is the addon's core value. These tests create the regression safety net that Phase 3 bug fixes will rely on. luacheck provides a permanent zero-warning lint baseline.

Output: `busted spec/` passes with all tests green. `luacheck Duncedmaxxing/` reports zero warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-CONTEXT.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-RESEARCH.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-PATTERNS.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-01-SUMMARY.md
@.planning/phases/02-test-framework-and-core-logic-tests/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from TipOfTheSpear.lua for test targets. -->

From Duncedmaxxing/Modules/TipOfTheSpear.lua:

  Constants:
    BUFF_DURATION = 10
    MAX_STACKS = 3
    CONSUMER_UPSYNC_GRACE = 2.75
    AURA_VERIFY_DELAY = 1.25
    FINAL_AURA_VERIFY_DELAY = 2.05
    KILL_COMMAND = 259489
    TIP_OF_THE_SPEAR = 260286
    CONSUMERS = { [1261193]=true, [1250646]=true, [259495]=true, [186270]=true, [1262293]=true }

  Runtime state fields on Tip:
    Tip.stacks (number, 0-3)
    Tip.expiresAt (number, epoch timestamp or 0)
    Tip.castVerifySerial (number, incremented per ScheduleCastVerify)
    Tip.auraVerifyPending (boolean)
    Tip.lastPredictAt (number, GetTime() value)
    Tip.lastPredictKind (string "generator"/"consumer" or nil)
    Tip.inCombat (boolean)
    Tip.testMode (boolean)
    Tip.expireTimer (timer handle or nil)

  Tip:ApplySpell(kind) — line 675:
    generator: stacks = ClampStacks(stacks + 2), expiresAt = now + BUFF_DURATION
    consumer: stacks = ClampStacks(stacks - 1), expiresAt = 0 if stacks == 0
    Both: lastPredictAt = now, lastPredictKind = kind, ScheduleExpiration(), Update(), ScheduleCastVerify()

  Tip:SyncFromAura() — line 323:
    Calls ReadLiveState(). Returns false if nil.
    Consumer grace suppression: inCombat AND lastPredictKind=="consumer" AND liveStacks > stacks AND GetTime() < lastPredictAt + CONSUMER_UPSYNC_GRACE
    On accept: syncs stacks and expiresAt from live aura, calls ScheduleExpiration()

  Tip:ScheduleExpiration() — line 350:
    Cancels existing expireTimer. If stacks <= 0 or no expiresAt, returns.
    remaining = expiresAt - GetTime(). If <= 0, zeros stacks immediately.
    Otherwise: C_Timer.NewTimer(remaining + 0.03, callback). Callback checks expiresAt boundary and either zeros stacks or reschedules.

  Tip:ScheduleCastVerify() — line 383:
    Increments castVerifySerial. Captures serial. Defines Verify() closure that checks serial == self.castVerifySerial (early return on mismatch). Schedules Verify at AURA_VERIFY_DELAY (1.25) and FINAL_AURA_VERIFY_DELAY (2.05).

From spec/support/init.lua (Plan 01 output):
  loader.load() returns DMX, Tip, mockClock
  loader.resetTipState(Tip, clock) — zeros all runtime fields, sets clock.now = 100

From spec/support/wow_stubs.lua (Plan 01 output):
  makeAuraData(overrides) — returns full AuraData struct
  mockClock:advance(dt) — advances time and auto-fires ready timers
  mockClock:reset() — zeros time and clears timers
  C_UnitAuras.GetPlayerAuraBySpellID overrideable per-test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ApplySpell, SyncFromAura, and timer tests</name>
  <files>spec/tip_spec.lua</files>
  <read_first>
    Duncedmaxxing/Modules/TipOfTheSpear.lua
    spec/support/init.lua
    spec/support/wow_stubs.lua
  </read_first>
  <action>
    Create `spec/tip_spec.lua` with three main `describe` blocks. All blocks use `loader.load()` and `loader.resetTipState(Tip, clock)` in `before_each` (D-06 isolation, clock.now starts at 100 per Pitfall 5).

    Also require `spec.support.wow_stubs` at the top of the file to access `makeAuraData` for SyncFromAura tests.

    **Tip:ApplySpell describe block** (TEST-03, at least 8 cases):
    - Adds 2 stacks on generator: `Tip:ApplySpell("generator")` then `Tip.stacks == 2`
    - Caps stacks at 3 when already at 2: set `Tip.stacks = 2`, call `ApplySpell("generator")`, assert `stacks == 3`
    - Caps stacks at 3 when already at 3: set `Tip.stacks = 3`, call `ApplySpell("generator")`, assert `stacks == 3`
    - Decrements stacks on consumer: set `Tip.stacks = 2`, call `ApplySpell("consumer")`, assert `stacks == 1`
    - Floors stacks at 0 on consumer: set `Tip.stacks = 0`, call `ApplySpell("consumer")`, assert `stacks == 0`
    - Sets expiresAt to now + BUFF_DURATION on generator: call `ApplySpell("generator")`, assert `Tip.expiresAt == 110` (clock.now=100 + 10)
    - Clears expiresAt when consumer drains to 0: set `Tip.stacks = 1`, call `ApplySpell("consumer")`, assert `Tip.expiresAt == 0`
    - Sets lastPredictAt and lastPredictKind: call `ApplySpell("generator")`, assert `Tip.lastPredictAt == 100` and `Tip.lastPredictKind == "generator"`
    - Ignores unknown kind: call `ApplySpell("unknown")`, assert `stacks == 0` (unchanged)
    - Schedules non-cancelled expireTimer on generator: call `ApplySpell("generator")`, assert `Tip.expireTimer` is not nil and `Tip.expireTimer:IsCancelled() == false`
    - Expiry timer fires after BUFF_DURATION: call `ApplySpell("generator")`, `clock:advance(10.1)`, assert `Tip.stacks == 0`
    - **pending** test for Twin Fangs (BUG-04, Phase 3): `pending("adds 3 stacks for Takedown with Twin Fangs talent (BUG-04)")` -- documents expected behavior without asserting it since BUG-04 is not yet implemented

    **Tip:SyncFromAura describe block** (TEST-04, at least 6 cases):
    - Returns false when ReadLiveState returns nil: set `GetPlayerAuraBySpellID` to a function that errors (pcall in ReadLiveState catches it), assert `Tip:SyncFromAura() == false`
    - Syncs stacks from live aura: set `GetPlayerAuraBySpellID` to return `makeAuraData({applications=2, expirationTime=clock.now+8})`, assert `Tip:SyncFromAura() == true` and `Tip.stacks == 2`
    - Syncs expiresAt from live aura: same setup, assert `Tip.expiresAt == clock.now + 8`
    - Zeroes stacks when aura absent: set `GetPlayerAuraBySpellID` to return nil (default), set `Tip.stacks = 2` first, call `SyncFromAura()`, assert `Tip.stacks == 0` and `Tip.expiresAt == 0`
    - **Consumer grace suppression within window**: set `Tip.inCombat = true`, `Tip.lastPredictKind = "consumer"`, `Tip.stacks = 1`, `Tip.lastPredictAt = clock.now` (100). Set `GetPlayerAuraBySpellID` to return `makeAuraData({applications=3, expirationTime=clock.now+8})`. Advance clock by 1 second (`clock.now = 101`; within grace: 101 < 100 + 2.75 = 102.75). Call `SyncFromAura()`, assert returns false and `Tip.stacks == 1` (unchanged).
    - **Consumer grace suppression past window**: same setup but advance clock by 3 seconds (`clock.now = 103`; past grace: 103 > 102.75). Call `SyncFromAura()`, assert returns true and `Tip.stacks == 3` (synced from aura).
    - **Grace suppression does not fire for generators**: set `Tip.lastPredictKind = "generator"`, `Tip.inCombat = true`, similar setup with live stacks > predicted. Call `SyncFromAura()` within the grace window, assert returns true (not suppressed).
    - **Grace suppression does not fire out of combat**: set `Tip.inCombat = false`, `Tip.lastPredictKind = "consumer"`, same setup. Call `SyncFromAura()`, assert returns true.

    **ScheduleCastVerify serial-mismatch describe block** (TEST-04 continued, at least 2 cases):
    - Serial-mismatch causes early return: call `Tip:ApplySpell("generator")` (serial becomes 1), then `Tip:ApplySpell("generator")` again (serial becomes 2, stacks capped at 3). Monkey-patch `Tip.SyncFromAura` to track whether it was called. Advance clock past AURA_VERIFY_DELAY (1.3s). The first timer fires with serial=1 but castVerifySerial is now 2, so Verify() returns early. Assert the spy was NOT called for the stale serial. Restore original SyncFromAura.
    - Matching serial allows SyncFromAura: call `Tip:ApplySpell("generator")` once only. Advance clock past 1.3s. Assert SyncFromAura WAS called (stacks may have changed via sync).

    Run `busted spec/tip_spec.lua` and iterate until all non-pending tests pass. Then run `busted spec/` to confirm the full suite passes.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing &amp;&amp; busted spec/tip_spec.lua &amp;&amp; busted spec/</automated>
  </verify>
  <acceptance_criteria>
    - `busted spec/tip_spec.lua` exits 0 with all non-pending tests passing
    - spec/tip_spec.lua contains at least 15 `it()` blocks plus 1 `pending()` block for Twin Fangs
    - ApplySpell tests cover: stack add (+2), cap at 3, consumer decrement, expiresAt on generator, expiresAt clear at 0, lastPredictAt/Kind, unknown kind ignored, expiry timer scheduling, timer firing after BUFF_DURATION
    - SyncFromAura tests cover: nil ReadLiveState, stack sync from aura, expiresAt sync, zero on absent aura, consumer grace suppression within window, suppression past window, generator bypass, out-of-combat bypass
    - Serial-mismatch test verifies early return when castVerifySerial has changed
    - `busted spec/` exits 0 (all spec files green)
  </acceptance_criteria>
  <done>All ApplySpell, SyncFromAura, and timer tests pass. Serial-mismatch verified. Twin Fangs pending test documents BUG-04. TEST-03 and TEST-04 are delivered.</done>
</task>

<task type="auto">
  <name>Task 2: Configure luacheck and achieve zero warnings</name>
  <files>.luacheckrc</files>
  <read_first>
    Duncedmaxxing/Core.lua
    Duncedmaxxing/Util.lua
    Duncedmaxxing/Options.lua
    Duncedmaxxing/Modules/TipOfTheSpear.lua
  </read_first>
  <action>
    Install luacheck if not already present: `sudo apt install -y lua-check`.

    Create `.luacheckrc` at repo root per D-10 and D-11.

    Set `std = "lua51"`.

    Set `globals` (writeable globals -- addon code assigns these): `DuncedmaxxingDB`, `SLASH_DUNCEDMAXXING1`, `SLASH_DUNCEDMAXXING2`, `Duncedmaxxing` (Core.lua line 9 sets `_G.Duncedmaxxing = DMX`).

    Set `read_globals` (read-only WoW API globals the addon references but does not define): `CreateFrame`, `UIParent`, `STANDARD_TEXT_FONT`, `C_UnitAuras`, `C_Timer`, `C_SpecializationInfo`, `C_Spell`, `GetSpecialization`, `GetSpellTexture`, `InCombatLockdown`, `UnitClass`, `GetTime`, `SlashCmdList`, `DEFAULT_CHAT_FRAME`.

    Set `exclude_files` to `{"spec/**/*.lua"}` per D-11 (spec files excluded from addon linting).

    Run `luacheck Duncedmaxxing/` and inspect output. If warnings appear:
    - "accessing undefined global X": add X to `read_globals` (if it is a legitimate WoW API) or `globals` (if addon writes it)
    - "unused variable X": add `--no-unused-args` flag OR fix the code if the unused variable indicates a bug
    - "line too long": not relevant for luacheck defaults; ignore

    Iterate until `luacheck Duncedmaxxing/` reports zero warnings (0 warnings / 0 errors). The exact command to run: `luacheck Duncedmaxxing/ --no-unused-args`.

    Then run the full verification: `busted spec/ && luacheck Duncedmaxxing/ --no-unused-args`.
  </action>
  <verify>
    <automated>cd /home/cela/random-projects/Duncedmaxxing &amp;&amp; luacheck Duncedmaxxing/ --no-unused-args &amp;&amp; busted spec/</automated>
  </verify>
  <acceptance_criteria>
    - `.luacheckrc` exists at repo root with `std = "lua51"`
    - `.luacheckrc` has `globals` list containing `DuncedmaxxingDB`, `SLASH_DUNCEDMAXXING1`, `SLASH_DUNCEDMAXXING2`, `Duncedmaxxing`
    - `.luacheckrc` has `read_globals` list containing all 14 WoW API globals listed above
    - `.luacheckrc` has `exclude_files` containing `spec/**/*.lua`
    - `luacheck Duncedmaxxing/ --no-unused-args` exits 0 with 0 warnings and 0 errors
    - `busted spec/` still exits 0 (full suite green)
  </acceptance_criteria>
  <done>luacheck reports zero warnings on all addon source files. Full test suite green. TEST-07 is delivered. All Phase 2 requirements (TEST-01 through TEST-07) are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No security-relevant surface; test files and lint config only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | N/A | spec/tip_spec.lua | accept | Test-only file; no production impact |
| T-02-05 | N/A | .luacheckrc | accept | Dev-only config; no secrets, no network |
| T-02-SC | Tampering | apt install lua-check | mitigate | System package from Ubuntu official repos; 1.27M downloads on LuaRocks |
</threat_model>

<verification>
1. `busted spec/` exits 0 with all tests passing across all three spec files
2. `luacheck Duncedmaxxing/ --no-unused-args` exits 0 with zero warnings
3. At least 1 pending test exists for Twin Fangs (BUG-04 placeholder)
4. spec/tip_spec.lua covers both ApplySpell and SyncFromAura with timer-based tests
</verification>

<success_criteria>
- ApplySpell tests cover stack add, cap at 3, consumer decrement, expiry scheduling, timer firing
- SyncFromAura tests cover grace suppression (within/past window), nil ReadLiveState, stack sync
- Serial-mismatch path tested and verified
- luacheck zero warnings on all addon Lua files
- Full suite: busted spec/ && luacheck Duncedmaxxing/ --no-unused-args both exit 0
</success_criteria>

<output>
Create `.planning/phases/02-test-framework-and-core-logic-tests/02-03-SUMMARY.md` when done
</output>
