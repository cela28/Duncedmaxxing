---
phase: 01-utility-extraction-and-module-encapsulation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
autonomous: true
requirements: [QUAL-02, QUAL-05]

must_haves:
  truths:
    - "D-06: Tip.root, Tip.pips, Tip.borders, Tip.label, Tip.numberText are populated after Initialize and are non-nil"
    - "D-07: EnsureFrame writes frames directly to self.root, self.pips, etc."
    - "D-08: Hot-path functions (Update, RefreshLayout) use local aliases at function entry for self.root, self.pips, etc."
    - "D-11: ClassifySpellID performs a plain table lookup with no pcall wrapper"
    - "No bare upvalue references to root, pips, borders, label, or numberText remain in TipOfTheSpear.lua function bodies"
    - "The addon loads and renders the tracker display without Lua errors"
  artifacts:
    - path: "Duncedmaxxing/Modules/TipOfTheSpear.lua"
      provides: "Frame references on Tip table fields; pcall-free ClassifySpellID"
      contains: "tip.root"
  key_links:
    - from: "Tip:Initialize"
      to: "EnsureFrame"
      via: "EnsureFrame(self) call"
      pattern: "EnsureFrame\\(self\\)"
    - from: "EnsureFrame(tip)"
      to: "Tip table fields"
      via: "tip.root, tip.pips, tip.label, tip.numberText, tip.borders assignment"
      pattern: "tip\\.root.*=.*CreateFrame"
    - from: "Tip:Update"
      to: "Tip table fields"
      via: "local aliases at function entry per D-08"
      pattern: "local root.*=.*self\\.root"
    - from: "OnDragStop closure"
      to: "SavePosition"
      via: "SavePosition(tip) capturing outer parameter"
      pattern: "SavePosition\\(tip\\)"
---

<objective>
Migrate all five module-level frame locals to Tip table fields and remove the pcall wrapper from ClassifySpellID.

Purpose: Make frame references accessible for Phase 2 testing (QUAL-02) and simplify ClassifySpellID to a direct table lookup (QUAL-05). After this plan, all frame state is owned by the Tip table (Tip.root, Tip.pips, etc.), private helper functions receive `tip` as their first parameter, and ClassifySpellID is a three-line function with no pcall.

Output: Fully refactored TipOfTheSpear.lua with externally-accessible frame references and clean ClassifySpellID.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-CONTEXT.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-RESEARCH.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-PATTERNS.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-01-SUMMARY.md

<interfaces>
<!-- Key structures in TipOfTheSpear.lua the executor must understand. -->

Module-level upvalue frame locals to REMOVE (lines 32-36):
```lua
local root            -- line 32
local pips = {}       -- line 33
local label           -- line 34
local numberText      -- line 35
local borders = {}    -- line 36
```

ClassifySpellID with pcall to SIMPLIFY (lines 56-70):
```lua
local function ClassifySpellID(value)
    local ok, kind = pcall(function()
        if value == KILL_COMMAND then return "generator" end
        if type(value) == "number" and CONSUMERS[value] then return "consumer" end
    end)
    if ok then return kind end
end
```

Category B — private local functions that access frame upvalues (must gain `tip` parameter):
- ApplyPosition()          — line 146, uses `root`
- SavePosition()           — line 154, uses `root`, calls ApplyPosition()
- EnsureBorders(parent)    — line 191, uses `borders`
- LayoutBorders(w,h,b,s)   — line 207, uses `borders`, `root`
- SetBordersShown(shown)   — line 244, uses `borders`
- EnsureFrame()            — line 292, uses all 5: root, pips, label, numberText, borders

Category A — Tip methods that access frame upvalues (must use self.* or local aliases):
- Tip:RefreshLayout()      — line 451, uses pips, root, numberText; calls SetBordersShown, LayoutBorders, ApplyPosition
- Tip:ApplyLock()          — line 540, uses root, label
- Tip:Update()             — line 581, uses root, pips, numberText, label; calls SetBordersShown
- Tip:Initialize(core)     — line 743, calls EnsureFrame()

Functions that do NOT reference frame upvalues (NO changes needed):
- ClampStacks, FindTrackedSpell, ReadLiveState, GetCfg, ColorTuple, ResolveSpellTexture
- CreateBorder, CreatePip, PaintBorder, SetPipBordersShown, LayoutPipBorder
- Tip:RefreshActive, Tip:SyncFromAura, Tip:ScheduleExpiration, Tip:ScheduleCastVerify
- Tip:ScheduleAuraVerify, Tip:ResetPosition, Tip:SetTestStacks, Tip:GetStacks
- Tip:ApplySpell, Tip:OnEvent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove pcall from ClassifySpellID and remove frame upvalue declarations</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (full file — understand all references to root, pips, borders, label, numberText across all functions; understand ClassifySpellID at lines 56-70)
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-PATTERNS.md (Edit 1 for upvalue removal, Edit 2 for ClassifySpellID replacement)
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-RESEARCH.md (Pattern 5 for ClassifySpellID after state, Pitfall 4 and 5 for pips/borders initialization)
  </read_first>
  <action>
    This task makes the two simplest changes that do not require the full frame reference migration. They are preparatory for Task 2 but also self-contained.

    Edit A — Remove pcall from ClassifySpellID (per D-11, QUAL-05):
    Replace ClassifySpellID (lines 56-70) with a direct implementation:
    - Remove the pcall wrapper and inner anonymous function
    - The function body becomes: check `value == KILL_COMMAND` returning `"generator"`, then check `type(value) == "number" and CONSUMERS[value]` returning `"consumer"`, then implicit nil return
    - Behavior is identical — pcall always succeeded on a pure table lookup; the pcall wrapper was unnecessary error protection

    Edit B — Remove module-level upvalue declarations (lines 32-36):
    Delete these 5 lines:
    - `local root`
    - `local pips = {}`
    - `local label`
    - `local numberText`
    - `local borders = {}`

    IMPORTANT: After this edit and before Task 2, the file will have undefined references. This is expected and intentional — Task 2 immediately follows to replace all bare references with `tip.*` and `self.*` access patterns. Do NOT attempt to run the addon between Task 1 and Task 2.
  </action>
  <verify>
    <automated>grep -v '^--' Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -c "pcall" | grep -q "^2$" && grep -c "^local root$" Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -q "^0$" && grep -c "^local pips = {}$" Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
    <human-check>The file should have exactly 2 pcall calls remaining (both inside ReadLiveState at lines ~86 and ~95, which are intentional error protection for WoW API calls). The 5 module-level frame locals should be gone.</human-check>
  </verify>
  <acceptance_criteria>
    - ClassifySpellID function body contains NO `pcall` call
    - ClassifySpellID contains `if value == KILL_COMMAND then return "generator" end`
    - ClassifySpellID contains `if type(value) == "number" and CONSUMERS[value] then return "consumer" end`
    - The 5 lines `local root`, `local pips = {}`, `local label`, `local numberText`, `local borders = {}` are absent from the file
    - ReadLiveState still contains its pcall calls (lines ~86, ~95 — these are intentional WoW API error protection, not affected by D-11)
    - All other functions in the file are unchanged at this point
  </acceptance_criteria>
  <done>ClassifySpellID is a direct three-line lookup with no pcall; all five module-level frame upvalue declarations are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all frame references to Tip table fields</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (full file after Task 1 changes — all bare root/pips/borders/label/numberText references are now undefined and must be migrated)
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-PATTERNS.md (Edit 3 full migration pattern for every function, including EnsureFrame, ApplyPosition, SavePosition, EnsureBorders, LayoutBorders, SetBordersShown, and all Tip methods)
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-RESEARCH.md (Pattern 4 for the private function problem, Pitfalls 2-5 for critical migration hazards, Open Questions 1-2 for cleanup and LayoutBorders signature)
  </read_first>
  <action>
    This is the largest single edit in the phase. Migrate all frame references from bare upvalues to Tip table fields per D-06, D-07, D-08.

    CATEGORY B — Private local functions gain `tip` as first parameter (per D-07, research A1):

    1. `ApplyPosition()` becomes `ApplyPosition(tip)`:
       - `root` becomes `tip.root` (guard and all references)
       - Signature: `local function ApplyPosition(tip)`

    2. `SavePosition()` becomes `SavePosition(tip)`:
       - `root` becomes `tip.root` (guard and GetCenter call)
       - `ApplyPosition()` call becomes `ApplyPosition(tip)`
       - Signature: `local function SavePosition(tip)`

    3. `EnsureBorders(parent)` becomes `EnsureBorders(tip)`:
       - Parameter changes from `parent` to `tip`
       - Idempotency guard changes from `if borders.top then return end` to `if tip.borders and tip.borders.top then return end` (Pitfall 5 prevention)
       - Initialize: `tip.borders = {}` at start if creating borders
       - All `borders.X = CreateBorder(parent)` become `tip.borders.X = CreateBorder(tip.root)`
       - Signature: `local function EnsureBorders(tip)`

    4. `LayoutBorders(width, height, borderSize, segmentWidths)` becomes `LayoutBorders(tip, width, height, borderSize, segmentWidths)`:
       - All `borders.X` become `tip.borders.X`
       - All bare `root` become `tip.root`
       - The `for _, border in pairs(borders)` loop becomes `for _, border in pairs(tip.borders)`
       - Signature: `local function LayoutBorders(tip, width, height, borderSize, segmentWidths)`

    5. `SetBordersShown(shown)` becomes `SetBordersShown(tip, shown)`:
       - `borders` becomes `tip.borders`
       - Signature: `local function SetBordersShown(tip, shown)`

    6. `EnsureFrame()` becomes `EnsureFrame(tip)` (the central migration point, per D-07):
       - Idempotency guard: `if root then return end` becomes `if tip.root then return end`
       - `root = CreateFrame(...)` becomes `tip.root = CreateFrame(...)`
       - All `root:Method()` become `tip.root:Method()`
       - Initialize tables BEFORE use: `tip.pips = {}` and `tip.borders = {}` before EnsureBorders call and pips loop (Pitfall 4 prevention)
       - `EnsureBorders(root)` becomes `EnsureBorders(tip)`
       - `pips[i] = CreatePip(root)` becomes `tip.pips[i] = CreatePip(tip.root)`
       - `label = root:CreateFontString(...)` becomes `tip.label = tip.root:CreateFontString(...)`
       - `numberText = root:CreateFontString(...)` becomes `tip.numberText = tip.root:CreateFontString(...)`
       - All `label:Method()` and `numberText:Method()` become `tip.label:Method()` and `tip.numberText:Method()`
       - OnDragStart closure: keep `self` as WoW frame parameter; `self:StartMoving()` is unchanged
       - OnDragStop closure: keep `self` as WoW frame parameter; change `SavePosition()` to `SavePosition(tip)` — `tip` is the outer function parameter captured by the closure, `self` is the WoW frame (Pitfall 3 prevention)
       - Signature: `local function EnsureFrame(tip)`

    CATEGORY A — Tip methods use self.* with D-08 local aliases at hot-path entry:

    7. `Tip:RefreshLayout()`:
       - `EnsureFrame()` becomes `EnsureFrame(self)`
       - Add local aliases at function entry (D-08): `local root = self.root`, `local pips = self.pips`, `local numberText = self.numberText`
       - All bare `pips[i]` use the local alias (already named `pips`)
       - All bare `root` use the local alias
       - `numberText:Hide()` uses the local alias
       - `SetBordersShown(false)` becomes `SetBordersShown(self, false)`
       - `LayoutBorders(w, h, b, s)` becomes `LayoutBorders(self, w, h, b, s)`
       - `ApplyPosition()` becomes `ApplyPosition(self)`

    8. `Tip:ApplyLock()`:
       - Guard: `if not root then return end` becomes `if not self.root then return end`
       - Add local aliases: `local root, label = self.root, self.label`
       - Remaining body uses the local aliases (unchanged call patterns)

    9. `Tip:Update()`:
       - `EnsureFrame()` becomes `EnsureFrame(self)`
       - Add local aliases at function entry (D-08): `local root = self.root`, `local pips = self.pips`, `local label = self.label`, `local numberText = self.numberText`
       - All bare `root`, `pips[i]`, `label`, `numberText` use local aliases
       - `SetBordersShown(false)` becomes `SetBordersShown(self, false)`
       - `SetBordersShown(drawShell and hasBorder)` becomes `SetBordersShown(self, drawShell and hasBorder)`

    10. `Tip:Initialize(core)`:
        - `EnsureFrame()` becomes `EnsureFrame(self)`
        - No other frame references in this method

    Functions NOT requiring changes (no frame upvalue references):
    ClampStacks, FindTrackedSpell, ReadLiveState, GetCfg, ColorTuple, ResolveSpellTexture, CreateBorder, CreatePip, PaintBorder, SetPipBordersShown, LayoutPipBorder, Tip:RefreshActive, Tip:SyncFromAura, Tip:ScheduleExpiration, Tip:ScheduleCastVerify, Tip:ScheduleAuraVerify, Tip:ResetPosition, Tip:SetTestStacks, Tip:GetStacks, Tip:ApplySpell, Tip:OnEvent
  </action>
  <verify>
    <automated>grep -v '^--' Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -c "EnsureFrame(self)" | grep -q "^[3-4]$" && grep -c "tip\.root" Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -q "^[1-9]" && grep -c "local root.*=.*self\.root" Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -q "^[2-3]$" && grep -c "SavePosition(tip)" Duncedmaxxing/Modules/TipOfTheSpear.lua | grep -q "^[1-2]$" && echo "PASS" || echo "FAIL"</automated>
    <human-check>Search for any remaining bare `root` references that are NOT: (a) inside a local alias assignment, (b) inside a string literal, (c) inside a comment, (d) part of another identifier like `tip.root`. Any bare `root` usage outside these categories indicates an incomplete migration.</human-check>
  </verify>
  <acceptance_criteria>
    - Zero bare upvalue references to `root`, `pips`, `borders`, `label`, `numberText` remain in any function body (only `tip.*` or `self.*` or local aliases from `self.*`)
    - EnsureFrame signature is `local function EnsureFrame(tip)` with `tip.root`, `tip.pips`, `tip.borders`, `tip.label`, `tip.numberText` assignments
    - EnsureFrame contains `tip.pips = {}` and `tip.borders = {}` initialization before their indexed use
    - EnsureFrame OnDragStop closure calls `SavePosition(tip)` (not `SavePosition()`)
    - EnsureBorders signature is `local function EnsureBorders(tip)` with guard `if tip.borders and tip.borders.top then return end`
    - ApplyPosition, SavePosition, LayoutBorders, SetBordersShown all have `tip` as first parameter
    - Tip:RefreshLayout contains `local root = self.root` alias at function entry
    - Tip:Update contains `local root = self.root` alias at function entry
    - Tip:ApplyLock guard is `if not self.root then return end`
    - Tip:Initialize calls `EnsureFrame(self)`
    - SetPipBordersShown and LayoutPipBorder are UNCHANGED (they operate on pip.border, not module-level borders)
    - CreateBorder and CreatePip are UNCHANGED (they receive parent as parameter, not frame upvalues)
  </acceptance_criteria>
  <done>All five frame references are Tip table fields; private helpers take tip parameter; hot-path methods use D-08 local aliases; the addon's frame ownership model is clean and testable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW API -> addon | GetPlayerAuraBySpellID returns untrusted aura data; ReadLiveState wraps in pcall (unchanged) |
| Frame events -> Tip methods | UNIT_SPELLCAST_SUCCEEDED carries spell IDs; ClassifySpellID validates via table lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | ClassifySpellID after pcall removal | accept | Function performs only `==` comparison and table lookup on numeric spell IDs — no error path exists in Lua 5.1 for these operations; pcall was unnecessary protection |
| T-01-05 | Denial of Service | EnsureFrame idempotency | mitigate | Guard `if tip.root then return end` preserved — prevents multiple frame creation; `tip.pips = {}` initialization prevents nil-index crash |
| T-01-06 | Tampering | OnDragStop closure variable capture | mitigate | Closure captures `tip` (Tip module table) from EnsureFrame parameter, not `self` (WoW frame) — variable naming convention prevents confusion per Pitfall 3 |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure Lua file edits |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "pcall" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns exactly 2 (both in ReadLiveState — the intentional WoW API protection)
2. `grep -c "^local root$\|^local pips\|^local label$\|^local numberText$\|^local borders" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns 0
3. `grep -c "tip\.root" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns a positive count (EnsureFrame assignments)
4. `grep -c "self\.root" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns a positive count (Tip method references)
5. `grep -c "EnsureFrame(self)" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns 3-4 (Initialize, Update, RefreshLayout)
6. `grep -c "SavePosition(tip)" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns 1-2 (OnDragStop closure, and SavePosition's own ApplyPosition call)
</verification>

<success_criteria>
- All 5 frame references (root, pips, borders, label, numberText) live on Tip table fields
- Private helper functions receive `tip` parameter instead of accessing upvalues
- Hot-path methods (Update, RefreshLayout) create local aliases from self.* per D-08
- ClassifySpellID is a direct table lookup with no pcall
- EnsureFrame properly initializes tip.pips and tip.borders tables before indexed assignment
- OnDragStop closure correctly captures `tip` (not `self`) for SavePosition call
</success_criteria>

<output>
Create `.planning/phases/01-utility-extraction-and-module-encapsulation/01-02-SUMMARY.md` when done
</output>
