---
phase: 01-utility-extraction-and-module-encapsulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Util.lua
  - Duncedmaxxing/Duncedmaxxing.toc
  - Duncedmaxxing/Core.lua
  - Duncedmaxxing/Options.lua
autonomous: true
requirements: [QUAL-01, QUAL-04]

must_haves:
  truths:
    - "DMX.Util.Clamp, DMX.Util.ParseHexColor, DMX.Util.Trim, DMX.Util.ParseOnOff are callable functions after addon load"
    - "No duplicate Clamp, ParseHexColor, Trim, or ParseOnOff function definitions remain in Core.lua or Options.lua"
    - "ForEachModule iterates moduleOrder array via ipairs, not pairs over modules table"
    - "Util.lua is listed before Core.lua in the TOC file"
  artifacts:
    - path: "Duncedmaxxing/Util.lua"
      provides: "Shared utility functions on DMX.Util table"
      contains: "DMX.Util"
    - path: "Duncedmaxxing/Duncedmaxxing.toc"
      provides: "Load order with Util.lua first"
      contains: "Util.lua"
    - path: "Duncedmaxxing/Core.lua"
      provides: "Local aliases for DMX.Util functions, moduleOrder array, ipairs-based ForEachModule"
      contains: "DMX.Util.Clamp"
    - path: "Duncedmaxxing/Options.lua"
      provides: "Local aliases replacing duplicate definitions"
      contains: "DMX.Util.Clamp"
  key_links:
    - from: "Duncedmaxxing/Util.lua"
      to: "DMX namespace"
      via: "DMX.Util table assignment"
      pattern: "DMX\\.Util"
    - from: "Duncedmaxxing/Core.lua"
      to: "Duncedmaxxing/Util.lua"
      via: "local aliases at file top"
      pattern: "local Clamp.*=.*DMX\\.Util\\.Clamp"
    - from: "Duncedmaxxing/Core.lua"
      to: "DMX.moduleOrder"
      via: "ipairs iteration in ForEachModule"
      pattern: "ipairs.*self\\.moduleOrder"
---

<objective>
Extract shared utility functions to Duncedmaxxing/Util.lua, wire consumer files via local aliases, and convert ForEachModule to ordered iteration.

Purpose: Eliminate duplicate utility definitions across Core.lua and Options.lua (QUAL-01), and make module iteration deterministic (QUAL-04). After this plan, the addon loads Util.lua first in the TOC, all utility call sites resolve through DMX.Util, and ForEachModule iterates modules in registration order.

Output: New Util.lua file; updated TOC, Core.lua, and Options.lua; deterministic ForEachModule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-CONTEXT.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-RESEARCH.md
@.planning/phases/01-utility-extraction-and-module-encapsulation/01-PATTERNS.md

<interfaces>
<!-- Key structures the executor needs. Extracted from source files. -->

From Duncedmaxxing/Core.lua (lines 1-7 — namespace bootstrap):
```lua
local addonName, DMX = ...
_G.Duncedmaxxing = DMX
DMX.name = addonName
DMX.version = "0.3.2"
DMX.modules = DMX.modules or {}
```

From Duncedmaxxing/Core.lua (lines 38-70 — utility defs to REMOVE):
```lua
local function Trim(text) ... end       -- line 38
local function Clamp(value, ...) ... end -- line 42
local function ParseOnOff(value) ... end -- line 50
local function ParseHexColor(value) ... end -- line 59
```

From Duncedmaxxing/Core.lua (lines 140-164 — module registry to MODIFY):
```lua
function DMX:RegisterModule(key, module) ... end  -- line 140
function DMX:GetModule(key) ... end               -- line 149
function DMX:GetDB() ... end                      -- line 153
function DMX:ForEachModule(method, ...) ... end    -- line 157 (uses pairs)
```

From Duncedmaxxing/Options.lua (lines 1-4 — namespace):
```lua
local _, DMX = ...
local Options = {}
DMX.Options = Options
```

From Duncedmaxxing/Options.lua (lines 17-37 — duplicate defs to REMOVE):
```lua
local function Clamp(value, ...) ... end       -- line 17
local function ParseHexColor(value) ... end    -- line 25
```

From Duncedmaxxing/Options.lua (line 39 — ToByte calls Clamp):
```lua
local function ToByte(value)
    value = Clamp(value or 1, 0, 1) or 1
    return math.floor(value * 255 + 0.5)
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Util.lua and update TOC load order</name>
  <files>Duncedmaxxing/Util.lua, Duncedmaxxing/Duncedmaxxing.toc</files>
  <read_first>
    - Duncedmaxxing/Core.lua (lines 38-70 for canonical function implementations — use the Trim-based ParseHexColor per D-04)
    - Duncedmaxxing/Duncedmaxxing.toc (current load order — Util.lua must be inserted as first file entry before Core.lua, per D-05)
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-PATTERNS.md (Pattern 1 for Util.lua structure, Pattern 2 for TOC entry)
  </read_first>
  <action>
    Create Duncedmaxxing/Util.lua as a new file per D-01 and D-02.

    File structure follows the project namespace convention:
    - Open with `local _, DMX = ...` (underscore form, matching Options.lua and TipOfTheSpear.lua line 1)
    - Create namespace: `DMX.Util = {}` then `local Util = DMX.Util`
    - Define exactly 4 local functions: `Trim`, `Clamp`, `ParseOnOff`, `ParseHexColor`
    - Copy implementations verbatim from Core.lua lines 38-70 (the canonical versions)
    - ParseHexColor MUST use the Trim-based nil handling: `value = Trim(value):gsub("^#", "")` — NOT the `tostring(value or "")` approach from Options.lua (per D-04)
    - At file bottom, assign to namespace: `Util.Trim = Trim`, `Util.Clamp = Clamp`, `Util.ParseOnOff = ParseOnOff`, `Util.ParseHexColor = ParseHexColor`
    - Do NOT include ToByte, ColorToHex, CopyDefaults, or MergeDefaults — D-01 explicitly excludes them

    Update Duncedmaxxing/Duncedmaxxing.toc:
    - Insert `Util.lua` as the first file entry (line 10, before `Core.lua`)
    - The metadata block (lines 1-9) remains unchanged
    - Use no leading path component (same convention as existing `Core.lua` entry)
  </action>
  <verify>
    <automated>grep -c "DMX.Util" Duncedmaxxing/Util.lua | grep -q "^[1-9]" && grep -c "Util.lua" Duncedmaxxing/Duncedmaxxing.toc | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Duncedmaxxing/Util.lua exists and contains `DMX.Util = {}`
    - Duncedmaxxing/Util.lua contains exactly 4 exported functions: Trim, Clamp, ParseOnOff, ParseHexColor
    - Duncedmaxxing/Util.lua line 1 is `local _, DMX = ...`
    - Duncedmaxxing/Util.lua ParseHexColor body contains `Trim(value)` (not `tostring`)
    - Duncedmaxxing/Util.lua does NOT contain ToByte, ColorToHex, CopyDefaults, or MergeDefaults
    - Duncedmaxxing/Duncedmaxxing.toc lists `Util.lua` on the line immediately before `Core.lua`
    - Duncedmaxxing/Duncedmaxxing.toc still contains all original file entries (Core.lua, Options.lua, Modules\TipOfTheSpear.lua)
  </acceptance_criteria>
  <done>Util.lua created with 4 canonical utility functions exposed on DMX.Util; TOC updated with Util.lua as first loaded file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Core.lua and Options.lua to DMX.Util and add moduleOrder</name>
  <files>Duncedmaxxing/Core.lua, Duncedmaxxing/Options.lua</files>
  <read_first>
    - Duncedmaxxing/Core.lua (full file — understand all call sites for Trim, Clamp, ParseOnOff, ParseHexColor; understand RegisterModule at line 140 and ForEachModule at line 157)
    - Duncedmaxxing/Options.lua (lines 1-50 — understand Clamp/ParseHexColor definitions at 17-37 and ToByte at 39 which calls Clamp)
    - Duncedmaxxing/Util.lua (the file just created in Task 1 — verify the Util table is populated correctly before creating aliases)
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-PATTERNS.md (Pattern 3 for alias pattern, Edit 2-3 for Core.lua moduleOrder changes)
  </read_first>
  <action>
    Modify Duncedmaxxing/Core.lua — three edits:

    Edit A — Remove utility definitions and add aliases (per D-01, D-02, D-03):
    - Delete the 4 local function definitions: `Trim` (lines 38-40), `Clamp` (lines 42-48), `ParseOnOff` (lines 50-57), `ParseHexColor` (lines 59-70)
    - Add 4 local aliases immediately after line 1 (`local addonName, DMX = ...`) and before `_G.Duncedmaxxing = DMX`:
      `local Clamp = DMX.Util.Clamp`
      `local ParseHexColor = DMX.Util.ParseHexColor`
      `local Trim = DMX.Util.Trim`
      `local ParseOnOff = DMX.Util.ParseOnOff`
    - All existing call sites (slash command handler lines 227, 295, 306, 315, 333) use the same local names — zero call site changes needed
    - CopyDefaults (line 72) and MergeDefaults (line 84) remain as local functions in Core.lua — they are NOT extracted per D-01

    Edit B — Add moduleOrder initialization (per D-09):
    - After `DMX.modules = DMX.modules or {}` (currently line 7), add: `DMX.moduleOrder = DMX.moduleOrder or {}`

    Edit C — Update RegisterModule and ForEachModule (per D-09, D-10):
    - In RegisterModule: add `table.insert(self.moduleOrder, key)` after `self.modules[key] = module` and before `module.key = key`
    - In ForEachModule: replace `for _, module in pairs(self.modules) do` with `for _, key in ipairs(self.moduleOrder) do` followed by `local module = self.modules[key]`
    - Add nil guard: `local fn = module and module[method]` (defensive for future robustness)
    - GetModule and GetDB methods are unchanged

    Modify Duncedmaxxing/Options.lua — one edit (per D-01, D-02, D-03):
    - Delete the local function `Clamp` definition (lines 17-23)
    - Delete the local function `ParseHexColor` definition (lines 25-37)
    - Add 2 local aliases after line 4 (`DMX.Options = Options`) and before `local WHITE_TEX`:
      `local Clamp = DMX.Util.Clamp`
      `local ParseHexColor = DMX.Util.ParseHexColor`
    - Do NOT add aliases for Trim or ParseOnOff — they are not used in Options.lua (verified by source audit)
    - ToByte (line 39, references `Clamp`) will resolve to the alias — this is correct as long as the alias appears before ToByte's definition in file order
  </action>
  <verify>
    <automated>grep -v '^--' Duncedmaxxing/Core.lua | grep -c "local function Trim\|local function Clamp\|local function ParseOnOff\|local function ParseHexColor" | grep -q "^0$" && grep -c "DMX.Util.Clamp" Duncedmaxxing/Core.lua | grep -q "^1$" && grep -c "ipairs(self.moduleOrder)" Duncedmaxxing/Core.lua | grep -q "^1$" && grep -v '^--' Duncedmaxxing/Options.lua | grep -c "local function Clamp\|local function ParseHexColor" | grep -q "^0$" && grep -c "DMX.Util.Clamp" Duncedmaxxing/Options.lua | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Core.lua contains zero `local function Trim`, `local function Clamp`, `local function ParseOnOff`, `local function ParseHexColor` definitions (excluding comments)
    - Core.lua contains `local Clamp = DMX.Util.Clamp` alias (exactly 1 occurrence)
    - Core.lua contains `local ParseHexColor = DMX.Util.ParseHexColor` alias (exactly 1 occurrence)
    - Core.lua contains `local Trim = DMX.Util.Trim` alias (exactly 1 occurrence)
    - Core.lua contains `local ParseOnOff = DMX.Util.ParseOnOff` alias (exactly 1 occurrence)
    - Core.lua still contains `local function CopyDefaults` and `local function MergeDefaults` (NOT extracted)
    - Core.lua contains `DMX.moduleOrder = DMX.moduleOrder or {}`
    - Core.lua RegisterModule contains `table.insert(self.moduleOrder, key)`
    - Core.lua ForEachModule uses `ipairs(self.moduleOrder)` instead of `pairs(self.modules)`
    - Options.lua contains zero `local function Clamp` and zero `local function ParseHexColor` definitions (excluding comments)
    - Options.lua contains `local Clamp = DMX.Util.Clamp` alias
    - Options.lua contains `local ParseHexColor = DMX.Util.ParseHexColor` alias
    - Options.lua does NOT contain `DMX.Util.Trim` or `DMX.Util.ParseOnOff` aliases (unused in this file)
    - Options.lua ToByte function body still references `Clamp` (unchanged call site)
  </acceptance_criteria>
  <done>Core.lua and Options.lua use DMX.Util aliases with zero duplicate definitions; ForEachModule iterates moduleOrder via ipairs; addon is structurally complete with utility extraction and ordered module iteration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> slash commands | Slash command arguments are untrusted strings parsed by Trim, Clamp, ParseOnOff, ParseHexColor |
| SavedVariables -> addon state | DuncedmaxxingDB is persisted across sessions; could contain stale or malformed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DMX.Util namespace | accept | Util table is populated at file load time in WoW sandbox; no external code injection vector exists in WoW addon environment |
| T-01-02 | Tampering | Slash command input | mitigate | Clamp, ParseOnOff, ParseHexColor already validate and sanitize all user input — extraction preserves existing validation logic verbatim |
| T-01-03 | Information Disclosure | SavedVariables | accept | No PII stored; settings are display preferences only (scale, color, position) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure Lua file edits loaded by WoW client |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "DMX.Util" Duncedmaxxing/Util.lua` returns a non-zero count
2. `grep "Util.lua" Duncedmaxxing/Duncedmaxxing.toc` shows Util.lua listed before Core.lua
3. No `local function Trim`, `local function Clamp`, `local function ParseOnOff`, or `local function ParseHexColor` exist in Core.lua or Options.lua (excluding comments)
4. `grep "ipairs(self.moduleOrder)" Duncedmaxxing/Core.lua` returns exactly 1 match
5. `grep "DMX.moduleOrder" Duncedmaxxing/Core.lua` returns at least 1 match
</verification>

<success_criteria>
- Util.lua exists with exactly 4 exported functions on DMX.Util
- TOC loads Util.lua before Core.lua
- Core.lua and Options.lua have zero duplicate utility definitions
- ForEachModule uses ipairs over moduleOrder array
- All existing slash command call sites and Options.lua widget call sites are unchanged (same local names resolve through aliases)
</success_criteria>

<output>
Create `.planning/phases/01-utility-extraction-and-module-encapsulation/01-01-SUMMARY.md` when done
</output>
