---
phase: 07-spell-coverage-add-missing-consumers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Duncedmaxxing/Modules/TipOfTheSpear.lua
  - spec/tip_spec.lua
  - .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]

must_haves:
  truths:
    - Moonlight Chakram (1264902) is classified as "consumer" by ClassifySpellID and decrements stacks by 1 via ApplySpell
    - Hatchet Toss (193265) is classified as "consumer" by ClassifySpellID and decrements stacks by 1 via ApplySpell
    - ClassifySpellID is directly testable via DMX._test.tip.ClassifySpellID escape hatch
    - Automated tests prove classification and decrement for both new spell IDs
    - Full test suite passes with no regressions (baseline 121 + 4 new tests)
    - In-game verification of both spells is flagged in the UAT checklist
  artifacts:
    - Duncedmaxxing/Modules/TipOfTheSpear.lua contains [1264902] = true and [193265] = true in CONSUMERS table
    - Duncedmaxxing/Modules/TipOfTheSpear.lua contains DMX._test.tip = { ClassifySpellID = ClassifySpellID } escape hatch
    - spec/tip_spec.lua contains describe("ClassifySpellID", ...) block with assertions for both IDs
    - spec/tip_spec.lua contains ApplySpell decrement it(...) blocks for both IDs
    - 01-HUMAN-UAT.md contains in-game verification entries for Moonlight Chakram and Hatchet Toss
  key_links:
    - CONSUMERS table entries feed into ClassifySpellID lookup which feeds into FindTrackedSpell dispatch which feeds into ApplySpell decrement
    - DMX._test.tip escape hatch exposes the private ClassifySpellID to spec/tip_spec.lua assertions
---

<objective>
Register Moonlight Chakram (1264902) and Hatchet Toss (193265) as Tip of the Spear consumer spells, prove classification and decrement via automated tests, and flag in-game verification in the UAT checklist.

Per D-01: only these 2 spells are in scope. Flamefang Pitch (1251592) is explicitly excluded.
Per D-02: both are plain consumers (consume 1 stack, no special mechanics).
Per D-03: unconditional CONSUMERS table entries, no runtime talent checks.
Per D-04: no confirmed alternate spell IDs for either spell; Moonlight Chakram variant 1264949 flagged for UAT verification.

Purpose: Close the spell-audit gap so all known Survival Hunter consumer abilities are tracked by the addon.
Output: Updated TipOfTheSpear.lua with 2 new CONSUMERS entries + test escape hatch, 4 new tests in tip_spec.lua, 2 UAT checklist entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-spell-coverage-add-missing-consumers/07-CONTEXT.md
@.planning/phases/07-spell-coverage-add-missing-consumers/07-RESEARCH.md
@.planning/phases/07-spell-coverage-add-missing-consumers/07-PATTERNS.md
</context>

## Artifacts this phase produces

| Symbol / Path | Kind | Location |
|---------------|------|----------|
| `CONSUMERS[1264902]` | table entry | `Duncedmaxxing/Modules/TipOfTheSpear.lua` CONSUMERS table |
| `CONSUMERS[193265]` | table entry | `Duncedmaxxing/Modules/TipOfTheSpear.lua` CONSUMERS table |
| `DMX._test.tip` | table (test escape hatch) | `Duncedmaxxing/Modules/TipOfTheSpear.lua` near EOF |
| `DMX._test.tip.ClassifySpellID` | function reference | `Duncedmaxxing/Modules/TipOfTheSpear.lua` near EOF |
| `describe("ClassifySpellID", ...)` | test block | `spec/tip_spec.lua` |
| UAT test #9: Moonlight Chakram | checklist entry | `01-HUMAN-UAT.md` |
| UAT test #10: Hatchet Toss | checklist entry | `01-HUMAN-UAT.md` |

<tasks>

<task type="auto">
  <name>Task 1: Add Moonlight Chakram and Hatchet Toss to CONSUMERS table + ClassifySpellID escape hatch</name>
  <files>Duncedmaxxing/Modules/TipOfTheSpear.lua</files>
  <read_first>
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (lines 20-28 for CONSUMERS table, lines 74-82 for ClassifySpellID, line 766 for RegisterModule call)
    - Duncedmaxxing/Core.lua (lines 213-220 for existing DMX._test escape hatch pattern)
    - .planning/phases/07-spell-coverage-add-missing-consumers/07-PATTERNS.md (exact code patterns to follow)
  </read_first>
  <action>
Two changes to Duncedmaxxing/Modules/TipOfTheSpear.lua, per D-01/D-02/D-03:

1. CONSUMERS table (lines 20-28): Append two entries after the last existing entry ([1262343] = true on line 27), before the closing brace. Add:
   - [1264902] = true with a comment naming Moonlight Chakram
   - [193265] with right-padding to align the = true, and a comment naming Hatchet Toss
   Follow the exact format of existing entries (spaces around =, comma after true, -- comment).

2. Escape hatch (insert before the final DMX:RegisterModule("tip", Tip) call on line 766): Add a test-only escape hatch block that extends the existing DMX._test table (created by Core.lua:213-220, which loads first per TOC order). Use DMX._test = DMX._test or {} as a nil-safety guard, then set DMX._test.tip to a table containing ClassifySpellID = ClassifySpellID. Include the same two-line comment header used in Core.lua:213-214 but reference spec/tip_spec.lua instead of spec/core_spec.lua.

No changes to ClassifySpellID, FindTrackedSpell, or ApplySpell functions. The new CONSUMERS entries are auto-discovered by the existing generic lookup.
  </action>
  <acceptance_criteria>
    - grep "1264902.*true" Duncedmaxxing/Modules/TipOfTheSpear.lua returns a match
    - grep "193265.*true" Duncedmaxxing/Modules/TipOfTheSpear.lua returns a match
    - grep "Moonlight Chakram" Duncedmaxxing/Modules/TipOfTheSpear.lua returns a match (in the CONSUMERS comment)
    - grep "Hatchet Toss" Duncedmaxxing/Modules/TipOfTheSpear.lua returns a match (in the CONSUMERS comment)
    - grep "DMX._test.tip" Duncedmaxxing/Modules/TipOfTheSpear.lua returns at least 1 match
    - grep "ClassifySpellID = ClassifySpellID" Duncedmaxxing/Modules/TipOfTheSpear.lua returns 1 match (in the escape hatch)
    - The escape hatch block appears before DMX:RegisterModule("tip", Tip) in the file
    - DMX._test = DMX._test or {} guard is present (does not clobber Core.lua fields)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "1264902.*true\|193265.*true\|DMX._test.tip\|ClassifySpellID = ClassifySpellID" Duncedmaxxing/Modules/TipOfTheSpear.lua</automated>
  </verify>
  <done>CONSUMERS table contains [1264902] = true (Moonlight Chakram) and [193265] = true (Hatchet Toss). DMX._test.tip.ClassifySpellID escape hatch exists before RegisterModule call. No other production logic changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ClassifySpellID and ApplySpell unit tests for both new consumer spells</name>
  <files>spec/tip_spec.lua</files>
  <read_first>
    - spec/tip_spec.lua (lines 9-27 for setup pattern, lines 202-218 for existing consumer decrement tests to mirror, line 219 for describe block closing)
    - Duncedmaxxing/Modules/TipOfTheSpear.lua (the CONSUMERS table and escape hatch from Task 1)
    - .planning/phases/07-spell-coverage-add-missing-consumers/07-PATTERNS.md (test patterns section)
    - .planning/phases/07-spell-coverage-add-missing-consumers/07-RESEARCH.md (Pitfall 1 — must test ClassifySpellID separately from ApplySpell)
  </read_first>
  <behavior>
    - Test: DMX._test.tip.ClassifySpellID(1264902) returns "consumer"
    - Test: DMX._test.tip.ClassifySpellID(193265) returns "consumer"
    - Test: Tip:ApplySpell("consumer", 1264902) at stacks=2 results in stacks=1
    - Test: Tip:ApplySpell("consumer", 193265) at stacks=2 results in stacks=1
  </behavior>
  <action>
Two additions to spec/tip_spec.lua:

1. ApplySpell decrement tests (SC-3): Inside the existing describe("Tip:ApplySpell", ...) block, after the Raptor Swipe (1262343) test at line 218 and before the closing end) at line 219, add two new it(...) blocks:
   - One for 1264902 (Moonlight Chakram): set Tip.stacks = 2, call Tip:ApplySpell("consumer", 1264902), assert.equals(1, Tip.stacks)
   - One for 193265 (Hatchet Toss): same pattern with 193265
   Mirror the exact format of the 1262343 test (lines 214-218): inline comment before the it() block explaining the regression coverage, spell name in both the it() description string and the inline comment after ApplySpell call.

2. ClassifySpellID tests (SC-2): After the describe("Tip:ApplySpell", ...) block closes (after the new tests from step 1), add a new describe("ClassifySpellID", ...) block. This block needs:
   - local DMX, Tip, clock variables
   - before_each calling loader.load() and loader.resetTipState(Tip, clock), same as the ApplySpell block
   - Two it(...) blocks asserting DMX._test.tip.ClassifySpellID(1264902) equals "consumer" and DMX._test.tip.ClassifySpellID(193265) equals "consumer"
   This is critical per RESEARCH.md Pitfall 1: calling ApplySpell with hard-coded kind="consumer" does NOT exercise ClassifySpellID. The escape hatch test is the only way to genuinely satisfy SC-2.

Run the full suite after both additions to confirm all tests pass (baseline 121 + 4 new = 125 expected).
  </action>
  <acceptance_criteria>
    - npx -y -p fengari@0.1.5 node spec/run.cjs exits 0 with 125 passed, 0 failed
    - grep -c "1264902" spec/tip_spec.lua returns at least 2 (one in ClassifySpellID test, one in ApplySpell test)
    - grep -c "193265" spec/tip_spec.lua returns at least 2 (one in ClassifySpellID test, one in ApplySpell test)
    - grep "ClassifySpellID" spec/tip_spec.lua returns matches in the new describe block (not just comments)
    - grep "DMX._test.tip.ClassifySpellID" spec/tip_spec.lua returns at least 2 matches
  </acceptance_criteria>
  <verify>
    <automated>npx -y -p fengari@0.1.5 node spec/run.cjs</automated>
  </verify>
  <done>4 new tests pass: ClassifySpellID returns "consumer" for 1264902 and 193265; ApplySpell("consumer", spellID) decrements stacks by 1 for both. Full suite green at 125/125.</done>
</task>

<task type="auto">
  <name>Task 3: Append in-game verification entries to UAT checklist</name>
  <files>.planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md (full file — understand format, current test numbering, scope frontmatter, summary counts)
    - .planning/phases/07-spell-coverage-add-missing-consumers/07-RESEARCH.md (Pitfall 3 — append to existing file, do not create a new one; Pitfall 4 — Moonlight Chakram requires Sentinel hero talent + Takedown cast first)
    - .planning/phases/07-spell-coverage-add-missing-consumers/07-CONTEXT.md (D-04 — variant 1264949 flagged for UAT)
  </read_first>
  <action>
Three edits to .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md per SC-4:

1. Frontmatter scope line: change scope value from "full-milestone smoke test (phases 01-05)" to "full-milestone smoke test (phases 01-07)" to reflect the expanded coverage.

2. Append two new test entries after the existing test #8 block and before the "## Summary" section. Number them 9 and 10:

   Test 9 — Moonlight Chakram (1264902) consumes 1 stack:
   - expected: With Sentinel hero talent active, build 2+ stacks via Kill Command, cast Takedown (which procs Moonlight Chakram replacement for 15 seconds), then cast Moonlight Chakram on target. Tracker should decrement by 1 instantly. Also note: check combat log or /etrace for the actual spell ID fired — confirm it is 1264902 and not the ambiguous variant 1264949 (per D-04 research finding).
   - result: pending

   Test 10 — Hatchet Toss (193265) consumes 1 stack:
   - expected: At 2+ stacks, cast Hatchet Toss (40yd ranged, 30 Focus). Tracker should decrement by 1 instantly. Hatchet Toss is always available to Survival Hunters (no talent gating).
   - result: pending

3. Update the Summary section: total from 8 to 10, pending from 0 to 2. Leave passed/skipped/issues/blocked unchanged.
  </action>
  <acceptance_criteria>
    - grep "phases 01-07" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
    - grep "Moonlight Chakram" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
    - grep "Hatchet Toss" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
    - grep "1264902" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
    - grep "193265" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
    - grep "1264949" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match (variant check note)
    - grep "total: 10" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
    - grep "pending: 2" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md returns a match
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Moonlight Chakram\|Hatchet Toss\|1264949\|total: 10\|pending: 2" .planning/phases/01-utility-extraction-and-module-encapsulation/01-HUMAN-UAT.md</automated>
  </verify>
  <done>UAT checklist has 2 new pending entries (tests 9 and 10) for Moonlight Chakram and Hatchet Toss in-game verification, including the 1264949 variant check note. Summary updated to total: 10, pending: 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client event bus -> addon Lua | Spell IDs arrive via UNIT_SPELLCAST_SUCCEEDED from trusted game engine; addon only classifies them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | CONSUMERS table | low | accept | Spell IDs are integer literals hard-coded in a static Lua table, not user- or network-supplied input. The WoW client event bus is a trusted source. No new input surface introduced by this phase. |
| T-07-02 | Information Disclosure | DMX._test.tip escape hatch | low | accept | Escape hatch exposes a read-only reference to a pure lookup function. No secrets, credentials, or user data involved. The function is already implicitly accessible via the UNIT_SPELLCAST_SUCCEEDED event path. WoW addon environment has no network exfiltration surface. |

No packages are installed by this phase. The supply-chain threat row (T-07-SC) is omitted because no npm/pip/cargo install tasks exist.
</threat_model>

<verification>
1. **Automated (blocking):** `npx -y -p fengari@0.1.5 node spec/run.cjs` exits 0 with 125 passed, 0 failed
2. **Source assertion:** `grep -c "1264902.*true" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns >= 1
3. **Source assertion:** `grep -c "193265.*true" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns >= 1
4. **Source assertion:** `grep -c "DMX._test.tip" Duncedmaxxing/Modules/TipOfTheSpear.lua` returns >= 1
5. **Source assertion:** `grep -c "describe.*ClassifySpellID" spec/tip_spec.lua` returns >= 1
6. **UAT:** 01-HUMAN-UAT.md contains pending entries for both spells
</verification>

<success_criteria>
- CONSUMERS table contains [1264902] = true (Moonlight Chakram) and [193265] = true (Hatchet Toss) — SC-1
- ClassifySpellID tests pass for both new spell IDs via DMX._test.tip escape hatch — SC-2
- ApplySpell decrement tests pass for both new spell IDs — SC-3
- UAT checklist has 2 pending in-game verification entries including the 1264949 variant check — SC-4
- Full test suite passes at 125/125 via fengari harness — SC-5
</success_criteria>

<output>
Create `.planning/phases/07-spell-coverage-add-missing-consumers/07-01-SUMMARY.md` when done
</output>
